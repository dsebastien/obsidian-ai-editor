# 2026-07-29

## Done

- Repo initialized from obsidian-plugin-template (init script, class renames to `AIEditorPlugin`, template tooling removed). Validate + build green from the start.
- v1 implementation plan written and iterated through three grilling rounds with Sébastien: `documentation/plans/ai-editor-v1-implementation-plan.md`. 30+ locked decisions (see plan §3), inspiration analysis of Juri Strumpflohner's Quill demos (frame-by-frame) and Maggie Appleton's LM Sketchbook.
- Adversarial plan review by codex (`gpt-5.6-sol`, xhigh): 8 blockers / 24 majors / 3 minors — verbatim in `documentation/reviews/2026-07-29-plan-review-codex-gpt-5.6-sol.md`, all adopted into plan §4c and the reordered M0-M9 milestones.
- GitHub issues #1-#9 created for the post-v1 backlog (tag review, related notes, references + citation tracking, learning loop, interview drafting, community packs, publish gates, metadata review, session summary).
- README rewritten: pitch + explicit inspiration credits (Maggie Appleton, Juri Strumpflohner, with links).
- **M0 started**:
    - Canonical docs filled: `Business Rules.md` (12 locked product invariants), `Architecture.md`, `Domain Model.md`, `Configuration.md`.
    - `manifest.json`: `isDesktopOnly` set to `true` (review blocker #8).
    - Domain layer (pure, no Obsidian imports):
        - `domain/ids.ts` — branded IDs + UUID generation.
        - `domain/snapshot.ts` — `DocumentSnapshot` with cyrb53 content hash.
        - `domain/anchoring/normalize.ts` — typographic normalization with reversible source-offset map.
        - `domain/anchoring/match.ts` — exact → normalized quote matching, occurrence disambiguation via prefix/suffix/occurrence hints; ambiguous = never guessed (display-only).
        - `domain/anchoring/anchor.ts` — anchor mapping through `TextChange` lists (CM6 `ChangeDesc` adapted at the ui boundary), stale detection, `verifyPrecondition` for Accept.
        - `domain/operations/contract.ts` — versioned discriminated operation contract (review, transform-selection, insert-at, refine-proposal, thread-turn, aggregate-panel) with bounded Zod schemas, typed `PanelResult`, evidence entries, `OperationEvent` union with exactly-once terminal semantics (review blocker #1, majors #17/#27/#33).
    - 63 new domain tests (106 total, all passing); tsc/lint/build green.

## Decisions

- Anchoring ships without a Levenshtein fuzzy tier for now: exact + normalized + hint disambiguation covers the realistic quoting drift; fuzzy matching would risk confidently wrong anchors (review blocker #5). Revisit only with evidence from real runs.
- Domain layer stays CM6-free via the `TextChange` abstraction.

## Open / next

- M0 remaining: transport spike (fetch SSE reader vs buffered `requestUrl`, per provider), CM6 dual-mode decoration spike (needs manual verification in a live vault), CLI protocol probe (headless claude/codex, versioned).
- `git push` is blocked by the vault's Claude Code git-guardrails hook — Sébastien pushes manually.
- Post-implementation adversarial codex review (same model/effort) planned before release (plan M9).

## Adversarial code-review fixes (same day)

Ten confirmed blocker/major findings from three adversarial reviewers, all fixed:

- **Late-finding remap (blocker)**: `ReviewRunHandle` now records every `applyTextChanges` batch and replays the history onto findings that arrive after the user edited — a late finding is remapped to current coordinates or marked stale, never left on snapshot offsets (BR #3/#4).
- **Anchor boundary off-by-one**: `mapAnchorThroughChange` uses inclusive boundary comparisons — deletions/replacements merely touching an anchor boundary shift/no-op instead of wrongly staling the finding.
- **Occurrence semantics**: `matchQuote` resolves `occurrence` against ALL document occurrences (contract definition), with prefix/suffix hints as an independent corroboration check; occurrence/hint disagreement → ambiguous (display-only), never a guessed anchor.
- **Settings salvage granularity**: `loadSettingsDetailed` salvages per entity (array elements, behavior fields) instead of per section — one corrupt backend no longer wipes all API keys, one corrupt scalar no longer resets privacy exclusions (BR #7). Dropped paths are reported; `bootstrapSettings` passes them up and the plugin shows a Notice.
- **Exclusions fail closed**: `isExcluded` with null metadata treats the note as excluded when tag exclusions are configured or the frontmatter opt-out is respected (cold-cache window no longer leaks `#private` notes). Folder matching is now case-insensitive (case-insensitive filesystems).
- **Excluded review target**: `assembleContext` structurally rejects an excluded target with typed `ExcludedTargetError` — no longer dependent on call-site pre-checks.
- **Error redaction seam**: `RunEditorSpec.redactError` — the run controller routes every outbound error message (thrown + error events) through it so provider 401 bodies echoing API keys never reach `EditorRunState.error` (BR #12).
- **Write/read symmetry**: `AIEditorPlugin.update` and the fallback facade validate the produced settings against the schema before `saveData` (reject + keep previous on failure); the rules-tab match-value input skips committing empty values; chip lists surface rejected saves via Notice.

New/updated specs across anchor, match, run-controller, settings-schema, settings-facade, exclusions, and context-assembler (440 tests total, green). tsc/lint/build green.

## M0 continued (same day)

- `services/backends/transport/sse.ts` — stateful, chunk-boundary-safe SSE decoder (mid-line, mid-event, CRLF-split chunks; comment/keep-alive lines; multi-line data; truncated-stream flush) + `parseSseJson` (never throws; contract Zod validation happens at the caller). 17 adversarial-chunking tests.
- 123 tests total, tsc/lint/build green.
- M0 items requiring a live Obsidian runtime remain open: CM6 dual-mode decoration spike and CLI protocol probe — flagged for manual verification.

## Workflow implementation session (M1-M2 slice)

### Implemented (per area)

- **Providers** (`src/app/services/backends/providers/`): pure request builders + response/stream parsers for Anthropic, OpenAI (+ OpenAI-compatible via base URL), Azure OpenAI, Ollama; shared prompt assembly (`prompt.ts`), normalized result/finding parsing (`result.ts`), typed provider interfaces (`types.ts`), registry (`index.ts`). All pure, all spec-covered with shared fixtures.
- **Settings domain** (`src/app/domain/settings/`): Zod schema with `schemaVersion` + migrations scaffold, per-element salvage load (`loadSettingsDetailed`) with dropped-paths report, referential integrity checks; starter pack seeding (`starter-pack.ts`).
- **Orchestration** (`src/app/services/orchestration/`): `RunController` + `ReviewRunHandle` (per-file run lifecycle, streaming finding ingestion, cancellation, edit-replay onto late findings), `FindingStore`.
- **Context assembly** (`src/app/services/context/`): exclusions (folders/tags/frontmatter opt-out, fail-closed on missing metadata), wikilink resolution, `assembleContext` with context budget and typed `ExcludedTargetError` for excluded targets, `VaultReader` seam.
- **Settings UI** (`src/app/settings/`): tabbed shell (Backends / Editors / Panels / Actions / Voice & Style / Rules / Behavior), entity modals, shared components (chip lists, color presets), settings facade + bootstrap with schema validation before every `saveData`.
- **CM6 skeleton** (`src/app/ui/editor/`): finding decorations, rail model + rail view, `ChangeDesc`→`TextChange` adapter. Plus status-bar item (`src/app/ui/status-bar.ts`) and a placeholder `Review current note` command.

### Validation status

`bun run tsc`, `bun run lint` (`--max-warnings 0`), `bun test` (440 pass), `bun run build` — all green. UI-side behavior (settings tabs, Notices, CM6 decorations) is **not runtime-verified**: needs manual checking in a live vault.

### Adversarial review + fixes

Three-reviewer adversarial pass: **17 findings total, 10 blocker/major**. All 10 blocker/major findings verified as real and fixed (0 skipped) — see "Adversarial code-review fixes (same day)" above for the full list (late-finding remap, anchor boundary off-by-ones, occurrence semantics, per-element settings salvage, privacy-preserving salvage report Notice, fail-closed exclusions, case-insensitive folder matching, excluded-target choke point, error redaction seam, write/read schema symmetry). Full suite re-verified green after fixes; nothing was committed mid-review. The 7 remaining minor findings are recorded as **Known review debt** in the plan (§0).

### Open / next

- **Runtime wiring**: `RunController` is not yet connected to the provider request builders + SSE transport — the review pipeline exists as tested units, not an end-to-end flow. → **Closed by the end-to-end review session below.**
- **CM6 manual verification**: decorations/rail need a live vault in Live Preview AND Source mode (agents cannot self-verify GUI behavior).
- **Setup wizard**: not started (M5 originally, pulled forward candidate).
- **Note-ref suggest control**: dedicated vault-note reference picker for prompt fields not built; prompt note refs are plain text for now.
- The placeholder `Review current note` command must not ship in a release (see Known review debt). → **Closed: the command is now the real flow.**

## End-to-end review flow (M2 wiring session, same day)

### Implemented

- **`src/app/services/review-service.ts`** (Obsidian-free, spec-covered — 24 tests): one review run end-to-end. Order of enforcement: exclusion refusal (typed `excluded` result, checked BEFORE anything, BR #7) → size guard (`needs-confirmation` above `behavior.sizeWarningWords`, requires explicit `confirmedLargeNote` retry) → editor resolution (enabled + review-capable; backend = `editor.backend ?? settings.defaultBackend`; typed skip report: `no-backend-configured`, `backend-not-found`, `backend-disabled`, `cli-backend-unsupported`, `no-model-configured`, `no-review-capability`) → per-editor `assembleContext` (attachments serialized into the system prompt as `<context-note path="…">` blocks) → `createApiEditorExecutor` (adapter `buildRequest` → injected `fetchImpl` with `AbortSignal` → HTTP status mapping (401/403 auth, 429 rate-limit, 408/504 timeout, ≥500 network) → `parseBufferedResponse` → single result event; `redactError` bound to the backend key, BR #12) → `RunController.startRun`. Buffered baseline; streaming is a marked seam.
- **`src/app/ui/review-controller.ts`**: per-MarkdownView glue. Mounts the `PersonaRail` into `view.contentEl` (wrapper div, absolute top-right, popout-safe via `ownerDocument`, hidden in reading view); subscribes to the `RunHandle` and projects findings into the CM6 decoration field (`setFindingsEffect`/`clearFindingsEffect`, per-editor color, stale flag, no-op-dispatch dedupe); forwards CM6 doc changes to `runHandle.applyTextChanges` via a single `EditorView.updateListener` extension (focused view only — Obsidian mirrors edits into linked panes and forwarding the replay would double-apply; safe because Accept re-verifies preconditions, BR #3); refresh is coalesced through a 0-ms timer because run notifications fire inside CM6 update cycles (no dispatch-in-update). Also: `ObsidianVaultReader` (Vault/MetadataCache → `VaultReader`), `SizeConfirmModal` (no `window.confirm`), status-bar count of open findings for the (sticky) active markdown file, reveal-finding (open file if needed, select span briefly, collapse selection after 1.5 s if untouched), and the single sanctioned `editor.cm` accessor (`editorViewOf`, instanceof-guarded, documented).
- **`src/app/ui/side-panel.ts`**: `ReviewSidePanelView` (`ItemView`, type `ai-editor-review`, right leaf). Dumb view over a `SidePanelBinding` pushed by the controller (and pulled via provider on open); subscribes to the run for live updates. Renders: file name, skip notices, per-editor section (color dot, name, status, verdict badge, redacted error, summary), findings with severity icon + critique/quote excerpts (click/Enter → reveal in editor), stale findings marked non-clickable, unanchored findings under "Not anchored".
- **Commands** (`src/app/commands/review-current-note.ts`, placeholder replaced): `Review current note` (`checkCallback`: active markdown view + not excluded) and `Open review panel`. Named "Open review panel" (not "Open AI Editor panel") — command names must not contain the plugin name (AGENTS.md / catalog rule).
- **`src/app/plugin.ts`**: owns `RunController` + `ReviewController`; registers the editor extension pair (decoration field + update listener), the panel view type, commands; `onunload` disposes UI glue then `cancelAll()` so no request outlives the plugin.
- **`src/styles.src.css`**: rail (button, dots with `--ai-editor-editor-color`, pulse animation, count badges), finding highlights (`color-mix` of the per-editor color custom property — light+dark safe; stale = dashed underline + reduced opacity), side panel (sections, severity colors, verdict badges, quote styling). Obsidian CSS vars throughout.

### Decisions

- **Buffered-only transport for now**: adapters advertise streaming capabilities and the SSE decoder exists, but no per-provider stream decoder is wired; the executor does one fetch → one result event. Renderer `fetch` (not `requestUrl`) is the deliberate transport (streaming-capable later; `requestUrl` cannot stream); `requestUrl` fallback for CORS-blocked providers is a marked seam.
- **Edit forwarding from the focused view only**: linked panes replay the same edit as separate transactions; forwarding all would double-apply changes to the anchor store. A missed remap from a programmatic background edit cannot corrupt an apply (Accept precondition, BR #3).
- **Binding rules**: not applied yet — all enabled review-capable editors run (SEAM M6 marked in review-service).
- Skips are always reported (Notice + panel), never silent.

### Validation

`bun run tsc`, `bun run lint` (0 warnings), `bun test` (544 pass), `bun run build` — all green. **Not runtime-verified** (needs a live vault): rail placement/overlap in Live Preview + Source + popouts, decoration rendering, panel behavior, size-confirm modal, reveal-scroll, status bar.

### Open / next (after this session)

- Manual runtime verification of the whole flow (see above) — agents cannot self-verify GUI behavior.
- M3 triage: cards, diffs, accept/reject (FindingStore transitions exist, no UI).
- Streaming decoders per provider; `maxConcurrentRequests` enforcement; `requestUrl` CORS fallback.
- Known review debt items in plan §0 still open (dedupe key, run-map eviction, etc.).

## Minimal review cards (click-a-highlight) — parallel agent session

- `src/app/ui/editor/finding-card.ts` (new): floating review card opened by clicking a finding highlight. Pure DOM + CM6 (no Obsidian imports, popout-safe via the view's `ownerDocument`); single active card per view; Escape / click-away / external doc change / scroll closes it. Sections per finding when spans overlap (innermost-first): editor color dot + name, severity badge, critique, quoted text, plain old/new suggestion preview (no word-level diff yet), Accept (rendered only with a suggestion, enabled only when `acceptable`, re-verified through the injected lookup before dispatching the replacement as one undoable transaction — BR #2/#3), Dismiss, and a disabled push-back input labeled "Coming soon" (thread wiring is a later milestone).
- Injection seam for the review controller: `FindingLookup` (`getCardData` / `acceptFinding` / `dismissFinding`) + `findingCardExtension(lookup)` factory (ViewPlugin with a `click` event handler on `.ai-editor-finding`). The controller must route `acceptFinding` through `FindingStore.accept` and mark dismissals.
- Additive hooks in `finding-decorations.ts`: `removeFindingsEffect` (drop marks of accepted/dismissed findings, composable with a change in the same dispatch) and `findingSpansAt(state, pos)` (candidate spans for the card).
- Pure helpers spec-covered (`finding-card.spec.ts`): `computeCardPosition` (below-then-flip-above placement, viewport clamping) and `selectFindingsAtPos` (inclusive boundaries, innermost-first ordering, dedupe); `finding-decorations.spec.ts` extended for the new effect + accessor.
- CSS: `ai-editor-finding-card*` block in `src/styles.src.css` (distinct from the settings `ai-editor-card*` names) + `cursor: pointer` on `.ai-editor-finding`.
- tsc / eslint (`--max-warnings 0` on touched files) / full `bun test` (520) / build all green. Card behavior needs manual verification in a live vault (Live Preview + Source mode, popout window) once the review controller wires the lookup.

## Integration pass (same day) — end-to-end seams closed

Closed the cross-scope seams left by the five parallel implementer sessions (transport executor, api-backend glue, vault reader, review flow, review cards):

- **Review card wired and reachable**: `plugin.ts` registers `findingCardExtension(reviewController.findingLookup())` next to `findingDecorationsField`. `ReviewController.findingLookup()` implements the `FindingLookup` seam: `getCardData` (null for terminal statuses, `acceptable` from `FindingStore.isActionable`), `acceptFinding` → `FindingStore.accept` (precondition against current text, BR #3) then `run.applyTextChanges` for the about-to-be-dispatched edit, `dismissFinding` → `FindingStore.dismiss`. The controller's CM6 update listener now skips card-originated transactions (recognized by `removeFindingsEffect`) — the card dispatches while focus is on the card, so the focused-view rule alone would miss the accept edit; applying it in `acceptFinding` + skipping it in the listener keeps the domain store consistent without double-apply. Decoration re-projection already excludes terminal statuses, so dismissed/accepted highlights never reappear.
- **`RunController.findRunWithFinding(findingId)`** (new, spec-covered): resolves a clicked finding id to its run without the card knowing the file.
- **Streaming executor wired**: `review-service.ts` now builds `RunEditorSpec`s via `createEditorSpec` → `backends/api-editor-backend.ts` `createApiEditorExecutor` (streams Anthropic/OpenAI/openai-compatible SSE with progress events; Azure/Ollama buffered; whole-operation timeout `API_REVIEW_TIMEOUT_MS` = 300 s; status-only error messages per BR #12; `redactError` still bound to the backend key as defense in depth). The service's local buffered executor duplicate (`createApiEditorExecutor`, `httpStatusToErrorCode`, body-excerpt errors) was deleted; its spec sections were replaced (SSE-framed Anthropic fixtures for the now-streaming path, integrated 401-no-key-leak and invalid-config-no-network tests). Transport/protocol behavior remains covered by `api-editor-backend.spec.ts`.
- **Vault reader consolidated**: `review-controller.ts` now uses the dedicated `src/app/ui/obsidian-vault-reader.ts` (fail-closed on cold caches per BR #7, markdown-only, subpath stripping, frontmatter tag tolerance) instead of its inline, less defensive duplicate (deleted).
- **Docs**: `docs/usage.md` documents the card interaction (accept/dismiss shipped; push-back still "coming next"); plan §0 M2 row updated (integrated items + remaining seams renumbered).

Notes / debt: `transport/executor.ts` remains an unwired generic transport overlapping `api-editor-backend`'s inline transport — consolidation candidate for M3, not a runtime issue (tree-shaken out of the bundle). `RunController.runs` eviction debt unchanged (plan §0). Multiple markdown views of one file: file-level run with per-view projection; edits forward from the focused view only (limitation documented in `handleEditorUpdate`).

Validation: `bun run tsc`, `bun run lint` (0 warnings), `bun test` (541 pass), `bun run build`, `bun run format` — all green. Runtime verification in a live vault still required (see plan §0 M2 seam 7).

## Adversarial review round 2 — confirmed blocker/major fixes (same day)

Five confirmed findings (2 blockers, 3 majors), all fixed:

- **Focus-keyed edit forwarding (blocker, `review-controller.ts`)**: `handleEditorUpdate` dropped every doc change dispatched while the view lacked focus (search-bar replace-all, programmatic edits from other plugins, menu-driven undo), leaving anchors on stale offsets — with repeated text an unforwarded shift could make Accept's precondition pass against the WRONG occurrence (BR #3/#4). Now one **canonical view per file** (first mounted glue whose refreshed + live paths match) forwards ALL doc-changing transactions, focus ignored; other panes only ever see Obsidian's mirrors and stay silent. The card's Accept edit flows through the same path (directly or as a mirror), so the direct `run.applyTextChanges` in `acceptFinding` and the `removeFindingsEffect` skip in the listener were removed — exactly-once by construction. Misleading "a missed remap can never corrupt an apply" comment corrected.
- **Snapshot gap (major, `review-controller.ts` / `review-service.ts`)**: edits typed while `assembleContext` awaited vault reads were invisible to the run's remap history (findings anchored against the command-time snapshot). `StartReviewInput.refreshSnapshot` re-snapshots the live doc synchronously right before `RunController.startRun` (falls back to the original snapshot when the view switched notes mid-await); the controller's `started` branch now calls `refreshAll()` synchronously (not the deferred `scheduleRefresh`) so `glue.run` is bound — and forwarding active — in the same synchronous block that started the run. Spec-covered (refreshed-snapshot anchoring, null/foreign-file fallback).
- **Duplicate transport (major)**: `transport/executor.ts` was dead code (nothing imported it) duplicating `api-editor-backend`'s live transport, with a second incompatible `TransportError` class. Deleted (with its spec); its superior pieces ported into `api-editor-backend.ts`: `composeAbort` (native `AbortSignal.any`/`AbortSignal.timeout` with behavior-identical manual fallback) and `reader.cancel()` in the stream teardown `finally` (connection released on every exit path). Exactly one `TransportError` type remains. `redactHeaderValues` was not ported: the backend's error messages are status-only by design (BR #12) — no body/header material ever enters a message.
- **Tailwind preflight shipped globally (blocker, `styles.src.css`)**: `@import 'tailwindcss'` bundled the global reset + `html` defaults + `:root` theme variables into `styles.css`, which Obsidian applies app-wide (guideline violation, theme collisions). Now imports only `tailwindcss/theme.css` (`layer(theme) theme(inline reference)`) + `tailwindcss/utilities.css`: no preflight, no `:root` emission at all — used theme values are inlined into each generated utility. Verified in `dist/styles.css` (0 preflight rules, 0 `:root`); only Tailwind-internal `--tw-*` `@property` registrations remain.
- **Dead What's New styles (major, `styles.src.css`)**: the whole dialog block targeted `.my-plugin-whats-new-*` while `whats-new-modal.ts` builds `${manifest.id}-whats-new-*` (= `ai-editor-whats-new-*`) at runtime — all rules dead. Renamed all 12 selectors, documented the runtime-prefix contract in the CSS comment, and added a drift guard to `scripts/build.spec.ts` (selectors must match `manifest.id`; source must never import Tailwind preflight).

Validation: `bun run tsc`, `bun run lint` (0 warnings), `bun test` (520 pass — executor spec removed, 5 tests added), `bun run build`, `bun run format` — all green. Canonical-view forwarding and the mirror behavior across panes/popouts still need **manual runtime verification** in a live vault (two panes on one file: type in each, replace-all without editor focus, Accept from either pane).

## E2E wiring session

Session wrap-up covering the full end-to-end wiring pass (details in the sections above).

### What landed

- **Transport executor** (streaming SSE execution; superior pieces later folded into the backend after the dead-twin consolidation).
- **API backend glue**: `src/app/services/backends/api-editor-backend.ts` — `createApiEditorExecutor` streams Anthropic/OpenAI/openai-compatible SSE with progress events, buffered Azure/Ollama, 300 s whole-operation timeout, status-only errors (BR #12), `composeAbort` (native `AbortSignal.any`/`timeout` + manual fallback), `reader.cancel()` teardown.
- **Vault reader**: `src/app/ui/obsidian-vault-reader.ts` — fail-closed on cold caches (BR #7), markdown-only, subpath stripping, frontmatter tag tolerance.
- **Review service + controller**: `src/app/services/review-service.ts` (exclusions → size guard → editor/backend resolution → `assembleContext` → executor → `RunController.startRun`, with `refreshSnapshot` re-snapshot right before start) and `src/app/ui/review-controller.ts` (per-view glue, canonical-view edit forwarding, rail, status bar, size-confirm modal, reveal).
- **Side panel**: `src/app/ui/side-panel.ts` — per-editor sections, findings with click-to-reveal, Not-anchored group, skip notices, live subscribe.
- **Finding cards**: `src/app/ui/editor/finding-card.ts` — click-a-highlight floating card, Accept/Dismiss wired through `FindingStore`, single undoable transaction.
- **Plugin wiring**: `plugin.ts` registers decorations + card extension + commands; real `Review current note` / `Open review panel` commands.

### Adversarial review

12 findings total, 5 important (2 blockers, 3 majors) — all 5 verified as real and fixed:

1. **BLOCKER — focus-keyed edit forwarding** (`review-controller.ts`): replaced the `update.view.hasFocus` criterion with canonical-view forwarding — one designated view per file (first mounted glue whose refreshed+live paths match, `canonicalGlueFor`) forwards ALL doc-changing transactions exactly once (programmatic edits, replace-all, menu undo included); the card's Accept edit now flows through the same path (directly or as Obsidian's mirror), so the direct `run.applyTextChanges` in `acceptFinding` and the `removeFindingsEffect` skip were removed; misleading safety comment corrected.
2. **MAJOR — snapshot gap**: added `StartReviewInput.refreshSnapshot` (`review-service.ts`) — the service re-snapshots synchronously immediately before `RunController.startRun`, falling back to the command-time snapshot when the view switched notes mid-await; the controller passes the callback and its `started` branch now calls `refreshAll()` synchronously (not deferred `scheduleRefresh`) so `glue.run` is bound in the same synchronous block that starts the run; 2 new spec tests (refreshed-snapshot anchoring at shifted offsets; null/foreign-file fallback).
3. **MAJOR — duplicate transport**: deleted dead `src/app/services/backends/transport/executor.ts` (+spec, nothing imported it) and ported its superior pieces into `api-editor-backend.ts` — `composeAbort` and `reader.cancel()` in the stream `finally`; exactly one `TransportError` type remains (`redactHeaderValues` intentionally not ported: backend errors are status-only per BR #12).
4. **BLOCKER — Tailwind preflight**: `src/styles.src.css` now imports `tailwindcss/theme.css layer(theme) theme(inline reference)` + `tailwindcss/utilities.css` — `dist/styles.css` verified to contain zero preflight rules AND zero `:root` theme variables (values inlined into utilities; only `--tw-*` `@property` registrations remain).
5. **MAJOR — dead What's New styles**: all 12 `.my-plugin-whats-new-*` selectors renamed to `.ai-editor-whats-new-*` (matches runtime `${manifest.id}-whats-new-*`), CSS comment documents the runtime-prefix contract, and `scripts/build.spec.ts` gained drift guards (selectors must match manifest.id; no preflight import).

The 7 remaining minor findings are recorded as review debt in plan §0 (reader-cancel in `executeStream` finally, `appliedChanges` growth after settle + run eviction, dead `markStaleEffect`, CORS-bound `window.fetch`, `skipsByFile` leak/staleness, `revealFinding` timer vs detached editor, finding-card missing resize repositioning).

### Validation

`bun run tsc`, `bun run lint` (0 warnings), `bun test` (520 pass), `bun run build`, `bun run format` — all green.

### Remaining gaps

- **Manual runtime verification in a live vault** — the whole flow has never run inside Obsidian (see plan §0 M2 checklist). Special attention: two panes on one file (type in each, replace-all without focus, Accept from either pane, popout window).
- Binding rules don't filter editors yet (SEAM M6); CLI backends skipped with typed reason (SEAM M7).
- `maxConcurrentRequests` not enforced; `requestUrl` CORS fallback not implemented.
- Push-back thread input is a disabled placeholder (M3).
- Minor review debt in plan §0 (7 items, fix opportunistically).

## Live vault verification session (first end-to-end run)

Verified in Sébastien's live vault via obsidian CLI + eval bridge, with a local Ollama backend (qwen3:4b) and only the Concision Editor enabled, against a deliberately flawed smoke-test note:

**WORKS end-to-end**: plugin enable + starter pack seeding + settings persistence across reloads; programmatic settings facade (`update`); review run → Ollama → 4 findings, ALL exactly anchored (from/to verified), each with critique + suggestion; run lifecycle (running → done), error normalization (timeout + invalid-output paths both exercised for real); CM6 highlight decorations render (8 spans for 4 findings across line breaks); rail renders with Review button; side panel opens; build → copy → hot-reload dev loop.

**Fixes made during verification** (commit this session):

1. Ollama adapter now sends `think: false` — thinking-family models (qwen3, deepseek-r1) burned the whole 300s timeout on reasoning before emitting JSON.
2. `validateOperationResult` now clamps over-long advisory prefix/suffix hints (tail/head-preserving) before Zod validation — small models overshoot the 200-char bound and the whole result was rejected for it.

**Bugs found, NOT yet fixed (next session)**:

1. Side panel renders a STALE run (showed the previous failed run's error while the new successful run had 4 findings) — panel binding must re-subscribe when startRun replaces a file's run.
2. Status-bar finding count never updates (empty/hidden with 4 findings present).
3. Review command via `checkCallback` no-ops silently in edge cases (CLI-triggered without focus events); consider a `lastActiveMarkdownFile` fallback + Notice when nothing is reviewable.
4. Finding-card click could not be verified with synthetic DOM events — needs a real user click on a highlight (smoke-test note left in the vault root for this).
5. `plugin:reload` via CLI reported "Failed to reload" while hot-reload succeeded in parallel — benign race, but confirm reload path manually.

**Still open**: Live Preview vs Source mode visual comparison (needs human eyes), card Accept/Dismiss flow, real API providers (Anthropic/OpenAI/Azure — Sébastien to configure in settings UI), panel run, selection scope, exclusions in anger.

## Humanizer upgrade + AI Providers backlog

- Starter-pack Humanizer prompt rewritten from Sébastien's `osk-writing-humanizer` skill: structure-over-vocabulary as law #1, warning/suggestion/info severity ladder mapped from the skill's P0/P1/P2 (credibility killers, machine-smell patterns incl. invented-majority, engagement bridges, hollow-emphasis pointers, insider-secret framing, headline templates; polish tier), don't-over-polish + rewrite-vs-patch-threshold rules, voice-profile-overrides-are-authoritative clause (user signature phrases must never be flagged). Live vault instance reseeded with the new prompt (backend config preserved).
- AI Providers plugin (pfrankov, maintained) support added to the plan backlog as §7b item 11 + issue #10.

## Humanize base action

- 'humanize' added to the built-in action verbs (schema + labels + descriptions) — bindable to any editor/panel like the rest; natural default binding: the Humanizer editor.
- Plan gains an "Action vocabulary expansion" step (post-M3) with candidate verbs and selection criteria.

## Double-load race fix (registerView)

- User hit "Plugin failure: Attempting to register an existing view type ai-editor-review" — a deploy-triggered hot-reload overlapped a manual reload/user interaction, leaving the dying instance's view registration alive; the new onload threw and the plugin died.
- Fix: `registerReviewPanelView` heals the race — on registration failure it drops the stale registration (guarded private `viewRegistry.unregisterView`) and retries once, binding the creator to the live instance; worst case (API absent) it logs and continues with a degraded panel instead of a dead plugin. onload can no longer be killed by this race.
- Process note: do not deploy to the vault while Sébastien is actively using Obsidian without telling him first.

## Card width + panel binding fallback (user feedback)

- Finding card widened: min(560px, 94vw) with more section padding/gap ("too narrow, needs air").
- Side panel "No review yet" while highlights existed: `getPanelBinding` now falls back to the live active MarkdownView when event-fed sticky tracking is empty (post-reload state) — same root cause family as the command no-op edge.

## Card flow verified + interaction surfaces plan step

- Sébastien verified the finding card end-to-end: highlight click opens the card, Accept applies, Dismiss clears. Wider card + panel fallback confirmed working.
- Plan gains an "Interaction surfaces design" step (M3 prerequisite): editor context menu (selection), file-explorer context menus incl. multi-note collections (batch review), full command-palette inventory, and Obsidian CLI extensions following obsidian-note-toolbar's CliHandlers pattern (flagged as needs-research).

## Review-debt closure sweep (later session)

All 14 known review-debt items from plan §0 fixed and spec-covered in four commits (`75dee05` orchestration, `c3499d2` settings, `f11180d` UI, `fa066a4` transport). Highlights: occurrence-aware finding dedupe; `RunController.discardRun` + cleared `cancelAll` + vault rename/delete lifecycle hooks (runs and skip reports follow the file); `resolveIdCollisions` — global entity-id uniqueness on load (keep-first, later ids regenerated, persisted + Notice; kills the sync-merge wrong-API-key routing hazard); status-bar count bug fixed via shared active-file resolver (live-view fallback); CORS named in opaque fetch failures. Starter-pack prompt word ceiling 500 → 700 (Humanizer runs long). `bun run validate` green, 532 tests.

## Interaction surfaces design (workflow: 4 researchers + designer + 3 adversarial critics)

Produced `documentation/plans/interaction-surfaces-design.md` — the M3-prerequisite design step from the plan. Verified against Obsidian typings and obsidian-note-toolbar source:

- `registerCliHandler` IS public API (`@since 1.12.2`, `CliData`/`CliFlags`/`CliHandler` exported) — the repo's pinned 1.12.0 typings just predate it. CLI surface = GO: bump typings to 1.12.3, guard `Platform.isDesktop && requireApiVersion('1.12.2')`, one `ai-editor:review` subcommand with JSON findings output.
- No submenus (`setSubmenu` not public) → flat editor context menu with `setSection` groups; no default hotkeys (community guideline); stable command ids derived from entity UUIDs so hotkeys survive renames; dynamic per-action/per-editor command registration diffing on settings mutations (needs a facade mutation observer).
- Batch review (files-menu multi-selection) deferred post-M4 — no cost estimation machinery yet; register nothing rather than shipping a disabled placeholder.
- Critique pass killed two spec errors before implementation: invented default hotkeys, wrong icon class for Review selection.

Open questions for Sébastien (also at the end of the design doc): (1) freeform "Ask an editor" as modal vs card-input only; (2) batch deferral OK?; (3) CLI: `review` alone or also `cancel`/`status`?

## Interaction surfaces slice 1 — foundations (typings bump, reviewability, settings observer)

Implemented design §5 slice 1 (commits `3cd53aa` build(deps), `64afd8e` feat(services)):

- `obsidian` devDependency 1.12.0 → 1.12.3. Verified `registerCliHandler`, `CliData`, `CliFlag`, `CliFlags`, `CliHandler` are present in the typings; tsc clean with no fallout.
- New `src/app/services/reviewability.ts`: `isReviewable(path, metadata, settings)` = not privacy-excluded AND `hasReviewCapableEditor` (enabled + review capability + backend resolves via `resolveApiBackend`). Re-exports `isExcluded`; zero duplicated exclusion logic. `ReviewController.canReview` now uses it, so `Review current note` also hides when no editor could dispatch (previously it only checked exclusion and surfaced a "no editors" Notice). Menus + CLI slices will share the same predicate.
- Settings facade mutation observer: `SettingsFacade.subscribe(listener)` → unsubscribe fn, notified after every successful update/persist, never on rejected updates. `createSettingsNotifier` (listener set, snapshot iteration, throwing listeners isolated) is used by the fallback facade, by a wrapper around subscribe-less hosts, and by `AIEditorPlugin` (notify in `persistSettings` — covers load-time repair saves too). Prereq for dynamic command registration diffing (design §3).
- Specs: `reviewability.spec.ts` (16 tests) + 7 new facade observer tests. `bun run validate` green (555 tests), `bun run build` green.

Open: slices 2-6 of the interaction-surfaces design (selection scope plumbing next).

## Interaction surfaces slice 2 — selection scope plumbing

Implemented design §5 slice 2 (commit `c3508d6`), the selection-capture contract from design §1:

- `StartReviewInput.requestedSelection?: { from, to }` — offsets into the capture-time snapshot's text, captured synchronously by the caller (menu item / command callback).
- New pure `isRequestedSelectionValid(requested, capturedHash, freshSnapshot)`: non-empty ordered range, in bounds against the fresh text, and text hash unchanged since capture (offsets into changed text are meaningless even when they fit — hash mismatch always invalidates). Bounds + hash are what's validatable; both are validated.
- Valid → the run snapshot carries exactly the requested selection, overriding any live selection the refreshed snapshot captured on its own. Invalid → whole-note scope (stale live selection stripped too) and `selectionFallback: true` on the `started` result.
- `ReviewController.startReview` gained an optional `requestedSelection` param: passes it through, preserves it across the size-confirmation round-trip, and shows "Selection changed — reviewing the whole note" when the service reports the fallback. No current caller passes a selection yet — slices 3 (editor context menu) and 5 (Review selection command) will.
- Whole-note path byte-for-byte compatible: without `requestedSelection` the snapshot flows untouched (identity-pinned in specs), including the legacy snapshot-carried selection behavior.
- Specs: 4 pure-validator tests + 6 `startReview` tests (valid propagation, refresh override, out-of-bounds / degenerate / changed-document fallbacks, legacy path). `bun run validate` green (565 tests), `bun run build` green.

Open: slices 3-6 of the interaction-surfaces design (editor context menu next).

## Interaction surfaces slice 3 — context menus

Implemented design §5 slice 3+4 (commit `9a5ed7c`), the editor and file context menus from design §1/§2:

- Pure `src/app/ui/menus/menu-model.ts` (spec-covered, 9 tests): `editorMenuItems` / `fileMenuItems` decide which items appear given surface state; the Obsidian `Menu` wiring stays thin. No disabled placeholders ever — an item that cannot dispatch is not offered.
- Editor menu (`editor-menu.ts`): "Review selection" (`message-circle`, section `ai-editor`) when the selection is non-empty, the view is an editable `MarkdownView`, and the note passes `isReviewable`. Selection offsets are read synchronously in the click callback and passed as `requestedSelection` — the slice 2 plumbing re-validates at run start and the fallback Notice ("Selection changed — reviewing the whole note") was already wired in `ReviewController.startReview`. v1 ships review-class items only; non-review action verbs (rephrase, …) have no dispatch path until the M3 transform operations.
- File menu (`file-menu.ts`): for a single reviewable `.md` `TFile` — "Review note" (opens the file when not open, then dispatches the exact same whole-note review as the command via new `ReviewController.reviewFile`) and "Open review panel" (`bot`). Both grouped via `setSection('ai-editor')`. `files-menu` deliberately not registered (batch review deferred post-M4).
- `ReviewController`: public `canReviewPath(path)` (closed-note reviewability for file menu / CLI; `canReview(view)` delegates), private `openMarkdownView(filePath)` helper extracted from `revealFinding` and shared with `reviewFile`.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 574 tests), `bun run build` all green.

Open: slices 5 (command inventory incl. `review-selection` command) and 6 (CLI handler). Live-vault check pending: menu items appearing/grouping in the real app.

## Interaction surfaces slice 4 — static command inventory

Implemented design §3's static commands (commit `b6827c6`); dynamic/triage commands deferred:

- New commands (stable ids, no default hotkeys): `review-selection` (editorCheckCallback: editable `MarkdownView` + non-empty selection + reviewable; selection captured synchronously via new shared `ReviewController.startSelectionReview` — editor context menu refactored onto the same entry, local helper removed), `cancel-run` ("Cancel review": unsettled run for the active file, read through new `ReviewController.getActiveRun`), `next-finding`/`prev-finding` (step through the active run's revealable findings — open/preview + live anchor — ordered by anchor position with wrap-around; cursor-relative so repeated invocations cycle; reveal reuses the side-panel reveal path).
- Pure modules (spec-covered): `src/app/commands/command-gates.ts` (`canReviewSelection`, `canCancelRun`) and `src/app/commands/finding-navigation.ts` (`navigableFindings` filter+ordering, `stepFinding` cursor stepping). 20 new tests.
- Registrar renamed `review-current-note.ts` → `review-commands.ts` (hosts all six commands); `plugin.ts` import updated; `.gitkeep` dropped.
- Deferred with rationale in the plan row (no non-functional UI): accept/dismiss current finding, `filter-severity`, dynamic `action-<id>`, per-editor accept/dismiss-all — the slice 1 settings observer stays unused until dynamic registration exists.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 595 tests), `bun run build` all green.

Open: CLI handler (design slice 6). Live-vault check pending: command availability/hiding in the palette, finding navigation cycling in a real note.

## Interaction surfaces slice 5 — CLI handler

Implemented design §4 (design slice 6), the `ai-editor:review` CLI subcommand (commit `fa58134`):

- Pure core `src/app/services/cli/review-cli.ts` (Obsidian-free, 36 specs): `handleReviewCli(params, deps)` parses CliData args (`file` required; `editors` comma-separated ids-or-names — names case-insensitive, unknown tokens fail the whole selection so a typo never silently narrows a review; `format` json default | text; `confirm-large` boolean = presence), resolves/reads the note via injected deps, narrows settings to the requested editors, dispatches through the injected runner, awaits `run.settled`, and renders one document: `{ ok, file, findings[{ id, editor, severity, quote, critique, suggestion, anchor }], skips, summaryByEditor, error }` or one text line per finding. Never throws — every failure is a typed error document (`file-not-found`, `excluded`, `needs-confirmation`, `no-editors`, `backend-error`, `timeout`).
- Redaction (BR #12): per-editor failure messages come off `EditorRunState.error` (already routed through the run's `redactError` seam); unexpected pipeline exceptions render as status-only "The review failed unexpectedly" — their message is never echoed.
- Output accounting: every participating editor lands in exactly one of findings/summaryByEditor/skips; post-run failures are appended to `skips` as `backend-error`/`timeout`/`cancelled`; overall `ok` is true when ≥1 editor completed, else the error code is `timeout` iff every failure was a timeout.
- Glue `src/app/cli/register-review-cli.ts`: file resolution tolerant like wikilinks (`getFileByPath` with/without `.md`, then `getFirstLinkpathDest`; markdown only), `ObsidianVaultReader` for reads/metadata, the SHARED `RunController` + `startReview` + `window.fetch` — a CLI run on an open note shows in rail/panel like any other. Registration in `plugin.ts` guarded by `Platform.isDesktop && requireApiVersion('1.12.2')` (runtime-guarded; `minAppVersion` stays 1.8.7). `plugin.ts` now holds the `RunController` in a local const for the narrowed type.
- Not shipped on purpose: `scope` flag (selection unsupported from CLI v1 — reserved in the design; no non-functional flags), streaming (CLI API returns one string), CLI-side backend overrides, background jobs.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 631 tests), `bun run build` all green.

Interaction surfaces design slices 1-6 all landed. Open: live-vault verification of the CLI (`obsidian ai-editor:review --file "Some Note" --format text`), plus the standing live checks from earlier slices. README/docs user-guide section for the CLI not yet written (docs surface, deliberate deferral to the docs pass).

## Interaction surfaces fix pass — adversarial review of slices 2-5

Fixed 6 confirmed findings from an adversarial review of the last five commits (commit `3e0f05f`):

- **CLI ↔ controller integration (major)**: a CLI run on an OPEN note bypassed the `ReviewController` — nothing bound `glue.run` (only workspace events or the controller's own `started` branch refresh), so edits typed during the run never reached `applyTextChanges` and anchors drifted (repeated-text Accept could pass against the wrong occurrence); the CLI also snapshotted saved vault state while the buffer held unsaved edits. Now: `registerReviewCli` takes the `ReviewController`; open note → live editor-buffer snapshot via `cliRunBinding` (whole-note scoped, with `refreshSnapshot`) + synchronous `bindCliRun(path, skips)` (same invariant as the `started` branch: bound before any keystroke can interleave). Not open → vault snapshot as before.
- **`reviewFile` selection leak (major)**: file-menu "Review note" delegated to `startReview(view)` whose `snapshotView` embeds a live selection — right-clicking an open note with a selection reviewed only the selection with no notice. Now `snapshotView`/`startReview` take a `SnapshotScope` (`'auto' | 'whole-note'`); `reviewFile` and the CLI use `'whole-note'`. Selection scope is exclusively the `requestedSelection` contract.
- **Vacuous round-trip re-validation (major)**: the size-confirm retry re-snapshotted AFTER the modal and validated the requested selection against that snapshot's own hash — self-comparison, so stale offsets passed when bounds fit. `requestedSelection` now carries `capturedHash` (service type requires it; the controller fills it from the first snapshot — same synchronous block as capture — and the confirm callback re-passes the filled object). New service spec pins the round-trip fallback (`selectionFallback: true`, selection stripped).
- **Minors**: `selectEditors` rejects explicitly requested disabled editors (`{ ok: false, unknown, disabled }`, typed `no-editors` error — the pipeline drops disabled editors without a skip entry, so the silent partial review was data loss for scripts); `registerReviewCli` call wrapped in try/catch (per obsidian.d.ts `registerCliHandler` THROWS on duplicate registration — double-load race would have failed `onload`; degrade to no CLI surface like the `registerView` heal); CLI runs on unopened notes are discarded after the output document is shaped (new optional `releaseRun(path, run)` dep; glue discards only when no markdown view shows the file AND the run still owns the controller slot); design doc §1/§4 amended (hidden-not-disabled rule, `scope` flag dropped for v1, `anchor: … | null`, `capturedHash` contract) so the design stays the authoritative contract.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 649 tests), `bun run build` all green.

Open: unchanged live-vault checks (CLI end-to-end invocation now also covers the open-note live-buffer path: run a CLI review while the note is open with unsaved edits and confirm highlights land correctly + typing during the run remaps them).

## CLI cancel + status — v1 CLI surface completed (design §6.3 decision)

Sébastien decided the CLI v1 surface is review + cancel + status. Implemented in three commits (`5b48768` refactor, `e34e5f7` cancel, `7be8452` status):

- **Shared machinery first** (`5b48768`, no behavior change): `src/app/services/cli/cli-shared.ts` extracts what all subcommands must agree on — `parseFileFlag`/`parseFormatFlag`, the `CliFinding`/`CliAnchor` shape, `shapeFindings` + `shapeSummaryByEditor` (pulled out of `shapeRunOutput`), and `formatFindingLine` (the `--format text` finding line). `review-cli.ts` re-exports `ReviewCliFinding = CliFinding` so its documented contract is the shared shape by construction. `src/app/cli/resolve-note-path.ts` extracts the wikilink-tolerant `--file` resolver (`getFileByPath` with/without `.md`, then `getFirstLinkpathDest`, markdown only) out of `register-review-cli.ts` — one resolution semantics across subcommands. `src/app/services/cli/spec-fixtures.ts` (same convention as the provider spec fixtures) hosts the shared `FakeRunHandle` (now with `cancelCount` + configurable `settled`) and `makeState`, so review/cancel/status specs shape the SAME run fixtures.
- **`ai-editor:cancel`** (`e34e5f7`): pure `handleCancelCli` (`cancel-cli.ts`, 8 specs) — `file` required (no `format`: JSON only, nothing to list). Resolves the note, looks up the run, cancels iff unsettled. Output: `{ ok: true, file, cancelled: true }` | `{ ok: true, file, cancelled: false, reason: 'no-run' | 'already-settled' }` | typed `file-not-found` error. Cancelling NEVER discards the run — `CancelCliDeps` deliberately has no discard capability (only `getRun`), so findings stay inspectable via `ai-editor:status` and the UI; spec pins the finding store surviving the cancel. Nothing-to-cancel is `ok: true` (the invocation worked; `reason` says why nothing changed).
- **`ai-editor:status`** (`7be8452`): pure `handleStatusCli` (`status-cli.ts`, 16 specs) — the read-only poll surface for external agents driving long reviews (start review → poll status until `settled` → optionally cancel; findings readable throughout). `file` + `format` (json default | text). `{ ok: true, file, run: null }` when no run; else `run: { settled, editors: [{ id, name, status, error }], findings, summaryByEditor }` — findings via the shared `shapeFindings`, byte-identical to review output (spec compares `shapeStatusRun(run).findings` `toEqual` `shapeRunOutput(...).findings` on the same fixture). Per-editor `error` comes off `EditorRunState.error` (already redacted, BR #12). Text mode: one status headline (`Run in progress (1 running, 1 done) — 2 findings so far` / `Run settled (2 done) — 1 finding`) + one line per finding in the exact review line format (`formatFindingLine`). Cross-handler spec: cancel-then-status still lists the findings.
- **Glue + registration**: `src/app/cli/register-run-cli.ts` (`registerCancelCli`, `registerStatusCli`) binds both handlers to the shared `RunController` + the shared note resolver. `plugin.ts` registers all three subcommands inside the same `Platform.isDesktop && requireApiVersion('1.12.2')` gate through a per-subcommand try/catch helper (`guardCliRegistration`) — a double-load race now degrades only the colliding subcommand instead of the whole CLI surface.
- **Docs**: design doc §4 rewritten as the three-subcommand contract (per-subcommand shapes, poll-loop framing, common non-goals); §6 "Open questions" replaced with "§6 Decisions (2026-07-29)": freeform "Ask an editor" = BOTH (context-menu modal entry point, M3-adjacent, AND per-finding push-back threads in the card, M4); batch review deferred until cost estimation exists (confirmed); CLI v1 = review + cancel + status.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 673 tests), `bun run build` all green after each commit.

Open: live-vault check of the new subcommands (`obsidian ai-editor:status --file "Some Note" --format text` while a review runs; `ai-editor:cancel` mid-run then status again), on top of the standing live checks. README/docs user-guide CLI section still deferred to the docs pass (now covering three subcommands).

## CLI verifier follow-ups

Adversarial verify of the cancel/status increment surfaced two minors, both fixed (`555d6c7`): `bad-args` error code (missing flag no longer masquerades as `file-not-found`, all three subcommands), and `ai-editor:status` `settled` derived from editor states so a poll right after cancel never reports "in progress" while every editor already shows cancelled. 674 tests green.

## M3 — incremental stale-marking while the user types

Wired the documented seam in `finding-decorations.ts` (commit `5fd0080`): `markStaleEffect` was defined, handled by the StateField, and spec-covered, but never dispatched — a finding whose anchor went stale mid-edit kept its normal highlight until the deferred full `setFindingsEffect` rebuild (`scheduleRefresh` → setTimeout 0).

- **Pure diff logic** `src/app/ui/editor/stale-diff.ts` (spec-covered without CM6, 9 tests): `staleIds(findings)` snapshots which finding ids are stale, `newlyStaleIds(before, after)` returns the ids that TRANSITIONED to stale — including a late finding that arrives already stale via edit-history replay. Operates on a structural subset of `TrackedFinding`, so `FindingStore.list()` passes straight in; the store stays the staleness authority.
- **Controller wiring** (`ReviewController.handleEditorUpdate`): snapshot stale ids before forwarding the edit batch to `run.applyTextChanges`, diff after, and dispatch `markStaleEffect.of(newlyStale)` to the same view's `EditorView` on a `queueMicrotask` — CM6 forbids dispatching from within an update listener (it would re-enter `EditorView.update`), and the microtask still lands ahead of the deferred setTimeout-0 rebuild. Safe without extra guards beyond `disposed`: dispatch on a destroyed `EditorView` is a CM6 no-op (verified in `@codemirror/view` source), and `markStaleEffect` ignores ids the decoration set no longer holds (note switch, cleared run).
- **Full rebuild untouched**: `dispatchDecorations`' `setFindingsEffect` path is byte-identical and remains the eventual-consistency backstop; the incremental effect only closes the visual gap between the edit and the rebuild.
- `finding-decorations.ts` header updated: staleness now reaches the decorations two ways (incremental `markStaleEffect` from `handleEditorUpdate`, deferred full rebuild) — the "NOT dispatched anywhere yet" caveat is gone.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 683 tests), `bun run build` all green.

Open: live-vault check — type inside a highlighted finding during/after a run and confirm the highlight dims immediately (before the deferred rebuild), plus the standing live checks.

## Concurrency limiter — behavior.maxConcurrentRequests enforced (plan §0 seam #3)

Settings exposed `behavior.maxConcurrentRequests` (1-10, default 3) but every editor executor stream started immediately. Enforced in commit `29bf5e5`:

- **Pure semaphore** `src/app/services/orchestration/semaphore.ts` (9 specs, dependency-free): FIFO admission; dynamic limit read via `getLimit()` at EVERY admission decision (each acquire and each release) — a settings change applies to subsequent acquisitions, permits already held are never revoked; abort-aware `acquire(signal)` — an aborted waiter leaves the queue immediately and rejects with the signal's reason, consuming nothing; idempotent per-permit release (double release frees exactly one slot); limits below 1 clamp to 1 so a bad value can never deadlock.
- **Gate placement**: `RunController` owns ONE `Semaphore` sized by an injected `getMaxConcurrentRequests` provider (default unlimited for headless/test callers) and passes it to every `ReviewRunHandle` — the gate is plugin-wide, so at most N backend requests are in flight across ALL concurrent runs, not per run. `ReviewRunHandle.consume` acquires BEFORE calling `spec.execute` (the stream — and thus the backend request — cannot start without a permit) and releases in a `finally` covering every exit (done, error, cancel, thrown stream).
- **Truthful UI state**: while queued the editor keeps its initial `'pending'` status (verified — `'running'` is only set after admission), so rail/panel show not-yet-started honestly.
- **Cancellation**: `cancelRun` aborts the shared signal → a queued waiter is ejected synchronously and settles as `'cancelled'` without its executor ever being called; no zombie waiters, no leaked permits (spec: a fresh run acquires immediately after). Cancelling a queued run leaves an in-flight run on another file untouched.
- **Wiring**: `plugin.ts` constructs `new RunController(() => this.settings.behavior.maxConcurrentRequests)` — live view, no reload needed. CLI-triggered reviews flow through the same controller, so they share the same gate.
- **Specs**: 5 new `RunController` concurrency specs (N+1 editors with limit N → one pending; release admits FIFO; gate shared across runs on different files; cancel-while-queued ejects + no leak; limit raised between runs applies to the next run). One pre-existing timing-sensitive cancel spec widened from 2 to 5 microtask turns (the acquire await adds one turn before the stream starts).

Gates: `bun run format`, `bun run validate` (tsc + eslint + 697 tests), `bun run build` all green.

Open: live-vault check — configure limit 1 with 2+ editors and confirm the second editor sits pending in the rail until the first finishes; standing live checks unchanged.

## Freeform "Ask an editor" modal — design §6 decision 1 (modal entry point)

The note-level half of decision §6.1 landed (commit `991fbcd`); per-finding push-back threads in the card stay M4. Pick one editor, type an instruction, get a normal selection-scoped review with that editor's prompt augmented for this run only.

- **Modal** `src/app/ui/ask-editor-modal.ts` (thin DOM glue, exempt from specs) over pure `src/app/ui/ask-editor-model.ts` (10 specs): editor picker (dropdown of review-capable editors, default = first in settings order; exactly one → static text, no pointless dropdown), instruction textarea (placeholder "Is this argument convincing?"), Cancel + Ask (CTA, disabled while blank — `canSubmitAsk` and the submitted value both derive from `normalizeInstruction`, so the button can never enable on text that submits as nothing). Keyboard: Enter inserts a newline (native, not intercepted), Ctrl/Cmd+Enter submits, Esc closes (Modal default). Double-submit guarded.
- **Entry points**: editor context menu "Ask an editor…" (`menu-model.ts` now returns `['review-selection', 'ask-editor']` under the same gate — the §6.1 hidden-until-it-exists rule is satisfied) and the `ask-editor` command (editorCheckCallback, same `canReviewSelection` gate as Review selection, no default hotkey).
- **Dispatch seam** (`review-service.ts`): `StartReviewInput.instruction?: RunInstruction {editorId, text}`. Participant pool narrows to exactly the chosen editor — the un-asked editors were not asked, so they are neither run nor reported as skips; an instruction whose editor vanished/disabled yields the typed `no-editors` refusal. The chosen editor's FULLY composed system prompt (persona + attachments) is augmented by pure `augmentSystemPrompt`: framed `<user-instruction>` block appended last (most salient), explicitly subordinated to the output format. Settings never mutated. The instruction is user content riding in the prompt — it cannot break the operation contract structurally: findings still stream through the same tool schema and Zod validation.
- **Selection-capture contract**: `ReviewController.openAskEditorModal` captures the selection range AND the capture-time hash (`hashText`) synchronously in the invoking menu/command callback — unlike `startSelectionReview`, the hash cannot be derived from the dispatch-time snapshot because the modal introduces an arbitrarily long gap (the service would validate stale offsets against themselves). On submit the flow is the regular `startReview` (new optional `instruction` param, threaded through the size-confirm round trip), so rail/panel/highlights/status bar show the run like any review. A selection invalidated while the modal was open falls back to whole-note with the usual Notice.
- **Helper extraction**: `reviewCapableEditors(settings)` in `reviewability.ts` (settings-order list of dispatchable editors; `hasReviewCapableEditor` now derives from it) — the picker can only ever offer an editor `startReview` would accept.
- Specs: 13 new (ask-editor-model 6, menu model updated, reviewCapableEditors 2, `augmentSystemPrompt` 3 + instruction pipeline 2 in review-service — narrowed run, augmented prompt reaching the wire body, un-asked persona absent, no-editors on unknown/disabled id). CSS: `.ai-editor-ask-textarea`, `.ai-editor-ask-single-editor`.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 710 tests), `bun run build` all green.

Open: live-vault check — right-click a selection → "Ask an editor…", confirm dropdown lists exactly the dispatchable editors, Ctrl/Cmd+Enter submits, run appears in rail/panel scoped to the selection, and the finding critiques reflect the instruction; standing live checks unchanged.

## M3 fix pass — adversarial review of the ask-editor + concurrency increment

Adversarial review of the M3 increment surfaced 5 findings (0 serious, 5 minor); all verified against the code and confirmed real. Fixed in commits `88456a7` (orchestration), `5a0b1c3` (UI), plus this docs commit.

- **Concurrency permit released at terminal state** (`run-controller.ts`): the plugin-wide `maxConcurrentRequests` permit was only freed in the consume loop's `finally`, i.e. when the editor stream's iterator ended. After `cancelRun` (or a terminal event) the loop keeps draining discarded events (`continue`), so an executor that ignores its AbortSignal — the `RunEditorSpec` seam where future CLI backends plug in — held its permit indefinitely: with limit N, N such streams deadlocked every future review plugin-wide (editors stuck `pending`) until plugin reload, and cancelling could not reclaim them. Fix: the release function is stored on the editor state and fired the moment the editor goes terminal (`terminate` and `cancelRun`); release is idempotent (semaphore guarantee) and the `finally` stays as backstop. 2 new specs: cancel reclaims the permit from an abort-ignoring executor (a fresh run acquires immediately); a terminal `result` frees the permit while the stream is still draining (queued editor completes even though the first stream never closes).
- **Ask-editor submit refuses a switched note** (`review-controller.ts`): `openAskEditorModal`'s submit called `startReview(view, …)`, which resolves `view.file` at submit time — if the leaf switched notes while the modal was open (programmatic `openFile`, another plugin, workspace restore), the instruction dispatched as a whole-note review of the WRONG note. The note path is now captured at modal-open time; a mismatched submit aborts with a Notice ("The note changed while the dialog was open — ask cancelled.").
- **Ask dispatch scope is `whole-note`, not `auto`** (`review-controller.ts`): the collapsed-capture case (`requested === undefined`) dispatched scope `auto`, so a selection made during the modal's arbitrarily long gap silently narrowed the run — violating design §1's rule that selection scope flows EXCLUSIVELY through the `requestedSelection` capture contract. `whole-note` is safe unconditionally: a valid captured selection overrides the snapshot scope explicitly, and the invalid-selection fallback already strips to whole-note.
- **Docs corrected**: the "Ask an editor" history entry claimed "Specs: 27 new (ask-editor-model 10, …)" — actual delta is 13 (697 → 710; ask-editor-model 6, reviewCapableEditors 2, augmentSystemPrompt 3, instruction pipeline 2), now stated correctly. Design doc drift closed: §3 command table gained the shipped `ask-editor` row (editorCheckCallback, same gate as Review selection), §1 item 3 no longer reads "M4; ships as a hidden item until the modal exists" — annotated as shipped (commit `991fbcd`).

Gates: `bun run format`, `bun run validate` (tsc + eslint + 712 tests), `bun run build` all green.

Open: standing live checks unchanged; new live check — cancel a run against an unresponsive backend (e.g. hung Ollama) with limit 1 and confirm a fresh review starts immediately.

## Slow & thinking models — piece 2: configurable request timeout

First slice of the "Slow & thinking models" increment (plan §0, decisions locked 2026-07-29): the hardcoded whole-operation API timeout is now a behavior setting (commit `28af75e`).

- **Schema**: `behavior.requestTimeoutSeconds` (int, min 30, max 3600, default 600 — laptop-realistic for slow local models; no-users policy: best default outright, no compat shim, `API_REVIEW_TIMEOUT_MS` deleted). Salvage loop picks it up automatically (iterates `behaviorSettingsSchema.shape`).
- **Wiring**: `reviewTimeoutMs(behavior)` (pure s→ms) in `review-service.ts`; `createEditorSpec` gained a required `timeoutMs` input and `startReview` threads the setting into every editor's `createApiEditorExecutor`. CLI reviews (`ai-editor:review`) go through the same `startReview` path, so they share the setting by construction — the per-CLI-backend `timeoutSeconds` (settings-schema) is a different, untouched knob for CLI *backends*.
- **Error message names the setting**: `normalizeError` timeout branch now renders `Provider did not answer within N s — raise 'Request timeout' in settings if your model needs longer.` — a user hitting it (the original Ollama-on-a-laptop report) learns the fix from the message itself.
- **UI**: Behavior tab → Runs section gained the 'Request timeout (seconds)' int field ("How long a single editor's backend request may run — raise this for slow local models."), same clamp-on-change `renderIntField` machinery as the other run guardrails.
- **Specs (3 files)**: schema default (600) + bounds (30/3600, ints only); `reviewTimeoutMs` conversion; `createEditorSpec` threads `timeoutMs` into the transport (hanging fetch + 20 ms timeout terminates with the setting-naming message); backend spec pins the exact message shape.

Gates: `bun run format`, `bun run validate` (tsc + eslint + 715 tests), `bun run build` all green.

Open: pieces 1 (per-editor retry) and 3 (per-backend thinking settings) of the increment still queued; live check — set a tiny-ish timeout, review against a hung backend, confirm the Notice/panel error names 'Request timeout'.
