# 2026-07-30

## Action dispatch — bindings become real (v1 sweep stage C, slice 3)

Stage C of the sweep is code-complete: bound actions now dispatch from the editor context menu AND the command palette, with sensible starter bindings so the feature exists out of the box. Six commits — three recovered from a session killed mid-flight on 2026-07-29 (`4c8584d`, `d670df1`, `9ed101d`, verified against the spec before continuing), three new (`d04b3cc`, `5837b77`, `ac61f7a`).

- **Multi-editor run instructions** (`4c8584d`, recovered): `RunInstruction.editorId` → `editorIds` — the participant pool narrows to exactly those editors and EACH gets the instruction augmented onto its composed system prompt. Ask-an-editor passes one id (unchanged); review-class verbs bound to a panel pass every member id (v1 panel dispatch; charter aggregation stays M6).
- **Action-binding resolution** (`d670df1`, recovered, `src/app/services/actions/action-resolution.ts`): the one decision point for "can this bound action run right now?". Built-in verbs take label/class/instruction from the verb registry; custom actions are transform-class with their instruction resolved fresh at dispatch from the `customInstruction` prompt source (direct text + referenced notes, exclusions honored per BR #7, capped at the operation contract's instruction limit — `followLinks` deliberately not expanded: an instruction is a directive, not context). Editor targets: enabled + class-appropriate capability + `resolveApiBackend` ok. Panel targets: review-class ONLY, fan out to every member (undispatchable members surface as run skips). Typed `ActionInvalidReason` + `actionInvalidReasonLabel` for the settings tab. Undispatchable actions never become UI.
- **Context-menu dispatch** (`9ed101d`, recovered): `editorMenuEntries` lists dispatchable bound actions first — icon by verb class (check/wand-2/message-circle), alphabetical by label, cap 10 — then Review selection / Ask an editor in their existing placement. Bound actions gate on not-privacy-excluded (independent of `isReviewable`: a rewrite-only vault still transforms). `ReviewController.startBoundAction` is the shared dispatch entry: re-resolves against current settings, captures selection + hash synchronously (selection-capture contract), then routes by class — review through the exact `startReview` path narrowed to the resolved editor set with the verb instruction augmented; transform/generate through `startAction` with every refusal surfaced as a Notice and the size guard round-tripping through the copy-aware confirmation modal. Transform with a collapsed selection refuses upfront.
- **Dynamic per-action commands** (`d04b3cc`, `src/app/commands/action-commands.ts` — pure model committed by the killed session, verified, wired into `plugin.ts` here): one `action-<bindingId>` command per DISPATCHABLE binding (design §3). `desiredActionCommands` derives from `resolveActions`; `diffActionCommands` diffs against the registered id→name map (rename re-registers under the unchanged id so user hotkeys survive; unrelated settings mutations are no-ops); `registerActionCommands` syncs via the settings facade's mutation observer — no reload. Per invocation: MarkdownView host + live `resolveActionById` + the new pure `canRunBoundAction` gate (transform = editable + selection, generate = editable, review-class = any mode) + privacy exclusion; dispatch through the same `startBoundAction` as the menu.
- **Starter-pack default bindings** (`5837b77`): `STARTER_ACTION_BINDINGS` — rephrase/summarize/simplify → Concision Editor, humanize → Humanizer, critique/identify-assumptions → Devil's Advocate, find-evidence → Fact Checker; generate verbs (continue, say-more) stay unbound (no starter persona is an authorial voice). Verb id doubles as the binding entity id (the `setBuiltInActionBinding` convention) so `action-<verb>` command ids are stable across installs. Seeding stays idempotent (`starterPackSeeded` guard — no duplication on reinstall); verbs the user already bound are never overridden. No-users policy: starter pack changed outright, no migration.
- **Actions-tab validation** (`ac61f7a`): dropdowns mirror the resolution rule — panels offered only for review-class verbs, custom actions (transform-class) get editors only. Any BOUND action that cannot dispatch shows why under its row ("Hidden from menus and the command palette: <reason>" via `actionInvalidReasonLabel`); unbound stays quiet (deliberate state). Binding changes refresh the tab; intro copy states the panel rule. New `.ai-editor-binding-warning` style.

Specs: resolution matrix (464-line spec, recovered), menu-model extension, command desired-set + diffing, `canRunBoundAction`, starter bindings (persona pairing, capability invariant, generate verbs unbound, user-binding preservation, id stability), multi-editor instruction path. 938 tests total.

Gates: `bun run format`, `bun run validate` (tsc + eslint --max-warnings 0 + 938 tests), `bun run build` all green after each commit. Nothing pushed.

### Open / live checks (UI not self-verifiable)

- Context menu on a selection shows bound actions with class icons, capped and alphabetical; Review selection / Ask an editor unchanged.
- Fresh install (or `starterPackSeeded` reset): the seven default bindings appear pre-wired in the Actions tab and in the menu/palette.
- Palette: `action-<verb>` commands appear/disappear live as bindings change in settings (no reload); a hotkey bound to Humanize survives renaming its editor.
- Transform verb dispatch end-to-end: select text → Humanize → chip pulses "transforming" → inline diff widget → Accept is ONE undo step; Esc rejects.
- Review-class verb bound to the starter panel: every member runs with the instruction; members without a usable backend reported as skips.
- Actions tab: bind a verb, disable its editor → warning line appears; transform verbs offer no panels in the dropdown.
