# AI Editor — v1 Implementation Plan

> Status: DRAFT — everything below is directional. The requirements were gathered as brainstorming; each feature is a candidate, not a contract. The one non-negotiable: **awesome UI/UX**. When a trade-off arises between feature count and interaction quality, interaction quality wins.

## 0. Execution status (update after EVERY step — agents switch mid-stream)

| Milestone                       | Status                                              | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M0 contracts & spikes           | **DONE (code)** / spikes partially open             | Canonical docs filled; operation contract (`src/app/domain/operations/contract.ts`); anchoring engine (`src/app/domain/anchoring/`); snapshots+hash; SSE decoder (`src/app/services/backends/transport/sse.ts`); `isDesktopOnly: true`. OPEN: CM6 dual-mode decoration spike + CLI protocol probe — need a live Obsidian runtime (manual).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| M1 provider & settings slice    | **MOSTLY DONE (code)**                              | Landed: settings schema (Zod, `schemaVersion`, migrations scaffold, per-element salvage load + dropped-paths report, referential integrity) in `src/app/domain/settings/`; starter pack seeding; provider request builders + response/stream parsers (Anthropic, OpenAI/OpenAI-compatible, Azure OpenAI, Ollama — pure, spec-covered) in `src/app/services/backends/providers/`; tabbed settings shell + entity modals + facade/bootstrap (validate-before-`saveData`) in `src/app/settings/`. OPEN: setup wizard not started; note-ref suggest control (vault note picker for prompt fields) not built; settings UI unverified in a live Obsidian runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| M2 buffered review              | **CODE-COMPLETE — pending LIVE VAULT VERIFICATION** | End-to-end review flow wired: `src/app/services/review-service.ts` (exclusion refusal → size guard → editor/backend resolution with typed skip report → per-editor `assembleContext` → `createApiEditorExecutor` (adapter buildRequest → fetch → parseBufferedResponse, buffered baseline) → `RunController.startRun`); `src/app/ui/review-controller.ts` (per-view glue: rail mount into `contentEl`, decoration dispatch via `setFindingsEffect`, CM6 update-listener → `applyTextChanges` (canonical view per file — every doc change forwarded exactly once, incl. programmatic edits), status bar, size-confirm modal, `ObsidianVaultReader`, single `editor.cm` accessor); `src/app/ui/side-panel.ts` (`ItemView` 'ai-editor-review': per-editor sections, findings with click-to-reveal, Not-anchored group, skip notices, live subscribe); real `Review current note` + `Open review panel` commands; styles (rail, color-mix finding highlights, stale = dashed+dimmed, panel). **FIXED (2026-07-29 adversarial review, round 2)**: (a) edit forwarding no longer keyed on view focus — one canonical view per file forwards every doc-changing transaction (search-replace-all/programmatic/menu-undo edits now remap anchors; Accept's edit flows through the same path, direct-apply in `acceptFinding` removed); (b) snapshot gap closed — `startReview` re-snapshots via `refreshSnapshot` synchronously right before `RunController.startRun`, and the controller binds `glue.run` via synchronous `refreshAll()` in the `started` branch; (c) dead `transport/executor.ts` deleted — its native `AbortSignal.any/timeout` composition and `reader.cancel()` teardown ported into `api-editor-backend.ts`, exactly ONE `TransportError` remains; (d) `styles.src.css` no longer ships Tailwind preflight or `:root` theme variables (`theme(inline reference)` imports), and the dead `my-plugin-whats-new-*` selectors were renamed to the runtime `ai-editor-whats-new-*` prefix (drift-guarded in `scripts/build.spec.ts`). **INTEGRATED (2026-07-29 integration pass)**: (a) review card wired end-to-end — `findingCardExtension(reviewController.findingLookup())` registered in `plugin.ts`; Accept routes through `FindingStore.accept` (precondition, BR #3) then dispatches replacement + `removeFindingsEffect` as one undoable transaction; the controller applies the accept edit to the domain store directly and its update listener skips card-originated transactions (recognized by `removeFindingsEffect`) to avoid double-apply; Dismiss → `FindingStore.dismiss`; re-projection excludes terminal statuses so dismissed/accepted highlights never reappear; `RunController.findRunWithFinding` resolves finding id → run. (b) `review-service` now builds executors via `backends/api-editor-backend.ts` `createApiEditorExecutor` (streaming for Anthropic/OpenAI/openai-compatible SSE, buffered for Azure/Ollama, whole-operation timeout `API_REVIEW_TIMEOUT_MS` = 300 s, status-only error messages per BR #12) — the local buffered duplicate was deleted. (c) The robust fail-closed `ObsidianVaultReader` (`src/app/ui/obsidian-vault-reader.ts`, subpath stripping, markdown-only, tag tolerance, null-on-cold-cache) replaced the inline duplicate in `review-controller.ts`. **REMAINING SEAMS**: (1) binding rules don't filter editors yet (marked SEAM M6 in review-service); (2) CLI backends skipped with typed reason (SEAM M7); (3) ~~`maxConcurrentRequests` not enforced~~ ENFORCED 2026-07-29 (commit `29bf5e5`): plugin-wide FIFO abort-aware semaphore (`orchestration/semaphore.ts`) owned by `RunController`, gating executor stream starts across ALL runs; limit read from settings at each admission, queued editors stay 'pending', cancel ejects queued waiters with no leaked permits; (4) `requestUrl` buffered fallback for CORS-blocking providers not implemented; (5) push-back thread input is a disabled placeholder; (6) **MANUAL LIVE-VAULT VERIFICATION REQUIRED — exact checklist**: (a) plugin loads in a live vault without console errors; (b) `Review current note` command runs on a test note; (c) highlights render in Live Preview AND Source mode; (d) rail shows status/counts; (e) side panel lists findings + jump-to-span works; (f) card opens on highlight click; (g) Accept applies the edit AND undoes cleanly (one undo step); (h) Dismiss clears the highlight; (i) cancel works mid-run; (j) exclusions block the review with a refusal; (k) size warning fires on an oversized note; plus multi-pane edge cases: two panes on one file (type in each, replace-all without focus, Accept from either pane, popout window).                                                                                                                                                                                                                                                                                                                    |
| Live vault verification         | **STARTED — first e2e run SUCCEEDED**               | 4/4 findings anchored via local Ollama (qwen3:4b). Fixed live: ollama `think:false`, advisory-hint clamping. VERIFIED by Sébastien: card opens on highlight click, Accept/Dismiss work. Fixed since: card widened (560px + air), panel binding falls back to live active view ("No review yet" bug), registerView double-load race healed, humanize base action added. Still open: status-bar count not updating, Live Preview vs Source mode visual comparison, real API providers unconfigured. See history 2026-07-29 "Live vault verification". Smoke note: `AI Editor Smoke Test.md` at vault root (delete after card verification).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Interaction surfaces design     | **DONE (design)**                                   | Research + adversarially-critiqued design in `documentation/plans/interaction-surfaces-design.md` (2026-07-29): editor/file context menus, full command inventory (no default hotkeys), CLI = GO (`registerCliHandler` is public `@since 1.12.2`; bump `obsidian` typings 1.12.0 → 1.12.3), batch review deferred post-M4. 3 open questions for Sébastien at the end of that doc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Interaction surfaces slice 1    | **DONE (code)**                                     | Foundations landed 2026-07-29 (commits `3cd53aa`, `64afd8e`): `obsidian` typings 1.12.0 → 1.12.3 (`registerCliHandler`/`CliData`/`CliFlags`/`CliHandler` now typed; tsc clean); shared reviewability predicate `src/app/services/reviewability.ts` (`isReviewable` = not excluded + ≥1 enabled review-capable editor whose backend resolves; re-exports `isExcluded`, reuses `resolveApiBackend` — no duplicated rules; `ReviewController.canReview` now routes through it so the Review command hides when nothing could dispatch); settings facade mutation observer (`SettingsFacade.subscribe(listener)` → unsubscribe, notified after every successful update/persist incl. load-time repairs, never on rejected updates; `createSettingsNotifier` shared by fallback facade, host-wrapping path, and `AIEditorPlugin.persistSettings`). All spec-covered (validate green, 555 tests). Next slices: selection scope plumbing (§5.2), menus, command inventory, CLI handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Interaction surfaces slice 2    | **DONE (code)**                                     | Selection scope plumbing landed 2026-07-29 (commit `c3508d6`): `StartReviewInput.requestedSelection` (selection-capture contract, design §1) — caller captures the range synchronously against the capture-time snapshot; at run start the service re-validates via the new pure `isRequestedSelectionValid` (non-empty ordered range, in bounds against the FRESH snapshot, text hash unchanged since capture). Valid → run snapshot selection-scoped on exactly that range (overrides any live selection the refreshed snapshot picked up); invalid → whole-note fallback with `selectionFallback: true` on the `started` result. `ReviewController.startReview(view, confirmedLargeNote, requestedSelection?)` passes it through, preserves it across the size-confirmation round-trip, and Notices "Selection changed — reviewing the whole note" on fallback. Legacy whole-note / snapshot-carried-selection path untouched (spec-pinned). 10 new specs (validate green, 565 tests; build green). Next: editor context menu (design slice 3), file menu, command inventory, CLI handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Interaction surfaces slice 3    | **DONE (code)**                                     | Context menus landed 2026-07-29 (commit `9a5ed7c`, design §1/§2): pure `menu-model.ts` (spec-covered) decides which items appear; `editor-menu.ts` + `file-menu.ts` are thin Obsidian `Menu` wiring registered from `plugin.ts` via `registerEvent`. Editor menu (v1 review-class only — action verbs wait for M3 transform ops): "Review selection" (`message-circle`) when selection non-empty + editable markdown view + `isReviewable`; selection offsets captured synchronously in the click callback → `requestedSelection` (slice 2 re-validation + whole-note fallback Notice already wired). File menu (single `.md` `TFile`, reviewable, `setSection('ai-editor')`): "Review note" (opens file if needed via new `ReviewController.reviewFile` → same `startReview` path as the command) + "Open review panel" (`bot`). `files-menu` intentionally NOT registered (batch deferred post-M4, no placeholders). `ReviewController` gained public `canReviewPath(path)` and private `openMarkdownView` (shared with reveal-from-panel). 9 new specs (validate green, 574 tests; build green). Next: command inventory (design slice 5), CLI handler (slice 6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Interaction surfaces slice 4    | **DONE (code)**                                     | Static command inventory landed 2026-07-29 (commit `b6827c6`, design §3): `review-selection` (editorCheckCallback: editable view + non-empty selection + `isReviewable`; dispatches via new shared `ReviewController.startSelectionReview` — the editor context menu now uses the same entry), `cancel-run` ("Cancel review"; unsettled run for the active file via new `ReviewController.getActiveRun`), `next-finding`/`prev-finding` (cursor-relative stepping through the active run's revealable findings — open/preview + live anchor — ordered by anchor position, wrap-around, revealed via the side-panel reveal path). Pure logic spec-covered: `src/app/commands/command-gates.ts` (availability predicates) + `src/app/commands/finding-navigation.ts` (`navigableFindings`/`stepFinding`, 20 new specs total); registrar renamed `review-current-note.ts` → `review-commands.ts` (six commands, still thin glue). No default hotkeys. DEFERRED (no non-functional commands): `accept-finding`/`dismiss-finding` (needs card triage state), `filter-severity` (needs filter state), dynamic `action-<actionId>` (transform ops not wired), `accept-all-/dismiss-all-<editorId>` (bulk ops, M4) — and with them the settings-driven dynamic registration diffing (observer from slice 1 stands ready). Validate green (595 tests), build green. Next: CLI handler (design slice 6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Interaction surfaces slice 5    | **DONE (code)**                                     | CLI handler landed 2026-07-29 (commit `fa58134`, design §4 / design slice 6): pure `handleReviewCli` core (`src/app/services/cli/review-cli.ts`, Obsidian-free, 36 specs) — CliData arg parsing (`file` required; `editors` comma-separated ids-or-names, case-insensitive on names, strict on unknown tokens; `format` json (default)\|text; `confirm-large` boolean), file resolution + read via injected deps, dispatch through the injected runner (production = the shared `startReview` pipeline, so exclusions/size guard/editor-backend resolution are the SAME refusals as the command), `await run.settled`, then one buffered output document: `{ ok, file, findings[{ id, editor, severity, quote, critique, suggestion, anchor: {from,to,state}\|null }], skips, summaryByEditor, error }` (JSON) or one line per finding (text). Typed error codes: `file-not-found`, `excluded`, `needs-confirmation` (oversized without `confirm-large`), `no-editors`, `backend-error`, `timeout` (all failures timeouts). Per-editor post-run failures join `skips` (`backend-error`/`timeout`/`cancelled`); messages reuse the run's redaction seam, unexpected pipeline failures are status-only (BR #12). Glue `src/app/cli/register-review-cli.ts` (ObsidianVaultReader + `getFileByPath`/`getFirstLinkpathDest` resolution, markdown only, shared `RunController` so CLI runs surface in rail/panel); registered in `plugin.ts` under `Platform.isDesktop && requireApiVersion('1.12.2')` — `minAppVersion` untouched. `scope` flag NOT registered (selection unsupported from CLI v1 — reserved, no non-functional flags). Validate green (631 tests), build green. LIVE-VAULT CHECK PENDING: actual `obsidian ai-editor:review --file …` invocation end-to-end. Interaction surfaces design slices 1-6 now all landed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Interaction surfaces fix pass   | **DONE (code)**                                     | Adversarial review of slices 2-5 fixed 2026-07-29 (commit `3e0f05f`), 6 confirmed findings: (1) **CLI run on an open note no longer bypasses the ReviewController** — `registerReviewCli` now takes the controller; when the note is open, the snapshot comes from the LIVE editor buffer (`ReviewController.cliRunBinding`, whole-note scoped) and the started run is bound to the view glue synchronously (`bindCliRun` → `refreshAll` + skip report), so edits typed during a CLI run remap anchors and rail/panel/highlights update immediately; (2) **file-menu "Review note" is explicitly whole-note** — `reviewFile` passes a new `SnapshotScope` (`'whole-note'`) through `startReview`/`snapshotView`, so a live selection never silently selection-scopes it (selection scope is exclusively the `requestedSelection` contract); (3) **selection re-validation is real across the size-confirmation round trip** — `requestedSelection` now carries `capturedHash` (hash of the capture-time text, filled from the first snapshot, carried unchanged through the confirm retry); the service validates against it instead of the post-modal snapshot's own hash (spec pins the round-trip fallback). Minors: `selectEditors` rejects explicitly requested DISABLED editors (typed `no-editors` error; silent-drop was data loss for scripts); `registerReviewCli` wrapped in try/catch in `onload` (`registerCliHandler` throws on duplicate registration in the double-load race → degrade to no CLI surface, don't fail the load); CLI runs on notes with no open view are discarded after output shaping (`releaseRun` seam, run-identity guarded — no unbounded accumulation from batch usage); design doc §1/§4 amended to the shipped contracts (hidden-not-disabled items, `scope` flag dropped for v1, `anchor` nullable, `capturedHash`). Validate green (649 tests), build green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CLI cancel + status             | **DONE (code)**                                     | Design §6.3 decision implemented 2026-07-29 (commits `5b48768`, `e34e5f7`, `7be8452`): CLI v1 surface is review + cancel + status. Shared machinery extracted first (`src/app/services/cli/cli-shared.ts`: flag parsing, the CLI finding shape, `shapeFindings`/`shapeSummaryByEditor`, `formatFindingLine`; `src/app/cli/resolve-note-path.ts`: one wikilink-tolerant `--file` resolver; `src/app/services/cli/spec-fixtures.ts`: shared `FakeRunHandle`) so the subcommands stay in lockstep by construction. `ai-editor:cancel` (`cancel-cli.ts` + spec): `file` required; cancels the unsettled run via `RunHandle.cancelRun` — NEVER discards (deps expose no discard capability; findings stay inspectable via status/UI); `{ ok, file, cancelled }` with `reason: 'no-run' \| 'already-settled'` when nothing to cancel; JSON only. `ai-editor:status` (`status-cli.ts` + spec): `file` + `format` (json\|text); read-only poll surface — `run: null` when none, else `{ settled, editors: [{id,name,status,error}], findings (review shape), summaryByEditor }`; text = status headline + review-format finding lines; lockstep pinned by spec (status findings `toEqual` review findings on the same run). Glue `src/app/cli/register-run-cli.ts`; `plugin.ts` registers all three subcommands under the same `Platform.isDesktop && requireApiVersion('1.12.2')` gate, each individually try/catch-guarded (double-load race degrades per subcommand). Design doc §4 rewritten for the three-subcommand surface; §6 open questions replaced with decisions (Ask-an-editor = modal + threads; batch review deferred until cost estimation; CLI = review+cancel+status). Validate green (673 tests), build green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| M3 incremental stale-marking    | **DONE (code)**                                     | The documented seam in `finding-decorations.ts` wired 2026-07-29 (commit `5fd0080`): `ReviewController.handleEditorUpdate` snapshots the `FindingStore`'s stale ids before forwarding an edit batch, diffs after (pure `src/app/ui/editor/stale-diff.ts`, spec-covered without CM6), and dispatches `markStaleEffect` for exactly the transitioned findings on a microtask (CM6 forbids dispatching inside an update listener) — a finding now dims the moment an edit stales it instead of waiting for the deferred rebuild. `dispatchDecorations`' full `setFindingsEffect` rebuild unchanged (eventual-consistency backstop).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Ask an editor modal             | **DONE (code)**                                     | Freeform "Ask an editor" (design §6 decision 1, modal entry point; card threads stay M4) landed 2026-07-29 (commit `991fbcd`): `src/app/ui/ask-editor-modal.ts` (thin Modal glue: editor picker — dropdown, or static text when exactly one; instruction textarea, Enter = newline, Ctrl/Cmd+Enter = submit; Cancel + Ask CTA disabled while blank) over pure `ask-editor-model.ts` (`normalizeInstruction`/`canSubmitAsk`/`defaultAskEditor`, spec-covered). Entered from the editor context menu ("Ask an editor…", same gate as Review selection — `menu-model.ts` now returns both items) and the `ask-editor` command (editorCheckCallback, no default hotkey). Dispatch seam: `StartReviewInput.instruction` (`RunInstruction {editorId, text}`) narrows the run to the chosen editor (un-asked editors are not skips) and augments ONLY that editor's fully composed system prompt via pure `augmentSystemPrompt` (framed `<user-instruction>` block appended last; settings untouched; findings still validate through the unchanged Zod operation contract). Selection contract: `ReviewController.openAskEditorModal` captures range + capture-time hash synchronously in the invoking callback (the modal gap makes the dispatch-time hash unusable), rides through `startReview`'s size-confirm round trip. `reviewCapableEditors(settings)` extracted in `reviewability.ts` so the picker can only offer editors `startReview` accepts. 27 new tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| M3 fix pass (ask + gate)        | **DONE (code)**                                     | Adversarial review of the M3 increment fixed 2026-07-29 (commits `88456a7`, `5a0b1c3`), 3 confirmed code findings + 2 doc corrections: (1) **concurrency permit released at terminal state** — the plugin-wide `maxConcurrentRequests` permit was only freed when the editor stream's iterator ended; after `cancelRun` the consume loop keeps draining discarded events, so an executor ignoring its AbortSignal held its permit indefinitely (N such streams = plugin-wide deadlock until reload). The release now lives on the editor state and fires in `terminate`/`cancelRun` (idempotent; the `finally` stays as backstop); 2 new specs (cancel reclaims from an abort-ignoring executor; a terminal result frees the permit mid-drain). (2) **Ask-editor submit refuses a switched note** — `openAskEditorModal` captures `view.file.path` at open; a submit whose view now shows a different note (programmatic openFile, workspace restore) gets a Notice instead of silently reviewing the wrong note. (3) **Ask dispatch uses `whole-note` scope** — a collapsed capture previously dispatched scope `auto`, letting a selection made while the modal was open silently narrow the run against design §1's exclusive capture contract; `whole-note` is safe unconditionally (a valid captured selection overrides explicitly). Docs: history spec count corrected (13 new, not 27), design §3 table gained the shipped `ask-editor` command row, §1 item 3 annotated as shipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Slow & thinking models          | **DONE (code) — all three pieces (2026-07-29)**     | From Sébastien's live testing (Ollama timeout on a laptop). Three pieces, one increment: (1) **Per-editor retry** — DONE (commits `4810a5b` core, `3343271` UI): editors ending in `error`/`cancelled` get a retry affordance in the rail chip AND the side-panel section header ('Retry <name>' aria-labels); both call `ReviewController.retryEditor(filePath, editorId)` → `RunHandle.retryEditor(editorId, freshText)`, which re-runs ONLY that editor inside the EXISTING run. Decisions baked in: a retry REPLACES that editor's previous findings (removed from the store — they came from a failed/partial attempt; other editors untouched); retried findings anchor against the CURRENT buffer text captured synchronously at retry time (per-editor anchor base: the run-level appliedChanges history became per-editor, recording stops at terminal and RESTARTS from the retry snapshot, so BR #3/#4 hold across retries); the retried request sends that fresh text to the backend (a selection scope only rides along while the text is still the snapshot text); fresh per-editor runId + per-retry AbortController make retry-after-cancel work (the shared run abort stays aborted; `cancelRun` lost its early-return so it also aborts in-flight retries) and stale still-draining streams harmless; the concurrency permit is acquired exactly like a first attempt; `isSettled()` is now DERIVED from live editor state so an active retry flips the run back to in-progress on every surface (rail Cancel/spinner, `Cancel review` gate, CLI status derives from states already) while the `settled` promise keeps first-settle semantics (CLI output await; documented). No open view → vault-read fallback (run-identity guarded). CLI has no retry surface (UI-only affordance). (2) **`Request timeout (seconds)`** — DONE (commit `28af75e`): `behavior.requestTimeoutSeconds` (int 30-3600, default 600 s — laptop-realistic; no compat shim) replaced the hardcoded `API_REVIEW_TIMEOUT_MS`; threaded as `timeoutMs` through `createEditorSpec` via `reviewTimeoutMs(behavior)`; CLI reviews share it (same `startReview` path); Behavior tab int field ('Request timeout (seconds)'); the timeout error message names the setting ("raise 'Request timeout' in settings"); specs pin schema default/bounds, s→ms conversion, the value flowing into the transport, and the message shape. (3) **Per-backend thinking setting** — DONE (commit `fd97786`; decision: per-backend is enough, NO global default): `apiBackendSchema` gained `thinking` ('off'\|'on', default 'off' everywhere — silent minutes-long reasoning reads as a hang), `thinkingBudgetTokens` (1024-32000, default 8192), `reasoningEffort` ('default'\|'minimal'\|'low'\|'medium'\|'high'), `extraBodyJson` (advanced, openai-compatible only). Ollama sends `think` from config, parser reads only `message.content` (spec: `message.thinking` ignored); Anthropic 'on' sends `thinking: {type:'enabled', budget_tokens}` with the budget ON TOP of the 8192 output budget (budget_tokens < max_tokens by construction) and `tool_choice` relaxed to `auto` (API rejects forced tool use while thinking) — parser + text-JSON fallback spec-covered with thinking/redacted_thinking blocks interleaved; OpenAI/Azure pass `reasoning_effort` through ('default' sends nothing); openai-compatible merges `extraBodyJson` (extras win, invalid JSON → invalid-config, modal validates at save); streaming accumulator treats `thinking_delta` as progress and drops thinking blocks at finalize. Backend modal shows only the controls matching the selected kind. **Fix pass (2026-07-29, commits `c34472f`, `df633f8`)** — adversarial review, 1 major + 4 minors, all confirmed + fixed: Anthropic 'on' now sends ADAPTIVE thinking (`{type:'adaptive'}` — manual `budget_tokens` is deprecated on Claude 4.6 and HTTP-400-rejected on 4.7+/5.x) with `tool_choice` KEPT forced (adaptive supports forced tool use) and `max_tokens` 32k (thinking spend counts against it); the legacy manual mode survives as a third `thinking` value `'budget'` (Claude ≤4.5 only, modal documents which mode applies) with the sum clamped to the legacy 32k output ceiling and `budget_tokens` clamped strictly below `max_tokens` (a 32k budget previously built `max_tokens` 40192 → HTTP 400 at validation); openai-compatible streaming counts `delta.reasoning_content`/`delta.reasoning` deltas as progress (DeepSeek/OpenRouter conventions — a thinking model enabled via `extraBodyJson` no longer reads as a hang); `ai-editor:review`'s `shapeRunOutput` reports mid-retry (pending/running) editors as `retrying` skips instead of silently dropping them (first-settle `settled` + UI retry race) and no longer fabricates a 'cancelled' message while a retry is in flight. Also DONE already (commit `298e02e`): verdict pills relabeled — 'All good' / 'Needs work' / 'Not ready' (wire tokens publish/needs-work/kill unchanged). |
| Daemon mode                     | **DONE (code) (2026-07-29)**                        | Sébastien: editors should either be "waiting for orders" (summon-only — DEFAULT, cost control) or in **daemon mode** (his name): watching file edits and refreshing their recommendations automatically. Decisions: `behavior.daemonMode: boolean` toggle (default `false`) + `behavior.daemonIdleSeconds` (int, sensible bounds, default 30) — after the user stops editing a reviewable note for N seconds AND the note changed since its last run, daemon mode re-dispatches the review (same editor set as the note's current run; all enabled review-capable editors if the note was never reviewed). Coalescing: never while a run/retry is in flight for that file (re-arm after settle); exclusions, size guard (daemon SKIPS oversized notes silently rather than modal-spamming), binding rules (once they land) and `maxConcurrentRequests` all apply; cancel/summon always available. Settings copy must state the cost implication plainly. Sébastien enables it in his vault; default stays off. **IMPLEMENTED (commits `a37da92` schema/settings, `9121540` scheduler core, `8a02ba5` glue+rail):** `behavior.daemonMode` (default false) + `behavior.daemonIdleSeconds` (int 5-600, default 30; Behavior tab → Daemon: toggle whose copy states the cost implication, idle field rendered only while on). Pure spec-covered scheduler `src/app/services/daemon/daemon-scheduler.ts` (21 specs — idle windows, per-file independence, coalescing incl. the fire-time run-in-flight refusal + re-arm-at-settle, changed-hash gate incl. undo-back-to-same, never-reviewed dispatch, silent oversized skip with one-log-per-file flag, settings flip mid-arm) behind thin glue `src/app/ui/daemon-controller.ts` (one `window.setTimeout` per file PATH — popouts/splits share a schedule; probes live facts at fire time; oversized skip = one log line, never modal/Notice). Edits arrive via the EXISTING canonical-view update listener in `ReviewController.handleEditorUpdate` (now also fires for files without a run; no second CM6 listener); run state via `refreshGlue` (`syncRunState` — summons, CLI, daemon runs, retries coalesce identically); config via the slice-1 settings observer (toggle/idle apply live, off clears all timers); timers cleared on dispose/unload, file close (last view only), vault delete/rename. Dispatch = `ReviewController.startDaemonReview`: shared `startReview` pipeline (exclusions, size guard, editor/backend resolution, concurrency gate all apply), whole-note scope, silent on every refusal, new `StartReviewInput.editorIds` re-dispatches the note's current run's editor set (null → all enabled review-capable editors), and new `abortWhen` guard (checked synchronously right before `startRun`, typed `aborted` status) so a daemon dispatch racing a user summon during context-assembly awaits NEVER cancels the user's run — the fire-time probe already refuses while a run/retry is unsettled (spec-pinned), the guard closes the async gap; summon/cancel/retry always win. Rail: tiny pulsing dot + tooltip while armed for the current note (`RailState.daemonArmed`, spec-covered model). BR #1 carve-out documented in Business Rules.md/README/docs/usage.md/review-service header. Binding rules still SEAM M6 (apply automatically once they land — same pipeline). **Fix pass (2026-07-29, commit `c1b84e1`)** — adversarial review, 4 confirmed minors, all fixed: `abortWhen` also aborts on `disposed` and on `behavior.daemonMode` off (a dispatch mid-await in context assembly no longer bills backends after the cost kill-switch was flipped, and no longer starts a fresh run whose backend fetches outlive the plugin — `cancelAll` settles runs, so the run-in-flight check alone passed during unload); `refreshGlue` detects same-pane note navigation (view rebound A→B) and clears A's daemon schedule when the last view showing A is gone (the `syncGlues` sweep only caught destroyed views — the most common close path leaked armed timers + scheduler state per navigated-away file; probe fails closed so no spend, but the documented last-view contract was violated); both pulse animations (armed dot, run-in-progress dot) stop under `prefers-reduced-motion` (WCAG 2.3.3 — the armed dot pulses for the whole idle window, up to 600 s); `plugin.ts` `onunload` comment reworded to the actual guarantee.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Live-testing feedback #2        | **DONE (code)**                                     | Both pieces implemented 2026-07-29 (commits `c605b90` rail tooltips, `9ecc949` follow-links). (1) **Rail editor hover names** (`c605b90`): every rail chip/dot carries a tooltip + `aria-label` of the form `<Editor name> — <status>` ("Concision Editor — 3 findings", "Hater — waiting", "Style Guard — failed (timeout)"). `rail-model`: `'pending'` joined `RailEditorStatus` (queued behind the concurrency limit reads 'waiting', distinct from 'reviewing'); failed editors append a short reason via the pure `railErrorReason` error-code mapping. `rail`: tooltips attach through an injected `RailTooltipSetter` (Obsidian `setTooltip`, placement left) with a `title`-attribute fallback — mutually exclusive so nothing shows twice; DOM layer stays Obsidian-free. `review-controller` projects pending distinctly, passes the failure reason, injects `setTooltip`. Pending chip pulses slowly (reduced-motion safe). (2) **Prompt note refs follow links** (`9ecc949`): `PromptSource.followLinks: boolean` — when on, notes linked from a referenced note are inlined into the assembled prompt: depth 1 (links-of-links NOT followed), embeds (`![[...]]`) count (vault reader's `getOutgoingLinks` = `links` + `embeds`), deduped against already-included paths, deterministic link order, `FOLLOWED_LINKS_CAP` = 20 per referenced note, subject to `contextBudgetChars`; unresolved and non-markdown links skipped silently; excluded roots never followed (BR #7). Defaults per no-users policy: **on** for the voice profile (the motivating case), off for editor prompts and panel charters. Settings UI: 'Follow links' toggle with plain-language description on every note-refs block (`renderNoteRefsEditor` — voice tab, editor modal, panel modal). Validate + build green after each commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Live-testing feedback #3        | **DONE (code, 2026-07-30 — stage D slice 0)**       | Implemented per the locked decisions (commit `d186d1b`): pure cycle logic `navigableEditorFindings`/`cycleFinding` (finding-navigation.ts, memory-based so the first click deterministically reveals the FIRST finding), pure decision matrix `chipClickAction` (rail-model.ts), `emphasizeEditorEffect` (~2 s flash on that editor's live marks, stale never flashes, reduced-motion aware, auto/rebind/dispose cleared), side panel scrolled per `data-editor-id` section. +21 specs, 971 total (the delta was first recorded as +29, computed against a baseline that had already moved: the concurrent `fb16f1c` OpenRouter session added 8 specs the "OpenRouter backend kind" row already claims). Original decisions: Sébastien: clicking an editor chip in the rail should show/highlight that editor's findings when it has any. Decisions: click on a chip with revealable findings (open/preview + live anchor) → (a) reveal the FIRST finding (same reveal path as the side panel: scroll + brief selection), subsequent clicks CYCLE through that editor's findings in anchor order (wrap-around; reuse `finding-navigation.ts` stepping scoped to one editor); (b) simultaneously EMPHASIZE that editor's highlights for ~2 s (temporary CSS class on that editor's decorations — e.g. stronger tint via a `data-editor-id` scoped rule; respects reduced motion; auto-clears); (c) chip with zero revealable findings but a summary/error → open the side panel scrolled to that editor's section; (d) chip while running → no-op (tooltip already says running). Keyboard accessible (chips are buttons already — Enter/Space follow the click behavior).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Transform ops — service layer   | **DONE (code) (2026-07-29)**                        | Stage C slice 1 of the sweep (commits `7c338c5` verb registry, `6525525` orchestration, `433ba2e` service). Verb registry (`src/app/domain/actions/verb-registry.ts`): pure id → { label, class, instruction } for all nine `builtInActionIdSchema` verbs — transform (rephrase/summarize/simplify/humanize → `transform-selection`), generate (continue/say-more → `insert-at`), review-class (critique/find-evidence/identify-assumptions → review pipeline via the ask-editor augmentation seam); instructions follow starter-pack persona conventions, spec-pinned free of output-format directives (the prompt serializer owns those). Orchestration (`transform-run.ts`): lean `TransformRunHandle` (pending/running/done/error/cancelled, same event protocol as reviews — runId match, exactly-once terminal, post-terminal discard, no-terminal = invalid-output; permit freed at terminal; redaction seam) + `TransformController` (one active transform per file, discardRun/cancelAll); `RunController.requestGate` is now public so reviews + transforms share ONE `maxConcurrentRequests` budget. Apply precondition (BR #3/#4): exact text equality against the dispatched target — selected span at original offsets for replace-span (edits strictly after stay valid), whole document for insert-at; never fuzzy-relocated. Service (`transform-service.ts` `startAction`): routes verb class, same executor machinery as reviews (adapter buildRequest already handled all operation kinds; Zod validates results; streaming buffered-equivalent; `reviewTimeoutMs`); typed refusals (excluded first/BR #7, size guard, `no-editor` + new `no-rewrite-capability` skip, `selection-required`, `selection-changed` after refresh re-validation); generate anchors accept a caret and default to note end. NO UI yet — slice 2 (stage C part 2) adds the inline diff, CM6-undo Accept, menu/command dispatch, and plugin wiring of `TransformController(runController.requestGate)`. +56 specs (855 total).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Transform ops — preview UI      | **DONE (code) (2026-07-29)**                        | Stage C slice 2 of the sweep (commits `4fff879` word diff, `01e485c` preview widget, `0139942` controller/plugin wiring). Word-level diff (`src/app/domain/diff/word-diff.ts`): LCS over word/whitespace-run tokens, whitespace-preserving (same+del reconstructs old, same+ins reconstructs new — spec-pinned invariants), del-before-ins per change region, whitespace-bridge folding (a full rewrite renders as ONE red + ONE green block), common prefix/suffix trimming + 4M-cell token budget with coarse del+ins fallback, unicode-safe. CM6 preview (`src/app/ui/editor/transform-preview.ts`): ONE block widget below the line containing the span end / insertion anchor (`previewWidgetPos`; a line-start anchor goes after the PREVIOUS line — whole-line selections), source text stays visible (BR #2), works identically in Live Preview and Source mode (BR #6); widget = persona-colored header (`<Verb> — <Editor>` via `actionLabel`, new optional field on `StartTransformInput`/handle), word-diff body (generate verbs: single green insertion block), rationale line, Accept (CTA) / Reject / key hint; widget-scoped keys — Enter accepts, Esc rejects, focused buttons keep native activation (no global hotkeys); Obsidian-free (callbacks ride the spec), popout-safe. Controller: presents when the run reaches 'done' AND `checkPrecondition(currentDoc)` passes; Accept re-verifies in the same synchronous block, then ONE transaction (changes + clear effect + cursor after insert) = single undo step (manual check queued — no `@codemirror/commands` dep for a history spec); 'text-changed' auto-dismisses with one Notice via the refresh cycle (edit hook in `handleEditorUpdate`; run discarded, never fuzzy-relocated, BR #3/#4), 'not-done' failure keeps the widget with a Notice. Errored runs → one redacted Notice + discard; cancelled → silent discard. Rail: an in-flight transform pulses the target editor's chip (`'transforming'`, `'pending'` reads "waiting" while queued; overlay only exists for review-capable chips — a rewrite-only editor still flips the button to Cancel), rail Cancel + `Cancel review or action` command (id `cancel-run` unchanged) cancel review AND transform. Plugin: `TransformController(runController.requestGate)` (shared budget), `transformPreviewField` registered, discard on rename/delete, `cancelAll` on unload. +44 specs (899 total). NOT yet user-reachable — dispatch (menus/commands) is the next slice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Transform ops — action dispatch | **DONE (code) (2026-07-30)**                        | Stage C slice 3 of the sweep (commits `4c8584d` multi-editor run instructions, `d670df1` binding resolution, `9ed101d` context-menu dispatch, `d04b3cc` dynamic commands, `5837b77` starter bindings, `ac61f7a` actions-tab validation). `RunInstruction` now targets a SET of editors (`editorIds`) — ask-an-editor passes one; review-class verbs bound to a panel pass every member with the verb instruction augmented onto each member's system prompt (charter aggregation stays M6). `src/app/services/actions/action-resolution.ts` is the ONE decision point for "can this bound action run right now?": built-in verbs from the registry; custom actions transform-class, instruction resolved fresh at dispatch from the customInstruction prompt source (BR #7/#8, capped); editor targets need enabled + class-appropriate capability + usable backend; panel targets valid ONLY for review-class verbs; undispatchable actions never become UI. Editor context menu: bound actions first (icon by class check/wand-2/message-circle, alphabetical, cap 10, privacy-gated), Review selection / Ask an editor keep their placement; dispatch via `ReviewController.startBoundAction` (synchronous selection capture, refusals as Notices, size guard through the copy-aware confirm modal). Dynamic `action-<bindingId>` palette commands follow the settings via the mutation observer with add/removeCommand diffing (rename re-registers under the unchanged id → user hotkeys survive; per-invocation gate `canRunBoundAction`: transform = editable+selection, generate = editable, review-class = any mode). Starter pack seeds default bindings (rephrase/summarize/simplify → Concision Editor, humanize → Humanizer, critique/identify-assumptions → Devil's Advocate, find-evidence → Fact Checker; generate verbs unbound — no authorial starter persona; idempotent, user bindings never overridden). Actions tab mirrors the resolution rule: panels offered only for review-class verbs (custom actions editors-only) and bound-but-undispatchable actions show why. LIVE CHECKS pending — see history 2026-07-30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Stage C review fix pass         | **DONE (2026-07-30)**                               | Review of stage C landed 5 verified findings, all fixed (commits `6d44d08` instruction skips, `b100e6e` transform preview hardening, + this docs commit). (1) Transform preview note-switch race: a refresh in the window where Obsidian has assigned `view.file` but the async load has not yet replaced the CM doc discarded a valid done transform with a spurious "text changed" Notice — the stale path is now non-destructive when the glue just rebound (`previousPath !== filePath`); the doc-replacing load transaction schedules the coherent re-check. (2) Accept undo isolation: the accept dispatch now carries `isolateHistory.of('full')` — without it CM6 merged the applied transform with adjacent typing (both directions, `newGroupDelay` 500ms), so Ctrl+Z reverted user keystrokes too; `@codemirror/commands` added as dev dependency (already an external; Obsidian provides it) and the history spec queued in slice 2 landed (3 specs incl. the merge-hazard control). (3) Instruction skip reporting: `startReview` silently dropped disabled members and deleted member ids from instruction pools, contradicting the `ResolvedAction.editorIds` contract — new `editor-disabled`/`editor-missing` skip reasons, emitted ONLY for instruction-named editors (default pool + daemon `editorIds` redispatch stay silent about disabled editors by design). (4) Slice-2 spec delta corrected: +44, not +36 (28 in `4fff879` + 16 in `01e485c`; 855 → 899). (5) Configuration.md no longer presents the panel charter's follow-links toggle as live — persisted but inert until M6 charter aggregation. +4 specs (942 total).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| OpenRouter backend kind         | **DONE (code) (2026-07-30)**                        | Sébastien: backends should include a dedicated OpenRouter entry for easier setup, even though it is OpenAI-compatible under the hood. Landed (commit `fb16f1c`): `'openrouter'` provider kind sharing the OpenAI adapter — base URL preset (`https://openrouter.ai/api/v1`, overridable), recommended attribution headers (`HTTP-Referer`/`X-Title`, static values), `reasoningEffort` forwarded as OpenRouter's unified `reasoning: { effort }`, advanced extra-body field available, API key required at save time, model placeholder `anthropic/claude-sonnet-4.5`. 8 adapter specs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Panel Review button             | **DONE (code, 2026-07-30)**                         | GitHub issue #16, landed with the stage F prerequisite work. Commits: `e749cf2` pure gate + button state, `e0d85ab` panel header + dispatch. `reviewGate` (services/reviewability.ts) becomes the ONE reviewability evaluation and now returns the REASON — `excluded` / `rule-disabled` (with the rule label) / `no-editor` / `ok`; `isReviewable` and `isPluginEnabledForNote` are projections of it, so a surface that explains a refusal cannot drift from the decision it explains. `ui/panel-review-button.ts` (pure, spec-covered) derives label + accessible name + tooltip + disabled + busy from the bound note; `busy` wins over every gate refusal (a rule added mid-run must not make the panel claim nothing is happening — the run stays bound and keeps remapping anchors). The panel now renders a HEADER (bound note name + Review button) that exists even with no run: `SidePanelBinding` became the optional half of a `SidePanelState`. The button dispatches the shared whole-note path (`reviewFile`, which opens/reveals the note first — findings are highlights in the text) and **REFUSES while a run or retry is in flight**, gated on the SAME `canCancelRun` predicate the `Cancel review` command uses; it refuses rather than cancel-replacing because `RunController.startRun` cancels the previous run for the file and destroying findings from the surface displaying them is never what a click meant. The refusal is re-checked on click, not trusted from the render. A transform/generate run deliberately does NOT count as busy (a review never replaces one; the shared concurrency gate just queues). Busy is read through a THUNK so run notifications alone start and stop the spinner; the spinner is `aria-hidden` decoration (the label says "Reviewing…") and static under `prefers-reduced-motion`. DECISION: a kill-switched note gets a DISABLED button naming the rule, not a hidden one — the panel binding is gone per §4b, but the leaf is one the user opened and a disabled button dispatches nothing; refusing to explain an emptied panel is not what the kill switch promises.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Keyboard triage (stage D sl. 1) | **DONE (code, 2026-07-30)**                         | M4 keyboard triage state machine (commits `f25912d` engine, `87bf9d9` CM6 layer, `8da93e4` wiring). ONE stepping engine (`triageStep`, finding-navigation.ts): memory-based next/prev with wrap + position-based eviction fallback — `cycleFinding` (chip clicks, locked slice-0 contract) is now a thin special case of it, and the accept/dismiss auto-advance IS the eviction fallback. Per-file triage cursor (`TriageCursorStore`, run-token validated → replaced runs evict on first contact; cleared on Escape/rename/delete/dispose). `next-finding`/`prev-finding` now move the cursor (first step seeds cursor-relative via `stepFinding`, then memory-based), ring the current finding (`ai-editor-finding-current` — spec-carried so it survives decoration rebuilds, unlike the effect-only emphasis flash; stale drops it; coexists with emphasis), reveal through the side-panel path, and open the finding card at the target (`showFindingCardEffect`, card-on-jump; `null` closes). New commands `accept-finding` ("Accept current finding": gated on store-actionable + note open; routes through `FindingStore.accept` exactly like the card button, one undoable transaction) and `dismiss-finding` ("Dismiss current finding": gated on current existing — navigable ⇒ dismissable); both auto-advance onto the next remaining finding with wrap; empty set → cursor cleared, card closed, one Notice. Escape precedence: open card's capture listener consumes Escape (card closes, cursor survives); otherwise a non-consuming `Prec.highest` editor keydown observer clears the file's cursor. +30 specs (1001 total). LIVE CHECKS pending — see history 2026-07-30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Bulk triage + filter (sl. 2)    | **DONE (code, 2026-07-30)**                         | M4 bulk operations + severity filter (commits `1a513a4` pure logic, `045986c` accept history isolation, `ec886ee` bulk ops, `2cce37d` severity filter). **Bulk**: pure `planBulkAccept` (commands/bulk-triage.ts) picks every acceptable finding whose precondition still holds against the LIVE text, in document order, keeping the EARLIER anchor when two spans overlap (the later suggestion is skipped — composing two rewrites of one span produces nonsense) and counting both skip kinds; adjacent spans do not conflict. `isBulkAcceptable` mirrors `FindingStore.isActionable` (spec-pinned equivalence) so the panel count and the planner advertise ONE set. `ReviewController.acceptAllFindings(editorId                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | null)`commits each accept through the store (BR #3 re-verified per finding) and dispatches every applied replacement as ONE history-isolated transaction — a single Ctrl+Z restores the batch, the canonical-view forwarding remaps the rest; one Notice reports applied + skipped (overlapping / no longer matching).`dismissAllFindings`closes the set through the store, drops the marks, closes any open card. Commands: dynamic`accept-all-<editorId>`/`dismiss-all-<editorId>`(registration follows the EDITOR entities via the settings observer — ids embed the entity id so hotkeys survive renames, a rename re-adds in place; availability re-checked per invocation against the active note's run, so no command churn per run and no dead commands) + static`accept-all`for every editor at once; side panel gained per-editor`Accept all (n)`/`Dismiss all (m)`buttons (counts from what the section shows, hidden at zero). **Filter**:`ui/severity-filter.ts`per-file VIEW lens (all → warnings and suggestions → warnings only → all),`filter-severity`command (Notice = mode + hidden count) + panel filter button with hidden count. Scope decision: the lens governs decorations, the panel list, triage stepping, chip cycling and bulk ops (accepting/stepping onto an invisible finding would be a surprise); run-report surfaces (rail chip counts, panel section status) keep reporting what the editors found, and a section whose findings are all hidden says so.`visibleFindings`is the single application point. **Also fixed**: the FINDING accept dispatches (card button,`accept-finding`) were annotation-less, so CM6 history joined them to adjacent typing in both directions and one Ctrl+Z reverted the accept AND the keystrokes — both now `isolateHistory.of('full')`like the transform accept (stage C fix), pinned by the new`finding-accept.spec.ts`(single + bulk shapes, ONE undo step for the batch). Extracted`commands/command-sync.ts`(the dynamic-command diff rules once;`diffActionCommands`delegates). +40 specs (1041 total —`1a513a4`+31,`045986c`+5,`ec886ee`+4,`2cce37d`+0: the severity filter end-to-end shipped no new specs because its pure logic was already covered by`1a513a4`; the delta was first recorded as +44, which did not even match the recorded 1001 previous total). LIVE CHECKS pending — see history 2026-07-30. |
| Push-back threads (sl. 3)       | **DONE (code, 2026-07-30)**                         | M4 per-finding push-back threads (commits `5eb3fbf` state, `112b0ef` orchestration + dispatch, `42dd072` card UI, `bea3060` layer isolation — the thread refusal vocabulary moved from `finding-store.ts` into `domain/operations/thread.ts` so the card does not import it from the services layer). The contract already had the `thread-turn` operation; only the RESULT needed the withdrawal branch — added `concede` (defaulted, so nothing else changed) + `revisedCritique`, `reply` tightened to non-empty, prompt rules taught when each applies. `domain/operations/thread.ts` owns the pure rules: cap = 6 completed exchanges (a triage device, not a chat — every turn replays the whole history), outcome normalization (concede WINS over any revision; blank revisions read as absent), and `currentSpanText` (a turn discusses the LIVE text of an anchored span, else the anchored text, else the raw quote — a stale anchor's offsets are never consulted). **State**: `FindingStore.thread` holds COMPLETED exchanges only (strictly alternating, replay-safe) while the in-flight/failed turn lives in `threadTurn`, so a failure never leaves a dangling user turn; a hold updates critique/suggestion IN PLACE (same observation, refined — `supersede` stays for refine-proposal), which re-derives the accept precondition for free (span stale mid-turn ⇒ revised suggestion display-only, BR #3) and drops a `preview` whose suggestion changed back to `open`; a concede dismisses + flags `conceded` but NEVER undoes an accept that landed mid-turn. **Orchestration**: `RunHandle.startThreadTurn` — threads live on the run handle because the run owns the findings, so a replaced/discarded run kills its turns for free and `cancelRun` aborts them AND marks their findings failed immediately (an abort-ignoring executor must not leave a card spinning). Same event protocol as reviews (runId match, exactly-once terminal, post-terminal discard, wrong result kind and stream-end-without-terminal = failures), same plugin-wide permit, same redaction seam. A turn does NOT touch `settled`/`isSettled()` (the review is over); per-turn aborts rather than the run signal, so pushing back on a CANCELLED run's findings still works. **Dispatch**: `services/thread-service.ts` shaped like `startReview`/`startAction` — exclusions fail closed first (BR #7), only the editor that PRODUCED the finding may answer (typed skip when missing / disabled / review-incapable / no usable backend), persona prompt assembled fresh, span text resolved before any await. No size guard: a thread-turn request never carries the note text and the review already passed it. **Card**: the disabled placeholder became a scrollable thread + reply row (Enter or Send); pure `threadView` pins idle/pending/failed/capped, `threadRefusalNotice` words store refusals; async replies reach an open card via `refreshFindingCardEffect` (dispatched only while some finding has thread activity), and the rebuild carries the reply draft, focus and caret. Closing the card cancels NOTHING — every completed turn also fires a Notice (withdrew / revised / replied / failed) because the card is usually closed by then. +55 specs (1096 total). LIVE CHECKS pending — see history 2026-07-30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Adaptive layout (sl. 4)         | **DONE (code, 2026-07-30)**                         | M4 adaptive narrow-pane layout (commit `10ad3d0`). ONE pure module owns the rules (`ui/editor/layout-mode.ts`): `nextLayoutMode` collapses at **≤500px** pane width and returns to wide only at **≥560px** — the band is hysteresis, because dragging a split across a single threshold would re-render the chrome the user is aiming at on every observed frame; a non-finite or ≤0 width is NOT a narrow pane but a pane measured while hidden (collapsed sidebar, deferred leaf, detaching view) and keeps the current mode. `paneCardViewport` insets the pane rect and intersects it with the window box, dropping the pane constraint **per axis** when it would leave less than a usable card (`CARD_MIN_WIDTH` 240 / `CARD_MIN_HEIGHT` 160) — squeezing a card into a sliver is worse than letting it overlay its neighbour, and a pane dragged partly out of the window can intersect to nothing; axes decide independently since horizontal and vertical splits are independent. **Observer glue** (thin, in `review-controller.ts`): one `ResizeObserver` per glue on `view.contentEl`, constructed from the VIEW's own window (popout panes are observed by their own document's implementation), initial mode measured synchronously at `createGlue` so an already-narrow pane never flashes the wide rail, disconnected in `destroyGlue` (hence on dispose too); the callback only stores a CHANGED mode and schedules the normal refresh — no synchronous DOM work inside a resize callback. **Compact rail**: the rail was already icon-only chips + tooltips, so the compact form drops the button LABEL for a glyph (`▶` / `■`, text glyphs — the rail is Obsidian-free DOM, no `setIcon`, no innerHTML), keeps `label`/`action` semantic, and appends the side-panel nudge to the button's accessible name (`NARROW_PANEL_HINT`) — only the button carries it, repeating it on every chip would be noise and the chip tooltips already carry name + status; dots, retry, badges and gaps shrink and the wrapper hugs the pane edge. **Cards**: `positionCard` now clamps into the pane box and applies the width cap BEFORE measuring (the measurement must be of the box that gets positioned), so a card opened in a split never spills over the document next to it; popout-safe (window box from the card's own document). **Overflow sweep**: card action row and transform-preview action row wrap instead of overflowing, the push-back input may shrink (`min-w-0`), the card is `min-w-0` with a code-set `max-width`; settings/panel surfaces already wrapped. +21 specs (1117 total). LIVE CHECKS pending — see history 2026-07-30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Stage D review fix pass         | **DONE (code, 2026-07-30)**                         | External review of stage D surfaced 19 findings; all verified against the code, 17 fixed, 2 partially skipped with reasons (see history 2026-07-30 "Stage D review fix pass"). Commits: `8ec4ce8` (triage cursor rebased onto live anchors — the stored offset was never remapped, so the eviction fallback stepped to the wrong finding after any edit), `10b399f` (a run detached during context assembly can no longer open a thread turn; a merely CANCELLED run still can, per the locked slice-3 decision), `dd664b1` (the `openrouter` kind was missing from `createStreamAccumulator`, silently forcing the buffered path), `ee9c7c7` (`FindingStore.isActionable` now requires `anchoredText`, making the claimed equivalence with `isBulkAcceptable` real), `cbec624` (push-back: optimistic pending row + locked input — the store write happens after an await, so the card re-rendered idle and a second Enter lost the message; empty drafts no longer shadow `restoreDraft`; focus parks on the card while the input is disabled), `70e153a` (card-on-jump survives the reveal scroll — the scroll-close listener arms one frame after opening), `7144183` (panel bulk buttons act on the file the panel RENDERED, not on the sticky active-file pointer), `a6ad3a9` (accessible names: severity-filter state, `role="log"` + keyboard-scrollable thread, and `NARROW_PANEL_HINT` finally naming a command that exists), plus this docs commit (spec-delta arithmetic for slices 0 and 2, the superseded M4 layout threshold, the `snapshotHash` contract note, the missing `bea3060` in the slice-3 record, and the status-bar carve-out on "stage D COMPLETE"). +10 specs, 1127 total.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Binding rules + note types      | **DONE (code, 2026-07-30)**                         | Stage E slice 1 — the SEAM M6 marker in `review-service` is gone (plan §4b, M5). Commits: `5a926f7` pure engine, `2251df5` vault seam + OSK adapter, `d0710ed` dispatch, `adf6bdd` UI suppression, `3d419a2` Rules tab, + this docs commit. **Engine** (`domain/rules/rule-engine.ts`, pure): a note resolves to `disabled` / `assigned` / `default`. Decided order — a matching kill switch wins from ANY list position (it must not be shadowed by an unrelated assignment ordered above it), then the FIRST matching `assign` rule in list order (the tab exposes explicit move-up/down, so list order IS the priority statement; specificity sorting was considered and rejected as making the visible order a lie); assignments never accumulate (a rule saying "only the Humanizer touches this folder" has to be able to mean exactly that); no match → today's behavior, every enabled review-capable editor. A cold metadata cache fails CLOSED for kill switches and OPEN for assignments; folder matching needs no metadata. `assign` rules with no target are skipped (they assign nothing). A `folder` rule on `/` — the value new rules are created with — is deliberately vault-wide, unlike a blank EXCLUSION, which stays a data-entry accident. **Note types** (`domain/rules/note-type.ts`): identity is a SET of identifiers, the OSK registry's canonical names (matched locally against each type's tag/folder/filename-regex mappings; `formula` not evaluable) plus the `type/<x>` tag convention — OSK names types `Personal Notes` while tagging them `type/personal`, so a rule matches either spelling instead of the plugin guessing a singular/plural mapping. `recognizeNoteType` deliberately NOT ported (async; every consumer is a synchronous decision). `ui/osk-note-types.ts` is the whole dependency on the Starter Kit: feature-detected, defensively normalized, `[]` on anything unexpected. `domain/rules/matchers.ts` now holds the folder/tag primitives and `context/exclusions.ts` imports them, so "this note is in that folder" means one thing plugin-wide. **Dispatch**: new `rule-disabled` refusal on `ReviewStart`/`ActionStart`/`ThreadStart` (+ CLI error code), checked before the size guard so a switched-off note never pops a confirmation dialog, and naming the rule in the message. Assign rules feed the default pool; precedence instruction > daemon `editorIds` > rule > all editors. Pools that NAME editors (instruction, rule) now share ONE code path and report disabled/missing named editors as skips; the daemon's re-dispatch pool stays silent as before. A rule pointing at a deleted panel refuses with a new `rule-target-missing` skip instead of quietly reviewing with everyone. `isReviewable` takes the vault reader now (rules need note-type facts) and folds the kill switch in, so commands/menus/CLI/daemon needed no further wiring; new `isPluginEnabledForNote` is the non-review gate. **UI**: `isNoteExcluded` → `isPluginEnabledFor` (exclusion AND kill switch — identical UI outcome, only the services keep them apart), `EditorMenuState.excluded` → `blocked` with an explicit "nothing at all" early return; the rail is now HIDDEN and highlights cleared for a blocked note (previously the rail stayed up on excluded notes offering a Review button whose only outcome was a refusal); `runContextFor` is the single choke point so no triage/bulk/filter command is available or can mutate a blocked note; side-panel binding and card lookup refuse likewise; `Cancel review` stays available on purpose. A hidden run stays BOUND (anchors keep remapping). **Rules tab**: copy now matches the engine, the obsolete "auto-discovery lands later, rules stay inert" note is replaced by a live Starter-Kit-detected/not-detected line, and `ruleSummary` says what a rule RESOLVES to and says so when it does nothing (no target / deleted editor / deleted panel / panel with no members). NOT shipped from §4b: per-rule default ACTION bindings (`defaultTarget` is one target and actions are already bound per verb; a second rule-scoped binding layer needs its own design). +94 specs (1127 → 1221). LIVE CHECKS pending — see history 2026-07-30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Context budget + send preview   | **DONE (code, 2026-07-30)**                         | Stage E slice 2 — plan §4d, M5. Commits: `094f41b` budget policy, `5d05749` one prompt-build entry point + preview service, `64bdfd3` modal/command/settings button, + this docs commit. **Verified already shipped, not rebuilt**: editor-level linked-note context (`includeLinkedNotes` / `maxLinkedNotes`) was already honored end-to-end by `assembleContext` — reviewed note's outgoing links AND embeds (`ObsidianVaultReader.getOutgoingLinks`), depth 1, deduped, capped, budget-aware, excluded notes never inlined, all spec-covered. **New**: `services/context/context-budget.ts` owns the whole budget POLICY (send/priority order, allocation, labels) and documents why the system prompt and the reviewed note are never truncated; `AssembledContext` reports one `ContextSection` per candidate (source size, sent size, `sent`/`truncated`/`dropped`) plus a `ContextBudgetReport` (total, overflow, truncated + dropped paths) — the old `preview`/`truncated` fields had NO consumer and conflated "sent a prefix" with "sent nothing". `buildEditorPrompt` is now the ONLY way a system prompt is built (reviews, transforms, threads, preview), and a spec captures the system prompt of a real dispatch and asserts the preview equals it byte for byte. Attachment blocks carry a `role` so a model can tell persona reference material from the reviewed note's own links. Preview = `preview-context` command (live editor buffer) + a Preview button in the editor dialog (unsaved draft persona, vault text). Specs 1221 → **1258** (+37, per commit +14/+18/+5). NOT shipped from M5: settings import/export and the setup wizard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Custom actions + import/export  | **DONE (code, 2026-07-30)**                         | Stage E slice 3 — plan §4e, M5. Commits: `8b2b589` schema/registry/resolution, `7ab0c51` dispatch, `b83c530` Actions tab, `50fd566` transfer module, `cd34766` dialogs, + this docs commit. **Custom actions are first-class**: `actionBindingSchema.customVerbClass` (transform \| generate \| review) is REQUIRED with no default — they were transform-class by construction, so an action whose instruction said "check this for factual errors" replaced the checked text; defaulting to transform would keep that bug behind a nicer UI. `resolveActionVerb` in the verb registry is now the ONE way any dispatch path turns an action id into the verb it runs (registry entry for built-ins, the custom action's own `ActionVerb` otherwise; built-ins win, so a binding cannot redefine "Humanize"), and in the controller verb resolution moved AHEAD of the class split — which is what lets a review-class custom action carry its vault-resolved instruction into `startReview` and bind a panel (the panel rule was already written in terms of the class). `followLinks` on a custom instruction is now live with context-assembly semantics (depth 1, embeds, cap per referenced note, exclusions before any read); the directive text is serialized first so the instruction cap can only cut reference material. Actions tab gained the class dropdown (no default value), the instruction-notes list with its follow-links toggle, per-class instruction copy, and a panel option that appears exactly when the class allows one; switching a bound action off review-class clears a panel binding the new class cannot use (`setCustomActionClass`, spec-covered) instead of letting the action vanish. **Import/export**: pure `domain/settings/settings-transfer.ts` + pure copy in `settings/transfer-model.ts`, dialogs in `settings/transfer-modals.ts`, entry points under Behavior → Import & export. Export writes the ticked sections (backends/editors/panels/actions/rules/voice profile) to a vault JSON path or the clipboard; unselected sections are absent, not empty; an existing file needs an explicit overwrite confirmation. **API keys never travel in EITHER direction** — stripped inside `exportSettings` (the only producer) and cleared on import even when the file carries one, so a copied `data.json` cannot bill its owner's account; both dialogs say so, and the import summary points at the Backends tab. Import validates element by element (one bad entity never drops its siblings), regenerates EVERY id and remaps references onto the new ones — `resolveIdCollisions`' keep-first semantics deliberately NOT reused, since there a duplicate means "same entity twice from a sync merge" while here it means "different entity from another vault", and keeping the reference would point an imported panel at the user's own editor. A reference outside the import survives only when that id exists here (re-importing panels into the vault their members live in), else it is cleared with a typed adjustment. Typed rejections for everything else: invalid entity, panel with no member editor, built-in verb already bound (two bindings for one verb = two menu entries + order-dependent lookups), and section-at-maximum — caught while PLANNING, because the facade rejects an invalid update wholesale and "could not save" names nothing. No format marker required (a foreign `data.json` is a legitimate source); an object with no recognizable section is the error. Nothing is written until the user confirms the summary; `voiceProfile` is the one value that replaces. Specs 1258 → **1310** (+52, per commit +12/+2/+3/+25/+10, each verified by checking out the commit and counting). NOT shipped from M5: the first-use setup wizard, now the last M5 item. LIVE CHECKS pending — see history 2026-07-30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Setup wizard (M5 last item)     | **DONE (code, 2026-07-30)**                         | Stage E slice 4 — plan §4f, M5 is now feature-complete. Commits: `540b2c0` pure core (step machine + one shared backend rule + real health check), `592a464` modal/copy/command/first-run trigger, + this docs commit. **Nothing is written until Finish**: the wizard edits a local draft and `applySetupWizard` is one settings update, so cancelling at any step cannot leave a half-configured plugin — there is no sequence of partial writes to interrupt. Commit-per-step was rejected because the backend step is not independently valid (a label with no model is a backend that exists and cannot run). **A step may be left EMPTY but not HALF-FILLED**: the backend step can be skipped outright, and refuses to advance with an incomplete backend. **`onboarded` is set on ANY exit**, matching the schema field's own definition ("completed or was skipped"): a dismissed wizard is not re-offered every launch, and the `Run setup wizard` command + the Behavior tab button keep it re-runnable (it re-seeds its draft from current settings, so a second pass edits rather than resets). **A wizard-added backend becomes the global default when there is none** (editors inherit, so otherwise the wizard hands back a backend nothing uses), never when one already exists. **Test connection sends ONE cheap real request through `createApiEditorExecutor`** — the same path a review takes — so a pass means "reviews will work here", not "the endpoint answers"; THREE outcomes, because a reachable endpoint whose model ignores the structured output (`unusable`) is a different fix from a connection failure. Its timeout is 60 s, not the 10-minute request timeout, and says a slow local model may still work for real runs. `domain/settings/backend-validation.ts` is now the single backend-validity rule (messages included — each message IS the rule in words); the Backends tab dialog calls it instead of its own copy. The last step reports "nothing will run yet" from `hasReviewCapableEditor` over the settings about to be written, never from an approximation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Stage E review fix pass         | **DONE (code, 2026-07-30)**                         | External review of stage E (rules, context budget/preview, custom actions + transfer, panel button, setup wizard) surfaced 17 findings; all verified against the code, all 17 fixed. Commits: `06be0ff` (settings transfer — imported API backends arrive DISABLED with the destination of each one listed before the import is committed, because stripping the key protects nothing when Ollama needs none and an imported enabled editor joins every review; section caps moved BEFORE reference remapping so a capped-out entity leaves no dangling reference; the export dialog stops claiming the file holds no secrets and names the backends whose `baseUrl`/`extraBodyJson` could carry one; `normalizeExportPath` refuses `..` instead of letting the adapter resolve it past the vault root and past the overwrite confirmation), `47dd1f1` (the "what will be sent" preview gains an action picker and resolves the instruction through the shared `resolveBoundActionVerb`, so the up-to-10 000 characters of vault content a custom action inlines are finally visible and accounted; a foreign OSK note-type regex is compiled once, length-bounded, and refused when it carries a nested quantifier, so it cannot freeze the renderer), `7de8c4c` (`resolveRuleEditorPool` resolves an EDITOR target symmetrically with a panel one — a deleted one made every matching note un-reviewable with an anonymous skip while the Rules tab said the rule "does nothing"; `reviewGate` becomes note-scoped through the extracted `resolveReviewParticipants`, adding `rule-target-unusable`, so a surface can no longer offer a review the dispatch refuses; panel empty state derived from the gate; panel dispatch latched before its first await), `ba85644` (the wizard persists the VALIDATED backend, requires a model before advancing, derives its step count, and stops blaming the Backends tab for causes it cannot prove), `b42ba2c` (the preview command survives a privacy exclusion so the modal can deliver its refusal — plan §4d — while the kill switch still hides it per §4b; `docs/usage.md` push-back placeholder line removed), + this docs commit. Specs 1394 → 1434 (+40), verified per commit by checking out and counting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Panel runs (stage F sl. 1)      | **DONE (code, 2026-07-30)**                         | M6 slice 1 — a panel stops being a stage-C fan-out and becomes ONE run. Commits: `6ffdb22` charter seam, `ef41d84` run + aggregation lifecycle, `89e8583` dispatch, + this docs commit. **The charter reaches every member** (plan §4 said "aggregation instructions"; it is now the panel's shared brief, in BOTH roles): `augmentPanelCharter` appends it inside `buildEditorPrompt`, after the persona/context and before the per-run instruction, framed so it directs what the panel weighs without replacing the member's own mandate — a panel that homogenized its members would be N copies of one editor. The send preview therefore stays honest (one prompt-build entry point, stage E). Resolution goes through the new shared `resolvePromptSourceText` (services/context), which is now the ONE resolution of a non-persona `PromptSource` — fresh reads, exclusions absolute, follow-links depth 1, per-note dedupe, capped; `resolveCustomInstruction` delegates to it. **The starter panel's charter was rewritten**: it used to script the chairperson ("You are the chairperson… produce a verdict per member"), which reads as nonsense in a member's prompt AND duplicates what the `aggregate-panel` operation prompt already dictates. One user-authored field, correct as a member brief and as the aggregation system prompt; the chairperson's mechanics stay in the contract. **The run owns both halves** (`run-controller.ts`): members are ORDINARY editor streams — same anchoring, same finding store, same plugin-wide permit gate, so they cannot starve another note's run and their findings keep their per-member editor identity. `StartRunInput.panel` adds the aggregation step; `RunHandle.getPanelState()` + `panelSettled` expose it. **Partial-failure policy — DECIDED** (`domain/panels/panel-aggregation.ts`, pure + spec-covered): the panel completes with the members that succeeded; a failed member is reported as failed, stays individually retryable inside the run (verified: the stage-A `retryEditor` works inside a panel run), and travels to the chairperson flagged so the scorecard states what it did not see; aggregation runs when ≥1 member succeeded, else `skipped`. All-or-nothing was rejected — one member timing out on a laptop model is common, and three honest reviews plus a named gap beats a refusal. Nothing is ever silently dropped. **Cancellation**: `cancelRun` cancels every in-flight member AND the pending/in-flight aggregation (an already-produced scorecard stays inspectable, like the findings of the members that did finish). A retry re-opens the panel and re-derives the scorecard, guarded by an attempt epoch. **Dispatch**: `startReview` takes a `panel`; a binding rule assigning an ENABLED panel makes the run a panel run with no caller involvement (so command/rail/panel button/CLI/daemon all get it at once), a DISABLED panel still supplies its members but without charter or scorecard; a review verb bound to a panel dispatches one panel run with its instruction on top of the charter; a daemon refresh keeps the panel identity. New typed refusal `panel-unavailable` (missing/disabled) — the members may be healthy, so it points at the Panels tab. A panel whose aggregation backend does not resolve still runs, reporting `unavailable`. **Deliberately NOT in this slice** (slice 2): the scorecard UI (side panel + rail), the panels settings tab CRUD, editors-vs-panels visual distinction, CLI shaping of a panel run, "Generate more" / `InsertAt`. Specs 1434 → 1472 (+38), per commit +11 / +19 / +8 — measured by running the suite, not by arithmetic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Panel scorecard (stage F sl. 2) | **DONE (code, 2026-07-30)**                         | M6 slice 2 — the typed `PanelResult` scorecard, on screen. Commits: `cdb9e4e` contract, `ffdb4f8` aggregation compaction + budget, `6bb0efb` side-panel scorecard, `35e1ba1` rail panel entity, + this docs commit. **Contract**: `topFixes` became `{ action, editorName?, quote? }` (the pointer back to a member finding — optional, because a structural fix anchors to no span and an invented pointer is worse than none) and `dissent` became `{ subject, positions: [{ editorName, stance }] }[]` (a single string invites averaging four readings into one balanced sentence, which is the opposite of what a panel is for); `recommendation` gained a one-line `rationale`; members gained `omittedFindings`. **Aggregation input**: compacted (anchoring aids, evidence, confidence and rationale dropped — the chairperson weighs, it never anchors or re-verifies; critique/suggestion/summary clipped; **quotes never truncated**, because the top-fix pointer resolves by quote) and fitted to half of `contextBudgetChars` minus the charter, **round-robin** so one verbose member cannot leave the last member of the panel unheard, each member kept a strict prefix with the remainder reported as `omittedFindings`. **UI**: the side panel renders the scorecard above the member sections for EVERY aggregation status (a failed synthesis never costs the reviews), top fixes resolve to live findings by quote (exact-in-member → exact-anywhere → normalized, null rather than a wrong span per BR #4) and reveal them, dissent renders as structure; the rail shows the panel as ONE hollow ringed chip owning its member dots (BR #11), named "(panel)" in the accessible name too. Specs 1472 → 1510 (+38: +3/+12/+17/+6, verified per commit). Not in this slice: Panels settings-tab CRUD, CLI shaping of a panel run, "Generate more" / `InsertAt`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Panel polish (stage F sl. 3)    | **DONE (code, 2026-07-30)**                         | M6 slice 3 — the editors-vs-panels distinction made consistent, and the continuation affordance. Commits: `435e6ad` entity vocabulary, `f4b1816` continuation core, `37ce095` continuation UI, `00ec193` charter refinement, + this docs commit. **Business Rules #11, one author**: four surfaces each said "panel" in their own words and each only halfway (rail inline, dropdown glyph+word, scorecard ring + plain name, menus/commands SILENT — a verb bound to a four-member panel read exactly like one bound to a single editor). `ui/entity-label.ts` now owns the vocabulary with two decisions written down: never shape or colour ALONE (a ring is invisible to assistive tech, so the NAME carries `(panel)`), and only panels are marked (an editor is the unmarked default — marking both makes the marker noise). Applied to the rail chip + its member group, the target dropdowns, the scorecard header, the side-panel member sections (indent + `aria-label` naming the panel while keeping the editor's own identity), the context menu and the dynamic action commands: a panel-bound verb reads `Critique (panel: Pre-publish Review)` — the panel is NAMED because one press is one request per member, so the user is choosing a cost. Menu ORDER still sorts on the bare label (the marker must not shuffle a menu navigated by verb). `ResolvedAction.panelName` carries the name so four surfaces do not each look it up. **"Generate more"** (`RunHandle.continueEditor`) is the mirror image of `retryEditor`: findings are KEPT and appended, only a `done` editor is continuable. Non-repetition is handled twice because either alone is weak — the request carries `alreadyReported` (quote + clipped critique; ABSENT rather than empty on a first pass so a backend can tell "nothing yet" from "found nothing") with a prompt asking for ADDITIONAL findings and explicitly allowing an empty result, and whatever slips through hits the editor's existing dedupe keys, deliberately kept. **The load-bearing decision is the failure policy**: a failed or cancelled continuation does NOT mark the editor `error`/`cancelled` — it restores `done` and records `continuationError` separately, because marking it failed would offer Retry, and Retry REPLACES an editor's findings; one click after an optional second pass timed out would destroy the first pass's work mid-triage. Same reasoning: a continuation reporting no summary/verdict leaves the first pass's standing. New findings anchor against the text passed at call time (fresh anchor base, like a retry, BR #3/#4); a member going back to work re-opens its panel's scorecard (`reopenPanel`, now shared with `retryEditor`). UI: `Generate more (n)` under each finished editor's bulk row — the count is in the label because every press is a paid request on a note that already has findings, and the number changing is the feedback; disabled (not hidden) while its own round runs, so one press is one round and the control does not vanish under the pointer; an editor at (0) still gets it ("I found nothing" is worth challenging once); a failed round renders BESIDE the button, section untouched. Palette `generate-more` fans one round out to every finished editor of the active note's run. The count comes from what the section shows and is NOT severity-filtered — the lens hides findings, it does not un-report them, and understating what an editor found would invite it to repeat itself. **Starter charter refined** for the slice-2 scorecard shape: two paragraphs at charter level (rank by what unblocks publication, tie a fix to the exact passage; keep disagreement as attributed positions, a difference of emphasis is not a disagreement) — the operation prompt dictates the FORMAT, nothing supplied the judgment behind it. Both read correctly in the charter's other role (every member's prompt). **Verified, not rebuilt**: panels settings-tab CRUD already ships (Panels tab + modal, card gallery, ringed dots, referential-impact deletion), and stage C's panel fan-out is already gone — `resolveActionBinding` → `ResolvedAction.panelId` → `runBoundAction` → `startReview(panel)` → `resolveReviewParticipants` is ONE path shared by command, rail, panel button, CLI, daemon and bound actions (slice 1). Specs 1510 → **1540** (+30), per commit +12 / +11 / +6 / +1, measured by running the suite. Remaining for M6: CLI shaping of a panel run. LIVE CHECKS pending — see history 2026-07-30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Stage F review fix pass         | **DONE (code, 2026-07-30)**                         | External review of stage F (panel runs, scorecard, polish) — 15 findings, all verified against the code and fixed. Commits: `6d11cdf` run lifecycle + scorecard reconciliation (a run being aggregated reports `isBusy()` so Cancel/rail/daemon/CLI stop treating it as idle; a continuation KEEPS its scorecard marked `resultStale` instead of discarding one that is still accurate; `memberVerdicts` reconciled against the run's roster; a top fix credits the member whose finding it actually reveals), `f2c62b6` the "what will be sent" preview accounts for the panel charter a bound action or a binding rule brings, and `panel-unavailable` refuses before the size guard (no confirmation dialog on the way to a refusal), `ab409e9` `omittedFindings` counts findings dropped by the contract cap, not only by the budget, `a2d5313` **CLI shaping of a panel run** — `ai-editor:review` awaits `panelSettled` and returns the scorecard instead of paying for one and aborting it (M6's last remaining item; the reconciliation moved to `domain/panels/scorecard-model.ts` so the CLI and the side panel share one policy), `49cd9f3` UI/a11y (the rail's panel verdict reaches the accessible name and the compact tooltip, finding cards carry their panel per BR #11, "Generate more" leads its accessible name with its visible label per WCAG 2.5.3, the palette fan-out states its cost, plus the missing-member pill contrast, a pulse for a scorecard being written — reduced-motion list updated — and the top-fix button's focus ring). Specs 1540 → 1558 (measured per commit by running the suite).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| CLI boundary (stage G sl. 1)    | **DONE (code, 2026-07-30)**                         | M7 slice 1 — the process seam, built provider-agnostic and BEFORE any tool adapter so no CLI backend can be wired around it. Commits: `988ee58` (executable/env/capture/protocol checks), `fff53ba` (process-tree kill), `c403c14` (the spawn + conformance suite). New folder `src/app/services/backends/cli/`; +84 specs (1558 → 1642). Business Rules #9, as code: **executable** = absolute path to an existing executable regular file, never resolved through PATH (hijacking), Windows scripts refused by extension (running one means running cmd.exe); **environment** built from `{}` — `HOME`/`PATH`/locale/Windows-required names plus explicitly-configured API-key variables, with loader/runtime/shell-hook names denied even when configured, `TMPDIR` redirected into the run directory and colour off so ANSI cannot corrupt the protocol; **cwd** = a `mkdtemp` throwaway created and removed by `spawnCliProcess`, which takes no cwd parameter — not the vault and not the plugin data folder (it lives inside the vault and syncs); **no shell** — `createCliChild` is the only door into `node:child_process` and its options type has no shell field, argv crosses as an array; **content on stdin only**, args capped at 4096 chars because argv is world-readable in `ps`; **bounded output** — stdout stops and fails (a truncated JSON document is corrupt, not smaller), stderr keeps the tail and flags it, and comes back as `StderrDiagnostics` whose toString/toJSON are status-only so a Notice or an error report cannot leak a transcript that echoes the key (BR #12); **process-tree kill** — POSIX process group (`kill(-pid)` + `kill(-pid, 0)` as the verification probe), Windows `taskkill /T /F`, graceful → grace → forced, result reported as `already-gone`/`terminated`/`force-terminated`/**`survived`** rather than assumed; **timeout** bounds the whole operation. Node access is the precedent set here, confined to `node-fs.ts` + `node-process.ts` (desktop-only, `node:*` already external — verified against a real bundle). Conformance suite spawns REAL children (`process.execPath -e`): happy path, malformed JSON, no output, slow vs timeout, huge vs cap, non-zero exit, signal death, cancel killing a sleeping GRANDCHILD (both pids probed until gone), a SIGTERM-ignoring tool being forced, stdin round-tripping metacharacters/unicode/512 KiB, argv verbatim (`$(touch /tmp/pwned)` asserted not to have run). NOT user-reachable yet: `review-service` still refuses `cli-backend-unsupported` — slice 2 brings the Claude Code / Codex adapters and their protocols. One deliberate deviation, documented: stderr overflow truncates instead of failing (memory is bounded either way; a chatty tool should not lose its result over its own logging).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| CLI adapters (stage G sl. 2)    | **DONE (code, 2026-07-30)**                         | M7 slice 2 — the Claude Code and Codex adapters, behind the slice-1 boundary. Commits: `05b689d` (tool adapters), `f1324f2` (the executor). +104 specs (1642 → 1746, +70 / +34 measured per commit). **Invocation contracts were READ OFF the binaries and exercised end to end**, success AND failure paths, with the exact argv the code emits — `claude --help` (2.1.220) and `codex --help` / `codex exec --help` (@openai/codex, 2026-07-30). No flag is inferred; a future version that drops one fails loudly with the unknown option named on stderr. Claude Code: `--print --output-format json --no-session-persistence --strict-mcp-config --permission-mode manual [--model M] [--tools '']`. Codex: `exec --json --color never --skip-git-repo-check --ephemeral --sandbox read-only [--model M] -`. **Session persistence off on both** — otherwise a review of a private note leaves a verbatim copy under the user's home directory, outside the vault's exclusion rules and outside anything the plugin can delete (BR #7, #12). **Consent, honestly**: Claude Code reports `canGrantTools: true` (`--tools ''` is a real switch, and granting tools does NOT relax `--permission-mode manual`); **Codex reports `canGrantTools: false` and its argv does not vary with `allowTools`** — running shell commands is not a Codex feature that can be switched off, so there is nothing consent could safely unlock, and a toggle that silently did nothing would be worse than none. `--dangerously-*`, `workspace-write`, `danger-full-access`, `--add-dir`, `-C/--cd`, `--mcp-config`, `--settings` are emitted by no code path (spec-asserted for both consent states). **Envelope parsing is defensive about what was OBSERVED**: Claude Code sets `subtype: "success"` and exits 0 on a failed run, so `is_error` is the authoritative flag; Codex emits non-terminal `error` ITEMS (a model-metadata warning on a run that then succeeds) and top-level `{"type":"error"}` reconnection lines before the terminal `turn.failed`, and also exits 0. `turn.failed` outranks any message that preceded it. **No failure message ever echoes tool-authored payload text** — a live Codex failure carried the full endpoint URL with query parameters; only a shape-checked status token (`/^[a-z][a-z0-9_]{0,39}$/`, dropped entirely if it does not match) and the reported HTTP status get through. **Prompt delivery reuses `buildUserMessage`** in its `'json-object'` style (neither tool exposes a usable forced-schema mechanism: `--json-schema` and `--output-schema` need argv space the 4096-char cap denies, or a file the run cannot own), persona framed in `<persona-instructions>` on stdin because both system-prompt flags are argv-bound. **`createCliEditorExecutor` is indistinguishable from `createApiEditorExecutor`** to the orchestrator: same signature, same exactly-one-terminal-event protocol, and the SAME `extractJsonPayload` + `validateOperationResult` — a result from a local pipe is not more trusted than one from HTTPS. Boundary failures map without guessing (`cancelled`/`timeout`/`stdout-overflow` → their equivalents, everything else `unknown`; `auth`/`rate-limit` only ever come from a tool-filled envelope). Messages stay status-only, adding the stderr byte-count summary, the timeout setting's name, and a plain statement when a process tree **survived**. `resolveCliModel` = per-editor override → backend default → tool default, where empty is a valid answer (unlike the API path's `no-model-configured`). Spec'd against an injected boundary: **the suite passes with neither binary installed**. STILL NOT user-reachable, deliberately: `review-service` continues to refuse `cli-backend-unsupported` and the backend modal still hardcodes `family: 'api'`, so slice 3 owns the settings form, the consent UI and the resolution wiring — flipping the refusal now would ship a CLI backend before there is a surface that asks for consent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| CLI settings + consent (sl. 3)  | **DONE (code, 2026-07-30)**                         | M7 slice 3 — CLI backends become reachable. Commits: `a120cc0` (consent as a record of WHICH binary, replacing the `allowTools` boolean), `926d9dd` (`resolveBackendRef` resolves both families + `backends/backend-executor.ts`, the one place a resolved backend becomes runnable — no dispatch surface branches on `family` any more), `86eb529` (health check through the real boundary + the never-runs-anything detector), `6b23f79` (Backends tab, CLI dialog, the two consent dialogs, spec-pinned copy), + this docs commit. Consent is stored as the executable path it was granted for, so an edited/imported/sync-merged path revokes it automatically; tool consent additionally requires launch consent. `enabled` is not consent — an enabled-but-unconsented backend resolves to the typed skip `cli-consent-required`. Imported CLI backends arrive switched off AND unconsented. Detection probes a curated location list, never `PATH` and never by executing. Test connection runs one real review through the boundary and asks for launch consent first, because it is a launch. Specs 1746 → 1825 (+79; per commit +29 / +10 / +22 / +17 / +1, measured per-commit in a clean worktree).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Stage G review fix pass         | **DONE (code, 2026-07-30)**                         | External review of the M7 CLI slices — 16 findings, all verified against the code and all fixed. Commits: `b046e89` (the process boundary: the tree is now probed and killed on EVERY path including a clean `exit 0` — the old guard keyed on `child.exitCode` and so never killed the descendants an agent leaves behind, reproduced live; `kill` moved onto the shared outcome base and a run whose tree could not be stopped now FAILS rather than delivering an answer under a leak; an already-aborted signal is refused before anything is allocated and again before `createCliChild`, so the program is never executed; `passThroughEnvNames` removed — no caller, no schema field, three doc surfaces promising it — which also retired the unreachable `invalid-environment` code), `863494e` (the Windows `taskkill` spawn went through `createCliChild` and `validateExecutablePath` instead of inheriting the renderer's full environment and trusting a concatenated `SystemRoot`), `80a4185` (launch consent enforced in `createBackendExecutor`, the seam where a backend becomes a process, so the health probe cannot start a binary the user never allowed), `fa5da22` (the health check quotes the timeout it applied rather than the 120 s ceiling; a re-render no longer strands the Test connection button for the length of a 120 s probe; the Backends tab validates the executable before asking for consent, like the dialog; two consent sentences narrowed to what the flags actually deliver), `b453515` (README/usage/Architecture/BR #9/adapter notes: 'no inherited MCP servers or plugins' was false for both tools — Codex reads `~/.codex/config.toml` where `[mcp_servers.*]` lives, Claude Code loads plugins/hooks/skills/CLAUDE.md and honours the user's own `permissions.allow`; a 'what the plugin does not bound' section replaces the claim, plus the fact that only Codex has a read-only sandbox; the NUL byte that made `tools/index.spec.ts` a binary file to git is gone). **NOT shipped on purpose:** `--setting-sources` would make two of those sentences true again and the binary accepts an empty list, but what it drops could not be demonstrated from outside (help text is silent on plugins/hooks; prompt caching confounds a token-count comparison), and shipping a flag as a security control on the strength of its name is how the false guarantee got written in the first place — open item with the experiment it needs. Specs 1825 → 1837, measured per commit in clean worktrees.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| v1 completion sweep             | **QUEUED — full remaining scope, quality-first**    | Sébastien (2026-07-29): implement EVERYTHING planned for v1 in one go; disregard implementation time, maximize quality. Staged: (B) daemon mode — DONE; (C) transform operations end-to-end (rephrase/summarize/humanize/continue + non-destructive inline diff, CM6-undo-integrated Accept, action dispatch from menus/commands + dynamic per-action commands) — service layer DONE + preview UI DONE, see the three "Transform ops" rows — stage C COMPLETE (action dispatch landed 2026-07-30); (D) M4 rest (keyboard triage state machine — DONE 2026-07-30, see "Keyboard triage" row; bulk ops incl. accept-all-non-conflicting as ONE undo step + severity filter — DONE 2026-07-30, see "Bulk triage + filter" row; per-finding push-back threads — DONE 2026-07-30, see "Push-back threads" row; adaptive narrow-pane layout — DONE 2026-07-30, see "Adaptive layout" row) — stage D COMPLETE for the four slices above. **Carve-out**: M4's status-bar finding-count item shipped with M2 and is NOT part of stage D; its live defect (count not updating) stays tracked in the "Live vault verification" row, so "stage D COMPLETE" does not mean "all of M4 is done"; (E) M5 — binding rules filter (SEAM M6) + note-type rules + OSK auto-discovery adapter DONE 2026-07-30 ("Binding rules + note types" row), `[[link]]` context budget + "what will be sent" preview DONE 2026-07-30 ("Context budget + send preview" row), custom actions + import/export DONE 2026-07-30 ("Custom actions + import/export" row), setup wizard DONE 2026-07-30 ("Setup wizard" row) — stage E COMPLETE, M5 feature-complete; (F) M6 panels end-to-end (parallel member runs DONE, typed `PanelResult` scorecard DONE, editors-vs-panels visual distinction + "Generate more" DONE 2026-07-30, CLI shaping of a panel run DONE 2026-07-30 in the stage F fix pass) — stage F COMPLETE, M6 feature-complete; (G) M7 CLI backends (Claude Code + Codex, security boundary, opt-in consent); (H) M8 durable margin comments (sidecar store — schema only, no migrations per no-users policy — rename handling, interrupted-job Retry, background runs, margin column UI); (I) M9 polish (theming vars, reduced-motion + ARIA, perf passes, support CTAs from template, README + docs/ content, community-checklist sweep) + full-repo adversarial review with fix pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| M3 (rest) + M4-M9               | superseded — see "v1 completion sweep"              | See milestone list §6. M3 remaining: per-provider streaming decoders where verified, live badges, cancellation races (precondition-checked apply already landed with M2's Accept path).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

Working conventions for any agent picking this up: read `documentation/history/` (latest day) + `AGENTS.md` first; definition of done = tsc+lint+test+build green + history updated + this table updated; commit per logical chunk (Conventional Commits, hooks never bypassed); `git push` is hook-blocked — push via `bash "$CLAUDE_JOB_DIR/tmp/push-ai-editor.sh"`-style script only when Sébastien asks. Adversarial codex review (`gpt-5.6-sol`, xhigh) after implementation phases (M9) — see `documentation/reviews/` for the plan review already done. **No-users policy (Sébastien, 2026-07-29): the plugin has no users yet — never spend effort on backwards compatibility, migrations, or behavior-preserving defaults; pick the BEST design/default outright and change schemas freely (schema-version machinery stays for post-release).**

### Known review debt — CLOSED 2026-07-29 (later session)

All 14 items below were fixed and committed on 2026-07-29 (commits `75dee05`, `c3499d2`, `f11180d`, `fa066a4`): #1 occurrence-aware dedupe key (+spec), #2 `discardRun` + `cancelAll` clears (+specs) + vault rename/delete hooks, #3 placeholder, #4 `resolveIdCollisions` (keep-first, global uniqueness, persisted repair + Notice, +specs), #5 stale-int fallback, #6 swatch aria labels, #7 obsolete (command functional since M2 wiring), #8 was already fixed (reader.cancel in finally), #9 appliedChanges stops growing after settle, #10 header comment corrected (markStaleEffect documented as M3 seam), #11 CORS named in the network-failure message (full `requestUrl` fallback still a seam), #12 skipsByFile follows rename/delete, #13 revealFinding timer re-validates the view, #14 card closes on window resize. Also fixed: status-bar count bug (shared active-file resolver with live-view fallback). Original findings kept below for context.

### Original findings (2026-07-29 adversarial code review — all fixed, see above)

1. `src/app/services/orchestration/run-controller.ts:265` (correctness) — finding dedupe key `JSON.stringify([quote, critique, suggestion])` omits `prefix`/`suffix`/`occurrence`, so two legitimate findings on different occurrences of the same repeated text (occurrence 0 vs 1 — exactly what the prompt instructs for repeated text) collapse into one; the second is silently dropped. Fix: include `raw.occurrence`, `raw.prefix`, `raw.suffix` in the key so only true stream-vs-result duplicates collapse.
2. `src/app/services/orchestration/run-controller.ts:349` (correctness) — `RunController.runs` never evicts: every reviewed file keeps its last `RunHandle` (snapshot text, `FindingStore`, listeners) forever; `cancelAll()` cancels but does not clear. Unbounded memory growth holding full document copies in long sessions. Fix: delete the map entry when a run is replaced-and-settled, or add a clear/dispose path (remove on file close and in `cancelAll`).
3. `src/app/settings/tabs/backend-modal.ts:12` (correctness) — OpenAI base-URL placeholder says `defaults to https://api.openai.com` but the adapter default (and required shape) is `https://api.openai.com/v1`; a pasted placeholder value yields requests to `https://api.openai.com/chat/completions` → 404 (the base URL replaces the default wholesale). Fix: placeholder → "Optional — defaults to https://api.openai.com/v1".
4. `src/app/domain/settings/settings-schema.ts:213` (security-privacy) — entity IDs have no uniqueness validation (`id: z.string().min(1)`, arrays unchecked) and `checkReferentialIntegrity` does not detect duplicates. data.json is syncable (BR #12); a sync-merge conflict producing two backends with the same id resolves by first-match everywhere (`settings.backends.find`), so an editor's backend ref can silently route note content + the wrong API key to a different endpoint than the UI displays. The `ids.ts` docstring promises collision detection/remap; nothing implements it. Fix: duplicate-id check (per array and across arrays) in `loadSettings` or `checkReferentialIntegrity`; on collision keep first, regenerate later ids (remap references), surface an integrity warning.
5. `src/app/settings/tabs/behavior-tab.ts:36` (obsidian-guidelines) — `renderIntField`'s NaN fallback uses the render-time `value` closure, not the last committed value; since numeric commits skip `refresh`, changing e.g. max concurrent requests 3→8 then later blurring the field empty commits the stale 3, silently reverting the user's change. Fix: track `let current = value` locally, use it as the `clampInt` fallback, update after each successful commit.
6. `src/app/settings/components.ts:119` (obsidian-guidelines) — color preset swatches get `aria-label: "Use var(--color-red)"`; screen readers announce the raw CSS token ("Use var dash dash color red"). Fix: store presets as `{ value, label }` pairs and set `aria-label` from the human label ("Use red").
7. `src/app/commands/review-current-note.ts:49` (obsidian-guidelines) — the `Review current note` command reports itself available but only shows a "not connected to a backend yet" Notice. Community-plugin review flags shipped non-functional placeholder commands. Fine as an M2 seam; **must not reach a submitted release**. Fix: gate registration (or `checkCallback` → false) until the run pipeline is wired, or block catalog submission on it.

Minor findings from the 2026-07-29 E2E wiring adversarial review (7 more, none block M3):

8. `src/app/services/backends/api-editor-backend.ts:245` (correctness) — `executeStream`'s `finally` only does `releaseLock()`; the body reader is cancelled solely via the abort listener. When the stream terminates abnormally WITHOUT an abort — an Anthropic in-stream `error` frame thrown from `accumulator.push` (line 315), or `ProviderError` from a malformed tool-input mid-loop — the HTTP response body is left locked-released and unconsumed, so the connection keeps downloading until the server closes it. `transport/executor.ts` (the dead twin) got this right with `await reader.cancel()` in `finally`. Fix: in the `finally` block, call `reader.cancel().catch(() => undefined)` before `releaseLock` (cancel on a done reader is a no-op).
9. `src/app/services/orchestration/run-controller.ts:186` (efficiency) — `applyTextChanges` appends a batch to `appliedChanges` on every forwarded transaction — i.e. every keystroke in a reviewed note — for the lifetime of the run, and the run lives until the plugin unloads (`RunController.runs` never evicts; each retained run also pins the full snapshot text). The history is only consumed by `ingestFinding`, which can no longer fire once every editor stream has settled, so a long editing session on a reviewed note accumulates thousands of dead `TextChange` batches per run, multiplied across every file reviewed in the session. Fix: skip the `appliedChanges.push` once `settledFlag` is true (`findings.applyTextChanges` must still run for staleness tracking), and consider evicting or compacting runs in `RunController` when a newer run replaces them or after settle.
10. `src/app/ui/editor/finding-decorations.ts:67` (dead-code) — `markStaleEffect` is defined and handled by the StateField but never dispatched anywhere in the codebase; the module header claims "the orchestrator dispatches markStaleEffect when a domain anchor goes stale", which is untrue — staleness only reaches decorations via the deferred full `setFindingsEffect` rebuild in `ReviewController.dispatchDecorations`. Dead effect + wrong documentation invites a future contributor to rely on a mechanism that does not run. Fix: either dispatch `markStaleEffect` from the refresh path for anchors that transitioned to stale (cheaper than full rebuild), or delete the effect and `applyStale()` and fix the header comment to describe the rebuild-based propagation.
11. `src/app/ui/review-controller.ts:236` (compatibility) — `fetchImpl: window.fetch.bind(window)` runs backend requests through the renderer's CORS-enforced fetch. Anthropic's API rejects browser-origin requests unless the `anthropic-dangerous-direct-browser-access` header is sent, and self-hosted Ollama/LM Studio endpoints fail preflight unless the user configures `OLLAMA_ORIGINS`/CORS. Obsidian's guideline is to use `requestUrl` to bypass CORS; the plugin is desktop-only so fetch works, but several supported backend shapes will fail with opaque network errors. Fix: if streaming justifies fetch, document the CORS constraint per backend and detect the failure (`TypeError` from fetch) to surface a targeted Notice ("This endpoint blocks browser requests — enable CORS / use a proxy"). For non-streaming backends, fall back to `requestUrl` which is CORS-exempt.
12. `src/app/ui/review-controller.ts:261` (leak) — `skipsByFile` (declared line 132) only ever gains entries — `this.skipsByFile.set(file.path, result.skips)` is never paired with a delete when a run is cleared, a file is deleted/renamed, or the plugin runs for hours across many notes. Beyond the slow leak, `getPanelBinding` (line 319) serves these stale skips for any later run on the same path even if that run's skip set should be empty-but-fresh (it is overwritten on start, but survives run cancellation/completion cleanup indefinitely). Fix: key the skip report on the run instead of the file (store it on/next to the `RunHandle` in `RunController`, cleared when the run is discarded), or delete the map entry in `refreshGlue`/`updatePanels` whenever `runController.getRun(path)` returns null. Also register a `vault.on('rename')` handler or re-key on rename if file-keyed storage stays.
13. `src/app/ui/review-controller.ts:692` (correctness) — the 1.5 s selection-collapse timer in `revealFinding` captures `editor` and calls `editor.getCursor()`/`setSelection()` when it fires, guarding only `this.disposed`. If the user closes the leaf (or the view switches file / enters reading mode) within that window, the callback operates on a detached editor whose CM instance has been destroyed, which can throw and also silently move the cursor if the same view now shows a different note. Fix: in the timer callback, re-validate the target before touching the editor: `const v = this.findMarkdownView(filePath); if (!v || v.editor !== editor || v.file?.path !== filePath) return;` (or look the glue up in `this.glues`) before the `posEquals` check.
14. `src/app/ui/editor/finding-card.ts:310` (ui-polish) — `openCard` closes the card on `scrollDOM` scroll, document mousedown, Escape, and doc changes, but never listens for window resize (or outer-container scroll). The card is `position: fixed` with coordinates computed once; resizing the window or a popout leaves it floating at stale coordinates, potentially fully outside the visible viewport with no way to see it (click-away still closes it, so it's recoverable, but it looks broken). Fix: in `openCard`, also register `win = doc.defaultView; win?.addEventListener('resize', this.onScroll)` and remove it in `closeCard` (reuse the existing close-on-scroll handler; alternatively re-run `positionCard()` on resize since the anchor rect is cached).

## 1. Vision

Bring AI editing/reviewing/QA **into** the Obsidian editor itself — not a chat sidebar bolted onto a note, but AI personas that live in the margins of the text, highlight what they care about, argue with you, and propose surgical edits you accept or reject inline.

The user writes; a configurable crew of **Editors** (AI personas) and **Panels** (groups of editors) review, critique, rewrite, fact-check, and coach — all grounded in the user's own vault (voice profile, style rules, personas documented as notes).

### Design north stars

1. **The text is the interface.** No modal chat. Findings anchor to spans in the note; actions happen where the text is (Maggie Appleton: "bring the LLM to the editing and thinking process, rather than exiting into a chat interface").
2. **Suggestions, never silent mutations.** Every AI change is a visible diff with Accept/Reject. The user stays the author.
3. **Ignorable by design.** Findings fade away when dismissed; daemons never block writing (Appleton's daemons principle).
4. **Vault as configuration.** Every prompt (editor persona, panel charter, voice profile) can be typed directly in a textarea OR sourced from 0-n vault notes. Documenting your assistant in your vault IS configuring the plugin.
5. **Full parity in Live Preview AND Source mode.** Both are CM6, so highlights, rails, cards, diffs, and margin comments must work identically in both. Source mode is a first-class citizen, not a degraded fallback — test every surface in both modes.
6. **Desktop-first.** Mobile support is explicitly not a priority.

## 2. Inspiration analysis

### 2.1 Juri Strumpflohner's "Quill" (X posts, analyzed frame-by-frame from demo videos)

Sources:

- https://x.com/juristr/status/2074494746484236459 (8-min demo, July 7 2026)
- https://x.com/juristr/status/2077036970895872368 (28-s dark-mode demo, July 14 2026)
- https://x.com/juristr/status/2079297727364464700 (screenshot of async review, July 20 2026)

Observed UX (all confirmed via frame extraction):

| Element                        | Behavior                                                                                                                                                                                                                                                                     |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Persona rail** (left gutter) | Colored dots, one per persona. Hover reveals name (e.g. "DEVIL'S ADVOCATE") + edit/delete icons. A `+` dot creates a new persona.                                                                                                                                            |
| **Summon button**              | Above the rail. Click → all personas review the doc. Button becomes "Cancel"; dots become spinners; when done, each dot shows a **count badge** (findings count, e.g. 8/4/8).                                                                                                |
| **Span highlights**            | Each persona highlights the text spans it has findings for (subtle tinted background).                                                                                                                                                                                       |
| **Review card**                | Click a highlight → floating card near the span: persona name ("Concision Editor"), critique text, quoted passage, buttons **Suggest / Apply / Dismiss**, plus a freeform input "Push back, ask for evidence…" to argue with the persona.                                    |
| **Inline diff**                | "Suggest" → in-place diff: red strikethrough deletions, green insertions, one-line rationale ("Dropped 'just' and 'a bunch of', replaced the vague clause…"), **Accept / Reject**, plus "Refine this suggestion…" input.                                                     |
| **Selection context menu**     | Select text → menu: Rephrase, Say more, Critique, Find evidence, Get research, Identify assumptions, + "Ask the daemon…" freeform. Keyboard shortcuts shown.                                                                                                                 |
| **Generate more**              | A ghost "+ Generate more" affordance between blocks → inline "Generating…" placeholder → continuation drafted in place.                                                                                                                                                      |
| **Async review (v2)**          | Select text + type an instruction → margin comment card showing the quote, the instruction, status **SUBMITTED**, then a live **"Reviewing 0:27"** timer while a background agent works. Result lands back in the card. Google-Docs-comments where the reviewer is an agent. |

### 2.2 Maggie Appleton — Language Model Sketchbook (https://maggieappleton.com/lm-sketchbook)

- **Daemons**: background characters with distinct epistemic roles (devil's advocate, synthesizer, evidence-fetcher, elaborator, encourager). Non-intrusive; suggestions can be ignored and fade.
- **Epi**: context-menu integration — localized, task-specific help (rephrase, critique, evidence, assumption-checking) inside the writing tool.
- Core thesis: chatbots are the wrong interface for writing support; embed affordances in the editor.

### 2.3 The user's OSK AI ecosystem (feature quarry)

The vault already contains a mature agent architecture ([[AI Assistant Architecture]]) whose concepts map 1:1 onto plugin features:

| Vault concept                                                                                                                                                | Plugin analog                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| **Agent** = SOUL.md (identity, voice, DO/DON'T, expertise, decision framework, boundaries)                                                                   | **Editor** persona whose prompt can point at vault notes (a SOUL note works as-is)          |
| **Panel** = N agents → individual verdicts + aggregated recommendation + top-3 fixes + dissent (e.g. osk-panel-publish: Editor, Beginner, Power User, Hater) | **Panel** entity: 1-n editors + aggregation step → scorecard view                           |
| `user-voice-profile` context loader (My Voice Profile, Content Strategy, Content Types)                                                                      | **Voice/style configuration** (textarea + 0-n note refs) injected into every editor run     |
| `osk-writing-humanizer` (AI-pattern detection, banned vocabulary, P0/P1/P2 severity, structure-is-the-#1-tell)                                               | Starter editor: **Humanizer** — flags AI-sounding passages, metronomic rhythm, banned words |
| `osk-writing-rigor` (hedge-stacking, uncited claims, magnitude language, weasel openers)                                                                     | Starter editor: **Rigor Auditor**                                                           |
| `developassion-style-police` / `style-guard` (banned words, emdash abuse, signature elements, pass/fail gate)                                                | Starter editor: **Style Enforcer**; a "pre-publish gate" panel                              |
| Agents: Hater, Skeptic, Beginner, Power User, Storyteller, Provocateur                                                                                       | Starter personas for the editor gallery                                                     |
| Scorecards with verdicts (publish / needs work / kill it)                                                                                                    | Panel result UI                                                                             |

## 3. Decisions locked so far (from the grilling session)

| Decision             | Choice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Audience             | **Public community plugin**, fully configurable; the user's own style rules become his persona configs                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Backends             | **Both**: direct LLM APIs (BYOK) **and** agent CLIs (Claude Code, Codex). Desktop-only                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| v1 surfaces          | **All four**: persona rail + summon, selection context menu, async margin comments, generate/continue                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Persistence          | **Ephemeral findings** (in-memory CM6 decorations, remapped as you type, gone on close — re-summon is cheap) + **sidecar store** for async comments (survive editor sessions, background agents outlive the note being open)                                                                                                                                                                                                                                                                                                                                 |
| Persona model        | Per-editor **model/backend override** (fallback to global default) + starter pack + JSON import/export                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Anchoring            | **Exact-quote matching** (structured findings quote the text; plugin locates quote in raw markdown, exact → normalized → fuzzy; unmatched findings degrade to a note-level list)                                                                                                                                                                                                                                                                                                                                                                             |
| Naming               | Personas are **"Editors"**; groups are **"Panels"**. Plugin name: **AI Editor**                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Panels               | **In v1** ("I want it all"): user composes panels from 1-n editors; aggregated scorecard (verdicts, top fixes, dissent)                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Action bindings      | Every UI action (rephrase, summarize, critique…) is **mapped in settings to an editor or panel**                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Prompt sourcing      | Every prompt field = **textarea AND/OR 0-n vault note refs** ("configure directly as a prompt, or indirectly by documenting things in my vault")                                                                                                                                                                                                                                                                                                                                                                                                             |
| Finding arrival      | **Stream as they land** — highlights pop in progressively, badges count up live; first insight in seconds                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Run scope            | **Whole note by default; selection overrides** when text is selected. Size warning + confirm above a configurable word count                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Panel aggregation    | **LLM aggregation call** after members finish: charter prompt + member outputs → recommendation, top-3 fixes, dissent. Panel has its own provider/model for this call                                                                                                                                                                                                                                                                                                                                                                                        |
| Extra context        | **Opt-in linked notes per editor** (1 hop, capped) + `[[wikilink]]` references typed in ANY prompt/input field (persona prompts, push-back, refine, custom actions) resolve and attach those notes as context. Wikilink autocomplete in plugin inputs                                                                                                                                                                                                                                                                                                        |
| Providers            | 1-n configurable provider instances: **Anthropic, OpenAI + any OpenAI-compatible (custom base URL), Azure AI Foundry (first-class: deployment name, api-version, auth header), Ollama**. Per-editor AND per-panel provider/model selection                                                                                                                                                                                                                                                                                                                   |
| Layout               | **Adaptive Juri layout**: narrow panes collapse to highlight-click → card + side-panel list. **Threshold superseded (stage D slice 4)**: the original ~900px governed the THREE-column layout (left rail + floating cards + right margin comment column); without the M8 margin column the chrome fits down to ~500px, so the shipped collapse is ≤500px → compact rail + pane-clamped cards, back to wide at ≥560px (hysteresis). ~900px is re-evaluated when the M8 margin comment column lands — it may need its own, higher threshold on top of this one |
| Keyboard             | **Full keyboard triage**: hotkey-assignable commands for next/prev finding, accept, reject, dismiss, suggest, focus push-back. Review = merge-conflict-style rapid triage                                                                                                                                                                                                                                                                                                                                                                                    |
| Onboarding           | **Guided setup wizard** on first use: pick/detect backend (Ollama running? `claude` on PATH?), test connection, seed starter editors, 30-second tour on sample text                                                                                                                                                                                                                                                                                                                                                                                          |
| Guardrails           | Size warning + confirm; **exclusions** (folders/tags/`ai_editor: false` frontmatter flag block review; strip-frontmatter option); **nothing runs automatically on note open — every AI action is user-initiated (sole opt-in exception: daemon mode, BR #1 carve-out)**                                                                                                                                                                                                                                                                                      |
| Conversations        | **Per-finding threads**: push-back → editor reply → counter → revised suggestion, collapsed history in the card. Session-scoped                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Editor modes         | **Full parity in Live Preview AND Source mode** (both CM6); Reading view interaction out of scope                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Note-type rules      | **Binding rules** (folder/tag/frontmatter → default editors/panel/bindings) + optional **OSK auto-discovery** (feature-detected, never mandatory) + per-scope **kill switch** (disable the plugin's UI entirely for chosen note types/folders)                                                                                                                                                                                                                                                                                                               |
| Bulk triage          | **Accept/dismiss all per editor** (rail dot menu), **accept all non-conflicting** (one undoable transaction, precondition-checked), **severity filter** (info/suggestion/warning)                                                                                                                                                                                                                                                                                                                                                                            |
| Ambient UI           | **Status-bar item** for the active note: last run's finding count + gate verdict badge; click → side panel. No heat strip in v1                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Settings UX          | **Tabbed settings** (Backends / Editors / Panels / Actions / Voice & Style / Rules / Behavior), well organized                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Overlapping findings | **Stacked card with tabs**: one blended highlight; the card shows every editor's take on that span (own suggestion + thread each); only one suggestion in diff-preview at a time                                                                                                                                                                                                                                                                                                                                                                             |
| Language             | **Match the note's language automatically** (critiques, suggestions, threads in the text's language) + global override ("always respond in X")                                                                                                                                                                                                                                                                                                                                                                                                               |
| Comment routing      | Async margin comments run on a **configurable default editor**, with a persona chip / @-mention in the input to reroute per comment                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Review surface       | **Body only in v1** — frontmatter/metadata review (description, title, aliases, full properties) is post-v1 (GitHub issue)                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## 4. Domain model

```
Editor
  id, name, color, icon?
  promptSource:      { text?: string; noteRefs: NotePath[] }   // concatenated at runtime
  backend:           BackendInstanceRef + model | 'inherit'    // provider instance or CLI agent
  contextPolicy:     { includeLinkedNotes: bool; maxLinkedNotes: number }  // opt-in, 1 hop, capped
  capabilities:      { review: bool; rewrite: bool; research: bool }  // what it may do
  enabled:           bool

Panel
  id, name, color/badge style (visually distinct from Editors everywhere)
  members:           EditorId[]           // 1-n
  charterSource:     { text?: string; noteRefs: NotePath[] }   // shared brief: augments EVERY member's prompt AND is the aggregation system prompt
  aggregationBackend: BackendInstanceRef + model | 'inherit'   // powers the scorecard call
  aggregation:       'scorecard' | 'merged-findings'
  enabled:           bool

BackendInstance (1-n configured in settings)
  ApiBackend:  provider (anthropic | openai-compatible | azure-ai-foundry | ollama),
               label, apiKey?, baseUrl?, deployment?/apiVersion? (azure), defaultModel
  CliBackend:  kind (claude-code | codex | custom), label, executablePath,
               args template, cwd (vault), timeout

VoiceProfile (global, singleton in settings)
  source:            { text?: string; noteRefs: NotePath[] }   // e.g. [[My Voice Profile]]
  injectInto:        'all' | per-editor opt-out

Action  (built-in verbs surfaced in context menu / command palette)
  id: rephrase | summarize | critique | say-more | find-evidence |
      identify-assumptions | simplify | continue | custom…
  binding:           EditorId | PanelId   // configurable in settings
  customActions:     user-defined {name, promptSource, binding}

Finding (ephemeral)
  editorId, quotedText, anchor {from,to} | null, critique,
  suggestedReplacement?, rationale?, status: open|applied|dismissed,
  thread: Message[]      // per-finding push-back conversation (session-scoped)

ReviewRun (ephemeral)
  target: note | selection, requestedBy: summon|action|panel,
  perEditor: pending|running|done|error, findings[], panelVerdict?

MarginComment (persisted, sidecar)
  id, filePath, anchor {quotedText, prefix, suffix},   // fuzzy re-anchoring
  instruction, status: submitted|running|done|dismissed,
  startedAt, elapsed, result?: Finding[]

Backend
  ApiBackend:  provider (anthropic|openai|openrouter|ollama|custom-openai-compatible),
               apiKey, baseUrl?, model
  CliBackend:  kind (claude-code|codex|custom), executablePath, args template,
               cwd (vault), timeout
```

Key invariants:

- An Editor's effective system prompt = voice profile (if injected) + persona prompt (textarea ⊕ resolved note contents, in declared order).
- Note refs are resolved at run time (fresh read), so editing [[My Voice Profile]] immediately affects every subsequent run — the vault is the config.
- Panels are visually distinguishable from Editors in every surface (rail, context menu, cards): distinct shape/badge (e.g. editors = solid dots, panels = ringed/stacked dots).
- A panel run is ONE run (M6, shipped): its members are ordinary editor streams on the same anchoring/finding/concurrency machinery, its charter augments every member's system prompt through `buildEditorPrompt`, and the run owns the aggregation step that follows them. Members keep their own identity — findings are never merged or re-attributed to the panel.

## 4b. Note-type awareness — **SHIPPED 2026-07-30** (see the "Binding rules + note types" §0 row)

- **Binding rules** (settings): ordered rules `[match: folder / tag / frontmatter property / note type → default reviewer]`. The Review button "does the right thing" per note type; rules only pick WHO reviews, never auto-run anything. **Shipped decisions**: a matching `disabled` rule wins from ANY list position (a kill switch must not be shadowed by an unrelated assignment ordered above it); among `assign` rules the FIRST match in list order wins (the tab exposes explicit ordering, so list order IS the user's priority statement — specificity sorting was considered and rejected as making the visible order a lie); assignments never accumulate; no match → every enabled review-capable editor (the pre-rules behavior). Assignments supply the DEFAULT pool only: an explicit ask-an-editor / bound action and a daemon re-dispatch both win. Cold metadata cache fails CLOSED for kill switches, OPEN for assignments. Default action bindings per rule were NOT shipped — `bindingRuleSchema.defaultTarget` is one target, and action bindings are already per-verb in the Actions tab; adding a second, rule-scoped binding layer needs its own design.
- **Optional OSK integration**: shipped as `ui/osk-note-types.ts` (`isStarterKitAvailable` / `listNoteTypes`, defensive normalization, graceful `[]` degradation) plus the pure `domain/rules/note-type.ts`. `recognizeNoteType` was deliberately NOT ported: it is asynchronous and every consumer is a synchronous decision (menu build, command gate, rail render), so recognition happens locally by matching the registry's own tag/folder/filename-regex mappings (`formula` mappings are not evaluable and recognize nothing). A note's type identity is a SET of identifiers — the registry's canonical names (`Personal Notes` → `personal-notes`) plus the `type/<x>` tag convention (`type/personal` → `personal`) — and a rule matches any of them, because OSK names types differently from how it tags them. **Never mandatory** — without the plugin, rules match the tag spelling.
- **Kill switch per scope**: shipped. A matching `disabled` rule deactivates the rail, the context-menu items, the palette commands (review, actions, triage, bulk, filter), the side-panel binding and the finding card, and refuses every dispatch path (review, transform/generate, push-back thread, daemon, CLI) with a typed `rule-disabled` status distinct from the privacy exclusion of §5.7. `Cancel review` stays available on purpose — stopping an in-flight request is never the wrong outcome. An existing run stays BOUND while hidden so its anchors keep remapping and removing the rule restores a coherent run.

## 4c. Adversarial review outcomes (2026-07-29)

An adversarial review by codex (`gpt-5.6-sol`, xhigh) — full findings in `documentation/reviews/2026-07-29-plan-review-codex-gpt-5.6-sol.md` — surfaced 8 blockers / 24 majors. Adopted into this plan:

1. **Operation-shaped contract, not one `review()`**: versioned discriminated operations — `Review`, `TransformSelection`, `InsertAt` (continuation), `RefineProposal`, `ThreadTurn`, `AggregatePanel` — each with its own result schema, stable IDs, and a typed `PanelResult` (member verdicts, top fixes, dissent, provenance).
2. **Live Preview / Source mode = equivalent semantics, not identical rendering** (widgets hide syntax in LP). Mode detection via `editorLivePreviewField`; a markdown fixture corpus exercises marks across links/embeds/headings/callouts in both modes.
3. **Snapshot & concurrency model**: every run pins a content snapshot + hash; anchors map through every CM6 `ChangeDesc`; a proposal whose range was edited goes **stale** (never silently fuzzy-relocated); Accept verifies the precondition text still matches.
4. **Transport reality**: Obsidian's `requestUrl` doesn't stream; renderer `fetch` has CORS caveats per provider. M0 includes a transport spike per provider; **buffered structured output is the first-class baseline**, streaming is a per-provider progressive enhancement ("first insight in seconds" is an aspiration, not a requirement).
5. **CLI backends are a security boundary**: spawn without shell, content via stdin, isolated working directory (not the vault), allowlisted env, read-only sandbox flags, tools/session persistence off by default, process-tree kill on cancel/unload, versioned protocol conformance tests, separate consent flow for tool/research mode. CLI support is **opt-in and late** in the milestone order.
6. **`isDesktopOnly: true` from M0** (manifest currently says false — fix immediately).
7. **Provider adapters are explicit profiles** (native OpenAI, narrowly-defined OpenAI-compatible, Anthropic, Ollama, Azure OpenAI deployment-based), each with capability negotiation (streaming? JSON schema? usage?), normalized errors, `AbortSignal`, health check, and exactly-once terminal events.
8. **Context assembly is budgeted and previewable**: token/byte budget across the whole context graph (not note counts), dedup/cycle handling, and a per-run "what will be sent" preview; exclusions are enforced before context resolution (excluded notes can't ride in via wikilinks).
9. **Settings integrity**: stable UUIDs, schema versions + migrations, referential integrity on delete (impact dialogs), import validation/remapping, idempotent starter-pack seeding.
10. **Wikilink references via a dedicated note-ref control** (`AbstractInputSuggest`-based) or a purpose-built contenteditable prompt editor — not naive textarea autocomplete.
11. **Key storage disclosure**: keys live in `data.json` (may sync) — documented prominently; keys/prompts redacted from logs; warning on non-loopback HTTP endpoints.
12. **Canonical docs** (`Architecture.md`, `Domain Model.md`, `Configuration.md`, `Business Rules.md`) get the locked invariants at M0, before feature code.

## 4d. Context budget & "what will be sent" — **SHIPPED 2026-07-30** (see the "Context budget + send preview" §0 row)

- **Priority order — decided and documented, `services/context/context-budget.ts`.** Sent in this order, and the LAST sections lose first: (1) the **system prompt** (voice-profile text + persona text + memory text as one string) — never truncated, because half a persona silently produces a different editor; (2) the **reviewed note** — never truncated, because findings must quote the submitted snapshot verbatim and anchors resolve against it (Business Rules #3/#4), and because the assembler _cannot_ trim it anyway (the note travels in the operation payload, `operation.text`, not in the system prompt — the budget only ACCOUNTS for it); (3) **attachments** in assembly order = `prompt-ref` → `wikilink-ref` → `followed-link` → `linked-note`. Since the first two are never truncated, the ordered truncatable region IS the attachment list, so ONE list serves as both send order and priority order — deliberate coupling, because two orders would let the preview list notes in an order that does not explain which of them got dropped.
- **Voice-profile notes outrank persona notes** (they come first inside `prompt-ref`, mirroring the system prompt's text order). Considered and rejected: hoisting persona refs above the voice profile because "the persona IS the editor" — the persona's TEXT is never truncated at all, and the voice profile is one small globally-configured note set while persona refs are per-editor and typically more numerous, so dropping the persona's fifth reference before the single voice-profile note is the better failure. A user who disagrees turns `injectVoiceProfile` off for that editor.
- **Over budget is reported, not absorbed.** When the never-truncated sections alone exceed `contextBudgetChars`, every attachment is dropped and `overBudgetChars` says by how much; the run still goes out (refusing to review a long note would be worse) and the preview states it plainly.
- **Truncated ≠ dropped.** An attachment allocated 0 characters is `dropped`, never `truncated`: "truncated" promises a prefix arrives. Every candidate is reported with its REAL source size even when dropped — that number is what tells the user whether to raise the budget or cut a reference. A note that cannot be read at all is not a section (the budget did not do that).
- **Editor-level linked notes were already shipped** and were verified rather than rebuilt: `includeLinkedNotes` / `maxLinkedNotes` attach the reviewed note's outgoing links AND embeds (`ObsidianVaultReader.getOutgoingLinks` merges `links` + `embeds`), depth 1, markdown only, deduped against everything already included, capped, budget-aware, and excluded notes never inlined — with excluded notes not consuming the cap. Attachment blocks now additionally carry a `role` (the attachment reason) so the model can tell persona reference material from the reviewed note's own subject matter.
- **One prompt-build entry point.** `buildEditorPrompt` (review-service) is the only path from settings + note to a system prompt; reviews, transforms/generations, push-back threads and the preview all call it. This is the enabling decision for the preview being trustworthy at all: a spec captures the system prompt of a REAL dispatch (via a capturing `fetch`) and asserts the preview's string equals it byte for byte, so drift is a test failure rather than a discovered lie.
- **Two preview entry points, deliberately different.** `preview-context` (palette: "Preview what will be sent") reads the LIVE editor buffer, because unsaved edits are what a run would send. The **Preview** button in the editor dialog reads the vault but previews the UNSAVED draft persona, because previewing the editor you are currently writing is the only useful answer there. Both refuse with their own message on a **privacy exclusion** (Behavior tab) — "nothing would be sent" is the most important thing a preview can say, so the palette command stays available on an excluded note precisely to say it. A **rule kill switch** is the other case and behaves the other way: §4b removes the plugin's commands from a note a rule switches off, so the palette command disappears; the `rule-disabled` status still exists for the settings dialog and for a rule that lands while the modal is open. (Corrected 2026-07-30 in the stage E fix pass: the command had been gated on both, which hid the exclusion refusal it was supposed to deliver.) The modal sends nothing: its only injected dependency is `previewEditorContext`.
- **The preview covers bound actions, not just plain reviews** (added 2026-07-30, stage E fix pass). The palette modal has an **Action** picker; picking one resolves the instruction through the same `resolveBoundActionVerb` the dispatch uses. This matters because a CUSTOM action inlines its referenced vault notes into its instruction (`CUSTOM_INSTRUCTION_MAX_CHARS` = 10 000), which is vault content leaving the vault that no surface showed. A review-class instruction is appended to the system prompt exactly as `startReview` appends it; a transform/generate instruction rides the operation payload and is reported separately with its size, because "it is in the prompt" and "it is in the request" are different answers. The editor dialog's Preview button has no action picker: its question is about the draft persona.
- **Not shipped from M5 by this slice**: settings import/export (shipped next, §4e) and the first-use setup wizard (§4f).

## 4e. Custom actions & settings transfer — **SHIPPED 2026-07-30** (see the "Custom actions + import/export" §0 row)

- **A custom action states what it does.** `actionBindingSchema.customVerbClass` (`transform` | `generate` | `review`) is **required, with no default**: custom actions used to be transform-class by construction, so an action whose instruction said "check this for factual errors" replaced the checked text with the answer. Defaulting to `transform` would keep that bug behind a nicer UI. An action without a class resolves to the typed `custom-class-missing` reason and is offered nowhere until the user picks one — the same treatment a blank name or instruction already got.
- **One verb interface for both kinds.** `resolveActionVerb(actionId, custom?)` in the verb registry is the only way a dispatch path turns an action id into the verb it runs: registry entry for a built-in id, the custom action's own `ActionVerb` (label + class + vault-resolved instruction) otherwise. Built-ins win over a supplied custom verb, so a persisted binding can never redefine what "Humanize" does. In the controller, verb resolution moved AHEAD of the class split, which is what lets a review-class custom action carry its vault-resolved instruction into `startReview` — and lets a review-class custom action bind a panel, since the panel rule was already written in terms of the class.
- **`followLinks` on a custom instruction is live**, with the same semantics as context assembly (depth 1, embeds, `FOLLOWED_LINKS_CAP` per referenced note, exclusions decided before any read, each note once). One toggle, one meaning plugin-wide. The direct instruction text is serialized FIRST so the instruction cap (`CUSTOM_INSTRUCTION_MAX_CHARS`) can only ever cut reference material, never the directive.
- **Export strips API keys inside `exportSettings`** — the only function that produces an export — so no caller can forget (Business Rules #12). **Import clears them too**, even when the file carries one: a `data.json` copied from another vault must not silently bill its owner's account. Both dialogs say so on screen. The advanced `extraBodyJson` escape hatch travels as-is: it is functional request configuration, and blanking it would break the backend it configures.
- **Imports add, they never overwrite.** Every imported entity gets a fresh id and references INSIDE the import are remapped onto them, so importing a file twice yields two independent sets rather than a half-merge onto whatever shared an id. `resolveIdCollisions`' load-time semantics (keep-first, leave references alone) are deliberately NOT reused as-is: there a duplicate id means "the same entity twice from a sync merge", here it means "a different entity from another vault", and keeping the reference would point an imported panel at the user's own editor. A reference to something outside the import survives only when that id exists in the current settings — which is what makes re-importing panels into the vault their members still live in do the right thing — and is otherwise cleared with a typed adjustment.
- **Unselected sections are absent from the file, not empty**, and `voiceProfile` is the one value that REPLACES (a single value cannot be "added"); the confirmation states it in capitals before anything is written.
- **Section caps are enforced while planning**, not at save time: the facade rejects a schema-invalid update wholesale, so an oversized import would fail with "could not save" instead of naming the entities that did not fit. The cap constants mirror the schema and a spec pins the mirror against `pluginSettingsSchema` itself.
- **The format marker is not required on import.** A plugin `data.json` from another vault is a legitimate source and carries none; an object with no recognizable section is the error case, because that is when the user picked the wrong file.
- Not shipped by that slice: the first-use **setup wizard** — shipped next, §4f, which closes M5.

## 4f. Setup wizard — **SHIPPED 2026-07-30** (see the "Setup wizard" §0 row)

- **Six steps**: welcome (what the plugin does + the plain-text-key disclosure) → add a backend (provider picker including OpenRouter, key, model, **Test connection**) → pick which editors are on → optional voice-profile note picker (with Follow links explained) → summon vs daemon with the cost stated → done + pointers. Triggered on the first load where `onboarded === false`, plus the `Run setup wizard` command and a Behavior → Setup button.
- **Nothing is written until Finish.** The wizard accumulates a draft; `applySetupWizard` is one pure whole-value function applied in one settings update. Cancelling at any step (Escape, the X, Cancel) therefore cannot leave a half-configured plugin, because there is no sequence of partial writes to interrupt. Commit-per-step was considered and rejected: it requires every step to be independently valid, and the backend step is not — a label with no model is a backend that exists and cannot run.
- **A step may be left EMPTY, but not HALF-FILLED.** Every step except the backend is optional by nature (defaults exist for all of them). The backend step is optional too — it can be skipped outright — but refuses to advance with an incomplete backend, because that is the one input where "some of it" is worse than none. The refusal uses the SAME rule as the Backends tab (`domain/settings/backend-validation.ts`, now the single source: per-kind requirements plus their wording, with the tab's dialog calling it instead of its own copy), **plus one wizard-only requirement: a model** (added 2026-07-30, stage E fix pass). The Backends tab legitimately saves a model-less backend, since a model can be set per editor; the wizard cannot, because it wires what it adds as the global default — "a label with no model is a backend that exists and cannot run" is this step's own stated example, and it was not enforced. **What the wizard persists is the VALIDATED backend**, not the draft: validation used to normalize a copy and discard it, so a pasted base URL kept its whitespace and every request from it threw `ERR_INVALID_URL`.
- **`onboarded` is set on ANY exit**, not only on Finish — which is exactly what the schema field already said it meant ("completed or was skipped"). A wizard the user dismissed must not reappear on every launch, and re-running it is a command and a settings button away. Re-runs are edits, not resets: the draft seeds from current settings, and the wizard never edits an EXISTING backend (that is the Backends tab's job; silently rewriting a configured backend from a first-run flow would be a surprise).
- **A wizard-added backend becomes the global default when there is none.** Editors inherit their backend, so a wizard that added one without wiring it as the default would hand back a configured backend that nothing uses — the exact "it says it is set up but nothing runs" state the wizard exists to prevent. An existing default is never repointed.
- **Test connection is ONE cheap real request through `createApiEditorExecutor`** — the same path a review takes — over a one-sentence probe document. A hand-rolled ping (models-list GET, bare completion) would answer a question nobody asked: it goes green for an endpoint that authenticates fine and then fails every review because the model cannot produce the structured output the operation contract requires. **Three outcomes, not two**: `ok`, `failed` (credentials / network / timeout / configuration), and `unusable` — reachable endpoint, answer the plugin cannot parse — which is a different fix (a stronger model) from a connection problem and gets its own status and its own color. The probe timeout is **60 s**, deliberately not `behavior.requestTimeoutSeconds` (10 minutes by default): a connection test that can hang for ten minutes is not a test. The timeout message says a slow local model may still work for real runs with a higher request timeout, so Ollama-on-CPU is never mistaken for broken.
- **The last step tells the truth about whether anything will run**, asked of `hasReviewCapableEditor` over the settings the wizard is ABOUT to write — the predicate that knows about disabled backends, dangling references, missing models and per-editor overrides. Re-deriving an approximation inside the wizard is how a "you are all set" screen starts lying.
- **Keyboard**: every control is focusable and the forward button is focused on each step, so the defaults path is Enter, Enter, Enter through native button activation. A modal-wide Enter binding was rejected: with the CTA focused it fires alongside the button's own activation and advances two steps at once.
- Field edits deliberately skip the re-render (text inputs must keep focus), which is why **Test connection reads the backend at click time** and keys its in-flight guard on the backend **id** rather than object identity: a captured draft would test the model as it read one keystroke ago, and identity comparison would discard the verdict on every keystroke typed during the request.

## 5. Architecture

### 5.1 Backend abstraction

```
interface ReviewBackend {
  review(req: ReviewRequest): AsyncIterable<BackendEvent>
  // BackendEvent: finding | progress | token | done | error
}
```

- **ApiBackend** (v1 core): direct `fetch` to provider APIs, streaming. Structured output via JSON schema / tool-call where supported; strict Zod validation of findings. **Findings are parsed incrementally from the stream** and emitted as soon as each is complete (streaming-first UX). Provider adapters: Anthropic, OpenAI-compatible (custom base URL — covers OpenAI, OpenRouter, Groq, LM Studio…), Azure AI Foundry (deployment name + api-version + auth header), Ollama.
- **CliBackend** (v1, desktop-only): spawn `claude` / `codex` in headless mode (`claude -p --output-format stream-json`) with the note (or selection) as context, cwd = vault root. Parse streamed JSON into BackendEvents. This is what powers "Get research"-grade actions (web, tools) and long async reviews.
- Per-editor backend override; global default backend in settings.
- All requests carry: system prompt (assembled per §4), the note content (raw markdown), optional selection range, an extensible `context[]` (resolved `[[wikilink]]` refs from prompts/inputs, opt-in linked notes per the editor's contextPolicy), and the structured-findings output contract.
- **Nothing runs on note open.** Every backend call is triggered by an explicit user action (Review, an action verb, a push-back, a comment submission).

### 5.2 Finding contract (what every backend must return)

```json
{
  "findings": [{
    "quote": "exact text from the document",
    "critique": "what's wrong / observation",
    "suggestion": "replacement text (optional)",
    "rationale": "one-liner shown with the diff (optional)",
    "severity": "info | suggestion | warning",
    "confidence": 0-1
  }],
  "verdict": "publish | needs-work | kill",   // panels / gate editors only
  "summary": "note-level remarks (optional)"
}
```

### 5.3 Anchoring pipeline (highest technical risk — build first)

1. Exact string match of `quote` in raw markdown.
2. Fallback: whitespace/typography-normalized match (smart quotes, NBSP, newlines).
3. Fallback: fuzzy match (e.g. bitap/levenshtein window, threshold ~0.9) using `prefix`/`suffix` hints.
4. Unmatched → finding still shown in the run's list view (no highlight), never lost.
5. Anchors live as CM6 decorations → positions remap automatically on edits; a finding whose span is edited by the user is marked stale (dimmed).

### 5.4 Editor integration (CM6)

- `ViewPlugin` + `StateField` for highlight decorations (per-editor tint from persona color).
- Gutter/rail: custom DOM in the editor margin (left, like Juri) showing editor dots + panel badges, Summon/Cancel, spinners, count badges.
- Review cards: floating widget (CM6 tooltip API or absolutely-positioned overlay) anchored to spans.
- Inline diffs: widget decorations replacing the span while in preview state; Accept applies a transaction, Reject restores.
- Full parity in Live Preview and Source mode (north star #5): all decorations, cards, and diffs must render and behave identically in both — anchoring against raw markdown makes this natural since Source mode shows exactly what we anchor to. Reading view out of scope for interaction (maybe read-only highlights later).

### 5.5 Async margin comments

- Sidecar store: `.obsidian/plugins/ai-editor/comments.json` (or plugin data dir) keyed by file path; quote+prefix+suffix anchors, fuzzily re-matched on file open (Hypothesis-style).
- Background runs via CliBackend (long-lived) or ApiBackend; live elapsed timer in the card ("Reviewing 0:27"); results survive note switches and Obsidian restarts.
- Right-margin comment column when panel width allows; collapses to icons + side panel on narrow editors.

### 5.6 Side panel (workspace leaf)

One `ItemView` ("AI Editor: Review") showing, per active note:

- current run status per editor/panel, findings list (jump-to-span on click),
- panel scorecards: per-member verdict, aggregated recommendation, top-3 fixes, dissent,
- unanchored findings and note-level summaries,
- history of margin comments for the note.

### 5.7 Settings design (UX matters here too)

Tabs/sections:

1. **Backends** — **1-n provider instances** (add multiple, label them): Anthropic / OpenAI-compatible / Azure AI Foundry / Ollama, each with keys, base URL/deployment, default model, test button; CLI agents (path detection, health check). First-use **setup wizard** (detect Ollama + `claude`/`codex` on PATH, test connection, seed starter editors, sample-text tour).
2. **Editors** — gallery of persona cards (color, name, enabled); create/edit modal with: name, color, prompt textarea, **note-ref picker (0-n notes, ordered, with fuzzy note search)**, backend/model override, capability toggles. Import/export JSON.
3. **Panels** — compose from existing editors (multi-select, 1-n), charter (textarea + note refs), aggregation mode. Distinct visual identity.
4. **Actions** — table of built-in verbs (rephrase, summarize, critique, say more, find evidence, identify assumptions, simplify, continue) each with a binding dropdown (editor or panel); add custom actions (name + prompt source + binding); hotkey hints (actual hotkeys via Obsidian's hotkey system).
5. **Voice & Style** — global voice profile: textarea + note refs (e.g. [[My Voice Profile]]); per-editor injection opt-out.
6. **Behavior** — size-warning threshold, concurrency, max tokens per run, **exclusions** (folders, tags, `ai_editor: false` frontmatter flag; strip-frontmatter toggle), hotkey pointers for the keyboard triage commands. No telemetry.

All plugin text inputs (persona prompts, charters, push-back, refine, custom actions) support **`[[wikilink]]` autocomplete**; referenced notes are resolved and attached as context at run time.

### 5.8 Starter pack (shipped editors; all fully editable)

Drawn from Maggie's epistemic roles + the user's OSK skills/agents:

1. **Concision Editor** — cut flab, merge redundancy (Juri's demo).
2. **Devil's Advocate** — attack weak arguments, find holes (OSK Hater/Skeptic).
3. **Fact Checker** — flag uncited claims, dated stats, verify when backend has research capability (osk-knowledge-fact-check).
4. **Flow & Structure Editor** — transitions, ordering, scannability (OSK Editor).
5. **Humanizer** — AI-tell detection: metronomic rhythm, banned vocabulary, hedge-stacking (osk-writing-humanizer + osk-writing-rigor).
6. **Beginner Reader** — jargon, assumed context, accessibility (OSK Beginner).

Starter panel: **Pre-publish Review** = Devil's Advocate + Flow & Structure + Beginner Reader + Humanizer → scorecard with publish / needs-work / kill verdicts (mirrors osk-panel-publish).

## 6. Milestones (what, not when; each lands green: tsc + lint + test + build; reordered per the adversarial review)

- **M0 — Contracts & spikes**: locked invariants written into `documentation/` canonical docs; operation contract (discriminated ops + JSON Schemas + Zod, stable IDs, versioning); anchoring pipeline (snapshot hash, occurrence disambiguation, `ChangeDesc` mapping, stale detection) with property/fuzz tests; **three de-risking spikes**: (a) transport per provider (streaming vs buffered, CORS), (b) CM6 decorations + rail + tooltip in Live Preview AND Source mode against a markdown fixture corpus, (c) CLI headless protocol probe. `isDesktopOnly: true`.
- **M1 — Provider & settings vertical slice**: 1-n provider instances (Anthropic, OpenAI, OpenAI-compatible, Azure OpenAI, Ollama) with capability negotiation, health checks, normalized errors, `AbortSignal`; tabbed settings shell; minimal editor entity + starter pack seeding.
- **M2 — Buffered single-editor review**: Review/Cancel from the rail, span highlights, side panel findings list (also the narrow-layout fallback), whole-note + selection scope, size warning + confirm, exclusions + per-type kill switch.
- **M3 — Streaming & anchors under edit**: per-provider streaming decoders where verified, live badges, cancellation races, stale-marking while the user types, precondition-checked apply.
- **M4 — Cards, diffs & keyboard triage**: floating review cards (single active tooltip, collision-aware), non-destructive inline diff (source stays visible; CM6-undo-integrated Accept), per-finding threads, full keyboard triage state machine, bulk operations (per-editor accept/dismiss-all, accept-all-non-conflicting as one undoable transaction, severity filter), status-bar finding-count item, adaptive layout via `ResizeObserver`.
- **M5 — Editors/actions/context CRUD**: persona gallery (colors, prompt textarea + dedicated note-ref control), voice profile section, action→editor/panel bindings, custom actions, note-type binding rules + optional OSK auto-discovery adapter (SHIPPED, §4b), `[[link]]`-in-inputs context with budget + "what will be sent" preview (SHIPPED, §4d), custom actions (SHIPPED, §4e), import/export with validation (SHIPPED, §4e), setup wizard (SHIPPED, §4f). **M5 is feature-complete.**
- **M6 — Panels**: panel CRUD (1-n members, charter, per-panel aggregation backend), parallel member runs + typed `PanelResult` scorecard (verdicts, top fixes, dissent, partial-failure policy), editors-vs-panels visual distinction, "Generate more" / `InsertAt` continuation affordance.
    - **Slice 1 SHIPPED 2026-07-30** (see the "Panel runs (stage F sl. 1)" §0 row): parallel member runs, charter → every member, the aggregation step's lifecycle (scheduling, cancellation, retry re-derivation) and the **partial-failure policy** — the panel completes with the members that succeeded, failures are named and individually retryable, aggregation runs when at least one member succeeded and states which members are missing; nothing is ever silently dropped.
    - **Slice 2 SHIPPED 2026-07-30** (see the "Panel scorecard (stage F sl. 2)" §0 row): the typed `PanelResult` scorecard — per-member verdict + one-line rationale, an overall recommendation with its own rationale, ranked top fixes that point back at the member finding they came from, and dissent kept as structure rather than prose; the aggregation input compacted and budget-fitted round-robin with every omission counted; the scorecard rendered at the top of a panel run in the side panel (surviving every aggregation failure) and the panel shown on the rail as ONE ringed entity owning its members (Business Rules #11).
    - **Slice 3 SHIPPED 2026-07-30** (see the "Panel polish (stage F sl. 3)" §0 row): the editors-vs-panels distinction given ONE author (`ui/entity-label.ts`) and applied to every surface where both kinds can appear — rail, dropdowns, scorecard, side-panel member sections, context menu and palette commands, always in the accessible name and never by shape alone; and the "Generate more" continuation affordance — a `done` editor is asked for ADDITIONAL findings, keeps the ones it made, is told what it already reported, and a failed extra round leaves the editor `done` so Retry cannot destroy the findings the user is triaging. Panels settings-tab CRUD was verified as already shipped rather than rebuilt.
    - Remaining for M6: CLI shaping of a panel run.
- **M7 — CLI backends (opt-in)**: Claude Code + Codex adapters behind the security boundary (stdin content, isolated cwd, allowlisted env, read-only sandbox, process-tree kill, protocol conformance tests), separate consent for tool/research mode. **SHIPPED** across three slices (see the `CLI boundary`, `CLI adapters` and `CLI settings + consent` §0 rows): the boundary was built first and provider-agnostic so no adapter could be wired around it; the adapters were built next and still unreachable; the settings, the two consents, the health check and the dispatch widening landed last, together, so a CLI backend could not become reachable before there was a surface that asked for consent. Remaining open items are live checks against real binaries, listed in `documentation/history/2026-07-30.md`.
- **M8 — Durable margin comments**: sidecar repository (schema version, migrations, rename handling, corruption recovery), interrupted-job semantics on restart (Retry, never fake resumption), background runs with live timers, margin column UI.
- **M9 — Polish & release**: theming via Obsidian CSS vars, reduced-motion + ARIA + non-color persona indicators, performance passes (large notes, many findings), docs CONTENT (README, docs/ user guide — the docs site is already published and redeploys automatically on push), community-review checklist sweep. **Post-implementation adversarial codex review (same model/effort) before release.** **Sébastien handles personally (2026-07-29 — NOT agent scope, do not plan or attempt): pushing, marketplace submission, docs-site publishing, screenshots, video.**
    - **Support CTAs (fleet-wide convention, 2026-07-29)**: every surface a user can see must carry the same calls to action — newsletter, YouTube channel, Knowii community, GitHub Sponsors, Buy me a coffee. All 21 other plugin repos were migrated on 2026-07-29; **this repo was deliberately skipped to avoid interrupting in-flight work**, so it must catch up before release. `obsidian-plugin-template` is the canonical source for exact wording and code — copy from it rather than re-inventing (see its `TEMPLATE_USAGE.md` §4.2, "Support CTAs"). Seven surfaces:
        1. **`src/app/ui/support-links.ts`** — shared module holding `KNOWII_COMMUNITY_URL`, `GITHUB_SPONSORS_URL`, `BUY_ME_A_COFFEE_URL`, `YOUTUBE_CHANNEL_URL`, `NEWSLETTER_URL` plus `renderSupportSection(containerEl, renderBadge?)`. Single source of truth: everything else imports from it. Copy the file verbatim from the template.
        2. **`src/app/ui/whats-new-modal.ts`** — drop its local URL consts and import them from `./support-links` (the file is byte-identical across the fleet; keep it that way).
        3. **Settings tab** — the support section delegates to `renderSupportSection(containerEl, (el) => this.renderBuyMeACoffeeBadge(el))`. The badge asset stays plugin-local and is injected as a callback. Settings get opened repeatedly, unlike the one-shot what's-new dialog, so this is the highest-traffic in-app surface.
        4. **`manifest.json` → `fundingUrl`** — an **object** of labelled URLs (`"Join Knowii"`, `"GitHub Sponsors"`, `"Buy Me a Coffee"`), not a single string. Obsidian renders each entry in the community plugin browser and in the installed-plugin entry; it is the only CTA surface Obsidian itself provides.
        5. **`.github/FUNDING.yml`** — `github: [dsebastien]`, `buy_me_a_coffee: dsebastien`, and `custom:` with the Knowii and store URLs, driving GitHub's Sponsor button and dropdown.
        6. **`.github/release-footer.md` + `release.yml`** — the "Extract changelog for release notes" step appends `printf '\n---\n\n'` then `cat .github/release-footer.md` into `$GITHUB_OUTPUT`, so every GitHub release body carries the CTAs. Note the footer file must **not** start with `---` (Prettier mangles it as frontmatter); the separator is emitted by the workflow instead.
        7. **`README.md` + `docs/README.md` + `docs/_config.yml`** — both markdown files end with a `## News & support` section preceded by a `<!-- support-cta -->` marker (marker to next `##` = generated block, regenerable fleet-wide), and carry a `<!-- other-plugins:start -->` / `<!-- other-plugins:end -->` cross-promotion table generated from every plugin's `manifest.json` (name + description, current repo excluded). `docs/_config.yml` sets `footer_content` so just-the-docs renders the links at the bottom of **every** docs page. When this plugin ships, regenerate the other-plugins table in all repos so it appears in the fleet too.
    - **Value-moment support prompt (planned, tracked in the vault)**: a one-time, dismissible notice after N successful operations, leading with a concrete usage number before the ask. To be built in the template first and rolled out; if it lands before this plugin ships, adopt it here rather than writing a bespoke one.

## 7. Risks & mitigations

| Risk                                                                                       | Mitigation                                                                                                                                       |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Quote anchoring fails on markdown syntax mismatch (LLM quotes rendered text vs raw source) | Always send raw markdown to the model; instruct quoting verbatim from input; 3-stage matcher + graceful degradation; heavy test corpus           |
| CM6 overlay complexity (cards/diffs fighting Live Preview widgets)                         | Prototype the diff widget early (M1); prefer CM6-native decorations over DOM hacks                                                               |
| CLI backend fragility (paths, versions, auth)                                              | Health-check button, clear error surfaces, API backend always available as fallback                                                              |
| Cost runaway (N editors × long notes)                                                      | Scope control (selection/section), per-run token caps, count badge = findings only after completion, explicit Summon (never auto-run by default) |
| Findings spam / annoying UX                                                                | Severity filter, per-editor max findings, "fade on dismiss" everywhere, ignorable-by-design                                                      |
| Obsidian review guidelines (no `innerHTML`, sentence-case UI, no "Obsidian" in name/desc)  | Template conventions + review-lint before submission                                                                                             |

## 7b. Later: knowledge-integration features (post-v1, tracked as GitHub issues)

Beyond reviewing prose, editors should help integrate the note into the vault's knowledge graph:

1. **Tag review** — an editor reviews/improves the note's tags: suggests existing vault tags that fit (never inventing new ones without flagging), flags redundant/missing ones. Respects vault tag conventions.
2. **Related-notes discovery** — find vault notes relevant to the current note and propose them as wikilinks, targeted at a specific section (e.g. `## Related`); accept/reject per suggestion, like findings.
3. **References management** — add/update a full references list at the end of the note:
    - **Internal**: every vault note mentioned/linked in the body, consolidated.
    - **External**: URLs the user added manually + **every external source an LLM used as input/reference while editing/writing** (source-citation tracking: backends must report the sources behind their suggestions so citations are effortless and honest).
4. Prerequisite plumbing: the operation contract's evidence entries (review finding #27) double as the citation source for external references.

Additional post-v1 features (each a GitHub issue):

5. **Per-editor learning loop** — optional per editor: distill accept/reject patterns + push-back arguments into a memory store (user chooses per editor: plugin settings OR a dedicated vault note) that is injected into future runs. Transparent and editable — you can read exactly what an editor "learned".
6. **Interview-first drafting** — an action that runs a structured one-question-at-a-time interview (angle, audience, unique take) in the card/side panel before drafting; answers feed the generation context. Mirrors the Ghostwriter's "interview first, write later" rule; builds on the per-finding thread machinery.
7. **Community packs** — in-plugin pack browser with a rich explanatory UI (what each editor/pack is, what it's for). Packs are contributed via PRs to this repo under a `community/` parent folder (dedicated subfolders per editor and per pack); the plugin loads the registry from there. Contribution guide in the docs.
8. **Publish gates** — settings to define gates (designate a panel as gate, optionally per note-type rule); a public plugin API other plugins can call ("latest gate verdict for file X", "run gate"); status-bar verdict badge. First consumer: obsidian-ghost-publish ("Style Guard failed" warning before publishing).
9. **Metadata review** — editors propose frontmatter improvements: first description/title/aliases (prose-ish, publishing value), later any property as a structured, schema-aware diff (OSK type definitions when available). Companion of the tag-review issue.
10. **Session summary** — on-demand command: distill a review session (per-editor accepted/rejected counts, key changes) into a formatted digest, copied to clipboard or appended to a chosen note (e.g. the daily note).
11. **AI Providers plugin integration** — optional, feature-detected backend source: when https://github.com/pfrankov/obsidian-ai-providers is installed, its centrally-configured providers appear as selectable backends (configure keys once across plugins). Never required. GitHub issue #10.

## 8. Open questions (park for later; don't block M0-M1)

### Interaction surfaces design (M3 prerequisite — DONE 2026-07-29, see `interaction-surfaces-design.md`)

> Research + design completed and adversarially critiqued; the authoritative spec now lives in `documentation/plans/interaction-surfaces-design.md`. Headlines: no submenus (not public API), no default hotkeys (community guideline), stable command ids from entity UUIDs, CLI GO via public `registerCliHandler` (typings bump to 1.12.3 required), batch review deferred post-M4. The original scoping notes are kept below.

Design pass over the three ways users trigger actions, before wiring the M3 action surface:

1. **Context menus** — (a) editor context menu on a selection inside the note (the Juri/Epi surface: bound actions + "ask an editor" freeform); (b) file-explorer context menus on a note, folder, or multi-selection (a "collection"): run an editor/panel over a set of notes (batch review). Scope carefully: batch runs multiply cost — reuse the size-warning/confirm machinery per note.
2. **Command palette surface** — full command inventory: review note, review selection, each bound action as its own command (hotkey-assignable), cancel run, open panel, next/prev/accept/reject/dismiss finding triage commands, bulk operations.
3. **Obsidian CLI extensions** — register plugin-specific CLI handlers so reviews/actions can be driven from the terminal, following the pattern in obsidian-note-toolbar (https://github.com/chrisgurney/obsidian-note-toolbar/blob/master/src/Cli/CliHandlers.ts). NEEDS RESEARCH: how that plugin registers handlers with the Obsidian CLI, the handler contract, and what surface makes sense (e.g. `obsidian ai-editor:review file=... editors=...`, machine-readable findings output for scripting/agents). Not researched yet — do the research when this step starts.

### Action vocabulary expansion (planned step, post-M3)

The built-in verb list (rephrase, summarize, critique, say-more, find-evidence, identify-assumptions, simplify, humanize, continue) is a starting set, not a ceiling. A dedicated pass will expand it, candidates drawn from the OSK skill catalog and Juri/Maggie affordances: fact-check, add-examples, add-analogies, strengthen-hook, tighten-structure, extract-outline, translate, change-tone, make-scannable, title-suggestions, counter-argue, steelman, devils-advocate, explain-simpler (ELI5), add-transitions, remove-jargon. Selection criteria: each verb must be meaningfully distinct as a one-shot operation, benefit from a specific instruction prompt (vs just picking a different editor), and map cleanly onto the operation contract (transform-selection or review). Default bindings for new verbs ship unbound; the settings Actions tab lists them automatically from the schema.

- Diff granularity: word-level vs sentence-level rendering of red/green inline diff (leaning word-level, like Juri).
- Post-run usage display: show actual token usage per run (providers return it) — nice trust-builder, not required.
- Whether "Get research" (web) is API-backend-gated or CLI-only in practice.
- Publish gates: a "Style Guard" mode that blocks a publish pipeline (integration with obsidian-ghost-publish?) — later.
- Backlinks (not just outgoing links) as opt-in context.

## 9. References

- Juri Strumpflohner's Quill demos: x.com/juristr statuses 2074494746484236459, 2077036970895872368, 2079297727364464700
- Maggie Appleton, Language Model Sketchbook: https://maggieappleton.com/lm-sketchbook
- Vault: [[AI Assistant Architecture]], DeveloPassion Ghostwriter SOUL, osk-panel-publish, osk-writing-humanizer, osk-writing-rigor, developassion-style-police, user-voice-profile
- Obsidian plugin guidelines: https://docs.obsidian.md/Plugins/Releasing/Plugin+guidelines
