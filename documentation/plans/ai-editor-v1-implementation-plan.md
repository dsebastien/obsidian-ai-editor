# AI Editor — v1 Implementation Plan

> Status: DRAFT — everything below is directional. The requirements were gathered as brainstorming; each feature is a candidate, not a contract. The one non-negotiable: **awesome UI/UX**. When a trade-off arises between feature count and interaction quality, interaction quality wins.

## 0. Execution status (update after EVERY step — agents switch mid-stream)

| Milestone                     | Status                                              | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M0 contracts & spikes         | **DONE (code)** / spikes partially open             | Canonical docs filled; operation contract (`src/app/domain/operations/contract.ts`); anchoring engine (`src/app/domain/anchoring/`); snapshots+hash; SSE decoder (`src/app/services/backends/transport/sse.ts`); `isDesktopOnly: true`. OPEN: CM6 dual-mode decoration spike + CLI protocol probe — need a live Obsidian runtime (manual).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| M1 provider & settings slice  | **MOSTLY DONE (code)**                              | Landed: settings schema (Zod, `schemaVersion`, migrations scaffold, per-element salvage load + dropped-paths report, referential integrity) in `src/app/domain/settings/`; starter pack seeding; provider request builders + response/stream parsers (Anthropic, OpenAI/OpenAI-compatible, Azure OpenAI, Ollama — pure, spec-covered) in `src/app/services/backends/providers/`; tabbed settings shell + entity modals + facade/bootstrap (validate-before-`saveData`) in `src/app/settings/`. OPEN: setup wizard not started; note-ref suggest control (vault note picker for prompt fields) not built; settings UI unverified in a live Obsidian runtime.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| M2 buffered review            | **CODE-COMPLETE — pending LIVE VAULT VERIFICATION** | End-to-end review flow wired: `src/app/services/review-service.ts` (exclusion refusal → size guard → editor/backend resolution with typed skip report → per-editor `assembleContext` → `createApiEditorExecutor` (adapter buildRequest → fetch → parseBufferedResponse, buffered baseline) → `RunController.startRun`); `src/app/ui/review-controller.ts` (per-view glue: rail mount into `contentEl`, decoration dispatch via `setFindingsEffect`, CM6 update-listener → `applyTextChanges` (canonical view per file — every doc change forwarded exactly once, incl. programmatic edits), status bar, size-confirm modal, `ObsidianVaultReader`, single `editor.cm` accessor); `src/app/ui/side-panel.ts` (`ItemView` 'ai-editor-review': per-editor sections, findings with click-to-reveal, Not-anchored group, skip notices, live subscribe); real `Review current note` + `Open review panel` commands; styles (rail, color-mix finding highlights, stale = dashed+dimmed, panel). **FIXED (2026-07-29 adversarial review, round 2)**: (a) edit forwarding no longer keyed on view focus — one canonical view per file forwards every doc-changing transaction (search-replace-all/programmatic/menu-undo edits now remap anchors; Accept's edit flows through the same path, direct-apply in `acceptFinding` removed); (b) snapshot gap closed — `startReview` re-snapshots via `refreshSnapshot` synchronously right before `RunController.startRun`, and the controller binds `glue.run` via synchronous `refreshAll()` in the `started` branch; (c) dead `transport/executor.ts` deleted — its native `AbortSignal.any/timeout` composition and `reader.cancel()` teardown ported into `api-editor-backend.ts`, exactly ONE `TransportError` remains; (d) `styles.src.css` no longer ships Tailwind preflight or `:root` theme variables (`theme(inline reference)` imports), and the dead `my-plugin-whats-new-*` selectors were renamed to the runtime `ai-editor-whats-new-*` prefix (drift-guarded in `scripts/build.spec.ts`). **INTEGRATED (2026-07-29 integration pass)**: (a) review card wired end-to-end — `findingCardExtension(reviewController.findingLookup())` registered in `plugin.ts`; Accept routes through `FindingStore.accept` (precondition, BR #3) then dispatches replacement + `removeFindingsEffect` as one undoable transaction; the controller applies the accept edit to the domain store directly and its update listener skips card-originated transactions (recognized by `removeFindingsEffect`) to avoid double-apply; Dismiss → `FindingStore.dismiss`; re-projection excludes terminal statuses so dismissed/accepted highlights never reappear; `RunController.findRunWithFinding` resolves finding id → run. (b) `review-service` now builds executors via `backends/api-editor-backend.ts` `createApiEditorExecutor` (streaming for Anthropic/OpenAI/openai-compatible SSE, buffered for Azure/Ollama, whole-operation timeout `API_REVIEW_TIMEOUT_MS` = 300 s, status-only error messages per BR #12) — the local buffered duplicate was deleted. (c) The robust fail-closed `ObsidianVaultReader` (`src/app/ui/obsidian-vault-reader.ts`, subpath stripping, markdown-only, tag tolerance, null-on-cold-cache) replaced the inline duplicate in `review-controller.ts`. **REMAINING SEAMS**: (1) binding rules don't filter editors yet (marked SEAM M6 in review-service); (2) CLI backends skipped with typed reason (SEAM M7); (3) ~~`maxConcurrentRequests` not enforced~~ ENFORCED 2026-07-29 (commit `29bf5e5`): plugin-wide FIFO abort-aware semaphore (`orchestration/semaphore.ts`) owned by `RunController`, gating executor stream starts across ALL runs; limit read from settings at each admission, queued editors stay 'pending', cancel ejects queued waiters with no leaked permits; (4) `requestUrl` buffered fallback for CORS-blocking providers not implemented; (5) push-back thread input is a disabled placeholder; (6) **MANUAL LIVE-VAULT VERIFICATION REQUIRED — exact checklist**: (a) plugin loads in a live vault without console errors; (b) `Review current note` command runs on a test note; (c) highlights render in Live Preview AND Source mode; (d) rail shows status/counts; (e) side panel lists findings + jump-to-span works; (f) card opens on highlight click; (g) Accept applies the edit AND undoes cleanly (one undo step); (h) Dismiss clears the highlight; (i) cancel works mid-run; (j) exclusions block the review with a refusal; (k) size warning fires on an oversized note; plus multi-pane edge cases: two panes on one file (type in each, replace-all without focus, Accept from either pane, popout window).                                                                                                                                                                                                                                                                                                                    |
| Live vault verification       | **STARTED — first e2e run SUCCEEDED**               | 4/4 findings anchored via local Ollama (qwen3:4b). Fixed live: ollama `think:false`, advisory-hint clamping. VERIFIED by Sébastien: card opens on highlight click, Accept/Dismiss work. Fixed since: card widened (560px + air), panel binding falls back to live active view ("No review yet" bug), registerView double-load race healed, humanize base action added. Still open: status-bar count not updating, Live Preview vs Source mode visual comparison, real API providers unconfigured. See history 2026-07-29 "Live vault verification". Smoke note: `AI Editor Smoke Test.md` at vault root (delete after card verification).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Interaction surfaces design   | **DONE (design)**                                   | Research + adversarially-critiqued design in `documentation/plans/interaction-surfaces-design.md` (2026-07-29): editor/file context menus, full command inventory (no default hotkeys), CLI = GO (`registerCliHandler` is public `@since 1.12.2`; bump `obsidian` typings 1.12.0 → 1.12.3), batch review deferred post-M4. 3 open questions for Sébastien at the end of that doc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Interaction surfaces slice 1  | **DONE (code)**                                     | Foundations landed 2026-07-29 (commits `3cd53aa`, `64afd8e`): `obsidian` typings 1.12.0 → 1.12.3 (`registerCliHandler`/`CliData`/`CliFlags`/`CliHandler` now typed; tsc clean); shared reviewability predicate `src/app/services/reviewability.ts` (`isReviewable` = not excluded + ≥1 enabled review-capable editor whose backend resolves; re-exports `isExcluded`, reuses `resolveApiBackend` — no duplicated rules; `ReviewController.canReview` now routes through it so the Review command hides when nothing could dispatch); settings facade mutation observer (`SettingsFacade.subscribe(listener)` → unsubscribe, notified after every successful update/persist incl. load-time repairs, never on rejected updates; `createSettingsNotifier` shared by fallback facade, host-wrapping path, and `AIEditorPlugin.persistSettings`). All spec-covered (validate green, 555 tests). Next slices: selection scope plumbing (§5.2), menus, command inventory, CLI handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Interaction surfaces slice 2  | **DONE (code)**                                     | Selection scope plumbing landed 2026-07-29 (commit `c3508d6`): `StartReviewInput.requestedSelection` (selection-capture contract, design §1) — caller captures the range synchronously against the capture-time snapshot; at run start the service re-validates via the new pure `isRequestedSelectionValid` (non-empty ordered range, in bounds against the FRESH snapshot, text hash unchanged since capture). Valid → run snapshot selection-scoped on exactly that range (overrides any live selection the refreshed snapshot picked up); invalid → whole-note fallback with `selectionFallback: true` on the `started` result. `ReviewController.startReview(view, confirmedLargeNote, requestedSelection?)` passes it through, preserves it across the size-confirmation round-trip, and Notices "Selection changed — reviewing the whole note" on fallback. Legacy whole-note / snapshot-carried-selection path untouched (spec-pinned). 10 new specs (validate green, 565 tests; build green). Next: editor context menu (design slice 3), file menu, command inventory, CLI handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Interaction surfaces slice 3  | **DONE (code)**                                     | Context menus landed 2026-07-29 (commit `9a5ed7c`, design §1/§2): pure `menu-model.ts` (spec-covered) decides which items appear; `editor-menu.ts` + `file-menu.ts` are thin Obsidian `Menu` wiring registered from `plugin.ts` via `registerEvent`. Editor menu (v1 review-class only — action verbs wait for M3 transform ops): "Review selection" (`message-circle`) when selection non-empty + editable markdown view + `isReviewable`; selection offsets captured synchronously in the click callback → `requestedSelection` (slice 2 re-validation + whole-note fallback Notice already wired). File menu (single `.md` `TFile`, reviewable, `setSection('ai-editor')`): "Review note" (opens file if needed via new `ReviewController.reviewFile` → same `startReview` path as the command) + "Open review panel" (`bot`). `files-menu` intentionally NOT registered (batch deferred post-M4, no placeholders). `ReviewController` gained public `canReviewPath(path)` and private `openMarkdownView` (shared with reveal-from-panel). 9 new specs (validate green, 574 tests; build green). Next: command inventory (design slice 5), CLI handler (slice 6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Interaction surfaces slice 4  | **DONE (code)**                                     | Static command inventory landed 2026-07-29 (commit `b6827c6`, design §3): `review-selection` (editorCheckCallback: editable view + non-empty selection + `isReviewable`; dispatches via new shared `ReviewController.startSelectionReview` — the editor context menu now uses the same entry), `cancel-run` ("Cancel review"; unsettled run for the active file via new `ReviewController.getActiveRun`), `next-finding`/`prev-finding` (cursor-relative stepping through the active run's revealable findings — open/preview + live anchor — ordered by anchor position, wrap-around, revealed via the side-panel reveal path). Pure logic spec-covered: `src/app/commands/command-gates.ts` (availability predicates) + `src/app/commands/finding-navigation.ts` (`navigableFindings`/`stepFinding`, 20 new specs total); registrar renamed `review-current-note.ts` → `review-commands.ts` (six commands, still thin glue). No default hotkeys. DEFERRED (no non-functional commands): `accept-finding`/`dismiss-finding` (needs card triage state), `filter-severity` (needs filter state), dynamic `action-<actionId>` (transform ops not wired), `accept-all-/dismiss-all-<editorId>` (bulk ops, M4) — and with them the settings-driven dynamic registration diffing (observer from slice 1 stands ready). Validate green (595 tests), build green. Next: CLI handler (design slice 6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Interaction surfaces slice 5  | **DONE (code)**                                     | CLI handler landed 2026-07-29 (commit `fa58134`, design §4 / design slice 6): pure `handleReviewCli` core (`src/app/services/cli/review-cli.ts`, Obsidian-free, 36 specs) — CliData arg parsing (`file` required; `editors` comma-separated ids-or-names, case-insensitive on names, strict on unknown tokens; `format` json (default)\|text; `confirm-large` boolean), file resolution + read via injected deps, dispatch through the injected runner (production = the shared `startReview` pipeline, so exclusions/size guard/editor-backend resolution are the SAME refusals as the command), `await run.settled`, then one buffered output document: `{ ok, file, findings[{ id, editor, severity, quote, critique, suggestion, anchor: {from,to,state}\|null }], skips, summaryByEditor, error }` (JSON) or one line per finding (text). Typed error codes: `file-not-found`, `excluded`, `needs-confirmation` (oversized without `confirm-large`), `no-editors`, `backend-error`, `timeout` (all failures timeouts). Per-editor post-run failures join `skips` (`backend-error`/`timeout`/`cancelled`); messages reuse the run's redaction seam, unexpected pipeline failures are status-only (BR #12). Glue `src/app/cli/register-review-cli.ts` (ObsidianVaultReader + `getFileByPath`/`getFirstLinkpathDest` resolution, markdown only, shared `RunController` so CLI runs surface in rail/panel); registered in `plugin.ts` under `Platform.isDesktop && requireApiVersion('1.12.2')` — `minAppVersion` untouched. `scope` flag NOT registered (selection unsupported from CLI v1 — reserved, no non-functional flags). Validate green (631 tests), build green. LIVE-VAULT CHECK PENDING: actual `obsidian ai-editor:review --file …` invocation end-to-end. Interaction surfaces design slices 1-6 now all landed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Interaction surfaces fix pass | **DONE (code)**                                     | Adversarial review of slices 2-5 fixed 2026-07-29 (commit `3e0f05f`), 6 confirmed findings: (1) **CLI run on an open note no longer bypasses the ReviewController** — `registerReviewCli` now takes the controller; when the note is open, the snapshot comes from the LIVE editor buffer (`ReviewController.cliRunBinding`, whole-note scoped) and the started run is bound to the view glue synchronously (`bindCliRun` → `refreshAll` + skip report), so edits typed during a CLI run remap anchors and rail/panel/highlights update immediately; (2) **file-menu "Review note" is explicitly whole-note** — `reviewFile` passes a new `SnapshotScope` (`'whole-note'`) through `startReview`/`snapshotView`, so a live selection never silently selection-scopes it (selection scope is exclusively the `requestedSelection` contract); (3) **selection re-validation is real across the size-confirmation round trip** — `requestedSelection` now carries `capturedHash` (hash of the capture-time text, filled from the first snapshot, carried unchanged through the confirm retry); the service validates against it instead of the post-modal snapshot's own hash (spec pins the round-trip fallback). Minors: `selectEditors` rejects explicitly requested DISABLED editors (typed `no-editors` error; silent-drop was data loss for scripts); `registerReviewCli` wrapped in try/catch in `onload` (`registerCliHandler` throws on duplicate registration in the double-load race → degrade to no CLI surface, don't fail the load); CLI runs on notes with no open view are discarded after output shaping (`releaseRun` seam, run-identity guarded — no unbounded accumulation from batch usage); design doc §1/§4 amended to the shipped contracts (hidden-not-disabled items, `scope` flag dropped for v1, `anchor` nullable, `capturedHash`). Validate green (649 tests), build green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CLI cancel + status           | **DONE (code)**                                     | Design §6.3 decision implemented 2026-07-29 (commits `5b48768`, `e34e5f7`, `7be8452`): CLI v1 surface is review + cancel + status. Shared machinery extracted first (`src/app/services/cli/cli-shared.ts`: flag parsing, the CLI finding shape, `shapeFindings`/`shapeSummaryByEditor`, `formatFindingLine`; `src/app/cli/resolve-note-path.ts`: one wikilink-tolerant `--file` resolver; `src/app/services/cli/spec-fixtures.ts`: shared `FakeRunHandle`) so the subcommands stay in lockstep by construction. `ai-editor:cancel` (`cancel-cli.ts` + spec): `file` required; cancels the unsettled run via `RunHandle.cancelRun` — NEVER discards (deps expose no discard capability; findings stay inspectable via status/UI); `{ ok, file, cancelled }` with `reason: 'no-run' \| 'already-settled'` when nothing to cancel; JSON only. `ai-editor:status` (`status-cli.ts` + spec): `file` + `format` (json\|text); read-only poll surface — `run: null` when none, else `{ settled, editors: [{id,name,status,error}], findings (review shape), summaryByEditor }`; text = status headline + review-format finding lines; lockstep pinned by spec (status findings `toEqual` review findings on the same run). Glue `src/app/cli/register-run-cli.ts`; `plugin.ts` registers all three subcommands under the same `Platform.isDesktop && requireApiVersion('1.12.2')` gate, each individually try/catch-guarded (double-load race degrades per subcommand). Design doc §4 rewritten for the three-subcommand surface; §6 open questions replaced with decisions (Ask-an-editor = modal + threads; batch review deferred until cost estimation; CLI = review+cancel+status). Validate green (673 tests), build green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| M3 incremental stale-marking  | **DONE (code)**                                     | The documented seam in `finding-decorations.ts` wired 2026-07-29 (commit `5fd0080`): `ReviewController.handleEditorUpdate` snapshots the `FindingStore`'s stale ids before forwarding an edit batch, diffs after (pure `src/app/ui/editor/stale-diff.ts`, spec-covered without CM6), and dispatches `markStaleEffect` for exactly the transitioned findings on a microtask (CM6 forbids dispatching inside an update listener) — a finding now dims the moment an edit stales it instead of waiting for the deferred rebuild. `dispatchDecorations`' full `setFindingsEffect` rebuild unchanged (eventual-consistency backstop).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Ask an editor modal           | **DONE (code)**                                     | Freeform "Ask an editor" (design §6 decision 1, modal entry point; card threads stay M4) landed 2026-07-29 (commit `991fbcd`): `src/app/ui/ask-editor-modal.ts` (thin Modal glue: editor picker — dropdown, or static text when exactly one; instruction textarea, Enter = newline, Ctrl/Cmd+Enter = submit; Cancel + Ask CTA disabled while blank) over pure `ask-editor-model.ts` (`normalizeInstruction`/`canSubmitAsk`/`defaultAskEditor`, spec-covered). Entered from the editor context menu ("Ask an editor…", same gate as Review selection — `menu-model.ts` now returns both items) and the `ask-editor` command (editorCheckCallback, no default hotkey). Dispatch seam: `StartReviewInput.instruction` (`RunInstruction {editorId, text}`) narrows the run to the chosen editor (un-asked editors are not skips) and augments ONLY that editor's fully composed system prompt via pure `augmentSystemPrompt` (framed `<user-instruction>` block appended last; settings untouched; findings still validate through the unchanged Zod operation contract). Selection contract: `ReviewController.openAskEditorModal` captures range + capture-time hash synchronously in the invoking callback (the modal gap makes the dispatch-time hash unusable), rides through `startReview`'s size-confirm round trip. `reviewCapableEditors(settings)` extracted in `reviewability.ts` so the picker can only offer editors `startReview` accepts. 27 new tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| M3 fix pass (ask + gate)      | **DONE (code)**                                     | Adversarial review of the M3 increment fixed 2026-07-29 (commits `88456a7`, `5a0b1c3`), 3 confirmed code findings + 2 doc corrections: (1) **concurrency permit released at terminal state** — the plugin-wide `maxConcurrentRequests` permit was only freed when the editor stream's iterator ended; after `cancelRun` the consume loop keeps draining discarded events, so an executor ignoring its AbortSignal held its permit indefinitely (N such streams = plugin-wide deadlock until reload). The release now lives on the editor state and fires in `terminate`/`cancelRun` (idempotent; the `finally` stays as backstop); 2 new specs (cancel reclaims from an abort-ignoring executor; a terminal result frees the permit mid-drain). (2) **Ask-editor submit refuses a switched note** — `openAskEditorModal` captures `view.file.path` at open; a submit whose view now shows a different note (programmatic openFile, workspace restore) gets a Notice instead of silently reviewing the wrong note. (3) **Ask dispatch uses `whole-note` scope** — a collapsed capture previously dispatched scope `auto`, letting a selection made while the modal was open silently narrow the run against design §1's exclusive capture contract; `whole-note` is safe unconditionally (a valid captured selection overrides explicitly). Docs: history spec count corrected (13 new, not 27), design §3 table gained the shipped `ask-editor` command row, §1 item 3 annotated as shipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Slow & thinking models        | **DONE (code) — all three pieces (2026-07-29)**     | From Sébastien's live testing (Ollama timeout on a laptop). Three pieces, one increment: (1) **Per-editor retry** — DONE (commits `4810a5b` core, `3343271` UI): editors ending in `error`/`cancelled` get a retry affordance in the rail chip AND the side-panel section header ('Retry <name>' aria-labels); both call `ReviewController.retryEditor(filePath, editorId)` → `RunHandle.retryEditor(editorId, freshText)`, which re-runs ONLY that editor inside the EXISTING run. Decisions baked in: a retry REPLACES that editor's previous findings (removed from the store — they came from a failed/partial attempt; other editors untouched); retried findings anchor against the CURRENT buffer text captured synchronously at retry time (per-editor anchor base: the run-level appliedChanges history became per-editor, recording stops at terminal and RESTARTS from the retry snapshot, so BR #3/#4 hold across retries); the retried request sends that fresh text to the backend (a selection scope only rides along while the text is still the snapshot text); fresh per-editor runId + per-retry AbortController make retry-after-cancel work (the shared run abort stays aborted; `cancelRun` lost its early-return so it also aborts in-flight retries) and stale still-draining streams harmless; the concurrency permit is acquired exactly like a first attempt; `isSettled()` is now DERIVED from live editor state so an active retry flips the run back to in-progress on every surface (rail Cancel/spinner, `Cancel review` gate, CLI status derives from states already) while the `settled` promise keeps first-settle semantics (CLI output await; documented). No open view → vault-read fallback (run-identity guarded). CLI has no retry surface (UI-only affordance). (2) **`Request timeout (seconds)`** — DONE (commit `28af75e`): `behavior.requestTimeoutSeconds` (int 30-3600, default 600 s — laptop-realistic; no compat shim) replaced the hardcoded `API_REVIEW_TIMEOUT_MS`; threaded as `timeoutMs` through `createEditorSpec` via `reviewTimeoutMs(behavior)`; CLI reviews share it (same `startReview` path); Behavior tab int field ('Request timeout (seconds)'); the timeout error message names the setting ("raise 'Request timeout' in settings"); specs pin schema default/bounds, s→ms conversion, the value flowing into the transport, and the message shape. (3) **Per-backend thinking setting** — DONE (commit `fd97786`; decision: per-backend is enough, NO global default): `apiBackendSchema` gained `thinking` ('off'\|'on', default 'off' everywhere — silent minutes-long reasoning reads as a hang), `thinkingBudgetTokens` (1024-32000, default 8192), `reasoningEffort` ('default'\|'minimal'\|'low'\|'medium'\|'high'), `extraBodyJson` (advanced, openai-compatible only). Ollama sends `think` from config, parser reads only `message.content` (spec: `message.thinking` ignored); Anthropic 'on' sends `thinking: {type:'enabled', budget_tokens}` with the budget ON TOP of the 8192 output budget (budget_tokens < max_tokens by construction) and `tool_choice` relaxed to `auto` (API rejects forced tool use while thinking) — parser + text-JSON fallback spec-covered with thinking/redacted_thinking blocks interleaved; OpenAI/Azure pass `reasoning_effort` through ('default' sends nothing); openai-compatible merges `extraBodyJson` (extras win, invalid JSON → invalid-config, modal validates at save); streaming accumulator treats `thinking_delta` as progress and drops thinking blocks at finalize. Backend modal shows only the controls matching the selected kind. **Fix pass (2026-07-29, commits `c34472f`, `df633f8`)** — adversarial review, 1 major + 4 minors, all confirmed + fixed: Anthropic 'on' now sends ADAPTIVE thinking (`{type:'adaptive'}` — manual `budget_tokens` is deprecated on Claude 4.6 and HTTP-400-rejected on 4.7+/5.x) with `tool_choice` KEPT forced (adaptive supports forced tool use) and `max_tokens` 32k (thinking spend counts against it); the legacy manual mode survives as a third `thinking` value `'budget'` (Claude ≤4.5 only, modal documents which mode applies) with the sum clamped to the legacy 32k output ceiling and `budget_tokens` clamped strictly below `max_tokens` (a 32k budget previously built `max_tokens` 40192 → HTTP 400 at validation); openai-compatible streaming counts `delta.reasoning_content`/`delta.reasoning` deltas as progress (DeepSeek/OpenRouter conventions — a thinking model enabled via `extraBodyJson` no longer reads as a hang); `ai-editor:review`'s `shapeRunOutput` reports mid-retry (pending/running) editors as `retrying` skips instead of silently dropping them (first-settle `settled` + UI retry race) and no longer fabricates a 'cancelled' message while a retry is in flight. Also DONE already (commit `298e02e`): verdict pills relabeled — 'All good' / 'Needs work' / 'Not ready' (wire tokens publish/needs-work/kill unchanged). |
| Daemon mode                   | **DONE (code) (2026-07-29)**                        | Sébastien: editors should either be "waiting for orders" (summon-only — DEFAULT, cost control) or in **daemon mode** (his name): watching file edits and refreshing their recommendations automatically. Decisions: `behavior.daemonMode: boolean` toggle (default `false`) + `behavior.daemonIdleSeconds` (int, sensible bounds, default 30) — after the user stops editing a reviewable note for N seconds AND the note changed since its last run, daemon mode re-dispatches the review (same editor set as the note's current run; all enabled review-capable editors if the note was never reviewed). Coalescing: never while a run/retry is in flight for that file (re-arm after settle); exclusions, size guard (daemon SKIPS oversized notes silently rather than modal-spamming), binding rules (once they land) and `maxConcurrentRequests` all apply; cancel/summon always available. Settings copy must state the cost implication plainly. Sébastien enables it in his vault; default stays off. **IMPLEMENTED (commits `a37da92` schema/settings, `9121540` scheduler core, `8a02ba5` glue+rail):** `behavior.daemonMode` (default false) + `behavior.daemonIdleSeconds` (int 5-600, default 30; Behavior tab → Daemon: toggle whose copy states the cost implication, idle field rendered only while on). Pure spec-covered scheduler `src/app/services/daemon/daemon-scheduler.ts` (21 specs — idle windows, per-file independence, coalescing incl. the fire-time run-in-flight refusal + re-arm-at-settle, changed-hash gate incl. undo-back-to-same, never-reviewed dispatch, silent oversized skip with one-log-per-file flag, settings flip mid-arm) behind thin glue `src/app/ui/daemon-controller.ts` (one `window.setTimeout` per file PATH — popouts/splits share a schedule; probes live facts at fire time; oversized skip = one log line, never modal/Notice). Edits arrive via the EXISTING canonical-view update listener in `ReviewController.handleEditorUpdate` (now also fires for files without a run; no second CM6 listener); run state via `refreshGlue` (`syncRunState` — summons, CLI, daemon runs, retries coalesce identically); config via the slice-1 settings observer (toggle/idle apply live, off clears all timers); timers cleared on dispose/unload, file close (last view only), vault delete/rename. Dispatch = `ReviewController.startDaemonReview`: shared `startReview` pipeline (exclusions, size guard, editor/backend resolution, concurrency gate all apply), whole-note scope, silent on every refusal, new `StartReviewInput.editorIds` re-dispatches the note's current run's editor set (null → all enabled review-capable editors), and new `abortWhen` guard (checked synchronously right before `startRun`, typed `aborted` status) so a daemon dispatch racing a user summon during context-assembly awaits NEVER cancels the user's run — the fire-time probe already refuses while a run/retry is unsettled (spec-pinned), the guard closes the async gap; summon/cancel/retry always win. Rail: tiny pulsing dot + tooltip while armed for the current note (`RailState.daemonArmed`, spec-covered model). BR #1 carve-out documented in Business Rules.md/README/docs/usage.md/review-service header. Binding rules still SEAM M6 (apply automatically once they land — same pipeline).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| v1 completion sweep           | **QUEUED — full remaining scope, quality-first**    | Sébastien (2026-07-29): implement EVERYTHING planned for v1 in one go; disregard implementation time, maximize quality. Staged: (B) daemon mode; (C) transform operations end-to-end (rephrase/summarize/humanize/continue + non-destructive inline diff, CM6-undo-integrated Accept, action dispatch from menus/commands + dynamic per-action commands); (D) M4 rest (keyboard triage state machine, bulk ops incl. accept-all-non-conflicting as ONE undo step, per-finding push-back threads, severity filter, adaptive layout); (E) M5 (binding rules filter — SEAM M6 —, note-type rules + OSK auto-discovery adapter, `[[link]]` context budget + "what will be sent" preview, custom actions, import/export, setup wizard); (F) M6 panels end-to-end (parallel member runs, typed `PanelResult` scorecard, editors-vs-panels visual distinction); (G) M7 CLI backends (Claude Code + Codex, security boundary, opt-in consent); (H) M8 durable margin comments (sidecar store — schema only, no migrations per no-users policy — rename handling, interrupted-job Retry, background runs, margin column UI); (I) M9 polish (theming vars, reduced-motion + ARIA, perf passes, support CTAs from template, README + docs/ content, community-checklist sweep) + full-repo adversarial review with fix pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| M3 (rest) + M4-M9             | superseded — see "v1 completion sweep"              | See milestone list §6. M3 remaining: per-provider streaming decoders where verified, live badges, cancellation races (precondition-checked apply already landed with M2's Accept path).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

Working conventions for any agent picking this up: read `documentation/history/` (latest day) + `AGENTS.md` first; definition of done = tsc+lint+test+build green + history updated + this table updated; commit per logical chunk (Conventional Commits, hooks never bypassed); `git push` is hook-blocked — push via `bash "$CLAUDE_JOB_DIR/tmp/push-ai-editor.sh"`-style script only when Sébastien asks. Adversarial codex review (`gpt-5.6-sol`, xhigh) after implementation phases (M9) — see `documentation/reviews/` for the plan review already done. **No-users policy (Sébastien, 2026-07-29): the plugin has no users yet — never spend effort on backwards compatibility, migrations, or behavior-preserving defaults; pick the BEST design/default outright and change schemas freely (schema-version machinery stays for post-release).**

### Known review debt — CLOSED 2026-07-29 (later session)

All 14 items below were fixed and committed on 2026-07-29 (commits `75dee05`, `c3499d2`, `f11180d`, `fa066a4`): #1 occurrence-aware dedupe key (+spec), #2 `discardRun` + `cancelAll` clears (+specs) + vault rename/delete hooks, #3 placeholder, #4 `resolveIdCollisions` (keep-first, global uniqueness, persisted repair + Notice, +specs), #5 stale-int fallback, #6 swatch aria labels, #7 obsolete (command functional since M2 wiring), #8 was already fixed (reader.cancel in finally), #9 appliedChanges stops growing after settle, #10 header comment corrected (markStaleEffect documented as M3 seam), #11 CORS named in the network-failure message (full `requestUrl` fallback still a seam), #12 skipsByFile follows rename/delete, #13 revealFinding timer re-validates the view, #14 card closes on window resize. Also fixed: status-bar count bug (shared active-file resolver with live-view fallback). Original findings kept below for context.

### Original findings (2026-07-29 adversarial code review — all fixed, see above)

1. `src/app/services/orchestration/run-controller.ts:265` (correctness) — finding dedupe key `JSON.stringify([quote, critique, suggestion])` omits `prefix`/`suffix`/`occurrence`, so two legitimate findings on different occurrences of the same repeated text (occurrence 0 vs 1 — exactly what the prompt instructs for repeated text) collapse into one; the second is silently dropped. Fix: include `raw.occurrence`, `raw.prefix`, `raw.suffix` in the key so only true stream-vs-result duplicates collapse.
2. `src/app/services/orchestration/run-controller.ts:349` (correctness) — `RunController.runs` never evicts: every reviewed file keeps its last `RunHandle` (snapshot text, `FindingStore`, listeners) forever; `cancelAll()` cancels but does not clear. Unbounded memory growth holding full document copies in long sessions. Fix: delete the map entry when a run is replaced-and-settled, or add a clear/dispose path (remove on file close and in `cancelAll`).
3. `src/app/settings/tabs/backend-modal.ts:12` (correctness) — OpenAI base-URL placeholder says `defaults to https://api.openai.com` but the adapter default (and required shape) is `https://api.openai.com/v1`; a pasted placeholder value yields requests to `https://api.openai.com/chat/completions` → 404 (the base URL replaces the default wholesale). Fix: placeholder → "Optional — defaults to https://api.openai.com/v1".
4. `src/app/domain/settings/settings-schema.ts:213` (security-privacy) — entity IDs have no uniqueness validation (`id: z.string().min(1)`, arrays unchecked) and `checkReferentialIntegrity` does not detect duplicates. data.json is syncable (BR #12); a sync-merge conflict producing two backends with the same id resolves by first-match everywhere (`settings.backends.find`), so an editor's backend ref can silently route note content + the wrong API key to a different endpoint than the UI displays. The `ids.ts` docstring promises collision detection/remap; nothing implements it. Fix: duplicate-id check (per array and across arrays) in `loadSettings` or `checkReferentialIntegrity`; on collision keep first, regenerate later ids (remap references), surface an integrity warning.
5. `src/app/settings/tabs/behavior-tab.ts:36` (obsidian-guidelines) — `renderIntField`'s NaN fallback uses the render-time `value` closure, not the last committed value; since numeric commits skip `refresh`, changing e.g. max concurrent requests 3→8 then later blurring the field empty commits the stale 3, silently reverting the user's change. Fix: track `let current = value` locally, use it as the `clampInt` fallback, update after each successful commit.
6. `src/app/settings/components.ts:119` (obsidian-guidelines) — color preset swatches get `aria-label: "Use var(--color-red)"`; screen readers announce the raw CSS token ("Use var dash dash color red"). Fix: store presets as `{ value, label }` pairs and set `aria-label` from the human label ("Use red").
7. `src/app/commands/review-current-note.ts:49` (obsidian-guidelines) — the `Review current note` command reports itself available but only shows a "not connected to a backend yet" Notice. Community-plugin review flags shipped non-functional placeholder commands. Fine as an M2 seam; **must not reach a submitted release**. Fix: gate registration (or `checkCallback` → false) until the run pipeline is wired, or block catalog submission on it.

Minor findings from the 2026-07-29 E2E wiring adversarial review (7 more, none block M3):

8. `src/app/services/backends/api-editor-backend.ts:245` (correctness) — `executeStream`'s `finally` only does `releaseLock()`; the body reader is cancelled solely via the abort listener. When the stream terminates abnormally WITHOUT an abort — an Anthropic in-stream `error` frame thrown from `accumulator.push` (line 315), or `ProviderError` from a malformed tool-input mid-loop — the HTTP response body is left locked-released and unconsumed, so the connection keeps downloading until the server closes it. `transport/executor.ts` (the dead twin) got this right with `await reader.cancel()` in `finally`. Fix: in the `finally` block, call `reader.cancel().catch(() => undefined)` before `releaseLock` (cancel on a done reader is a no-op).
9. `src/app/services/orchestration/run-controller.ts:186` (efficiency) — `applyTextChanges` appends a batch to `appliedChanges` on every forwarded transaction — i.e. every keystroke in a reviewed note — for the lifetime of the run, and the run lives until the plugin unloads (`RunController.runs` never evicts; each retained run also pins the full snapshot text). The history is only consumed by `ingestFinding`, which can no longer fire once every editor stream has settled, so a long editing session on a reviewed note accumulates thousands of dead `TextChange` batches per run, multiplied across every file reviewed in the session. Fix: skip the `appliedChanges.push` once `settledFlag` is true (`findings.applyTextChanges` must still run for staleness tracking), and consider evicting or compacting runs in `RunController` when a newer run replaces them or after settle.
10. `src/app/ui/editor/finding-decorations.ts:67` (dead-code) — `markStaleEffect` is defined and handled by the StateField but never dispatched anywhere in the codebase; the module header claims "the orchestrator dispatches markStaleEffect when a domain anchor goes stale", which is untrue — staleness only reaches decorations via the deferred full `setFindingsEffect` rebuild in `ReviewController.dispatchDecorations`. Dead effect + wrong documentation invites a future contributor to rely on a mechanism that does not run. Fix: either dispatch `markStaleEffect` from the refresh path for anchors that transitioned to stale (cheaper than full rebuild), or delete the effect and `applyStale()` and fix the header comment to describe the rebuild-based propagation.
11. `src/app/ui/review-controller.ts:236` (compatibility) — `fetchImpl: window.fetch.bind(window)` runs backend requests through the renderer's CORS-enforced fetch. Anthropic's API rejects browser-origin requests unless the `anthropic-dangerous-direct-browser-access` header is sent, and self-hosted Ollama/LM Studio endpoints fail preflight unless the user configures `OLLAMA_ORIGINS`/CORS. Obsidian's guideline is to use `requestUrl` to bypass CORS; the plugin is desktop-only so fetch works, but several supported backend shapes will fail with opaque network errors. Fix: if streaming justifies fetch, document the CORS constraint per backend and detect the failure (`TypeError` from fetch) to surface a targeted Notice ("This endpoint blocks browser requests — enable CORS / use a proxy"). For non-streaming backends, fall back to `requestUrl` which is CORS-exempt.
12. `src/app/ui/review-controller.ts:261` (leak) — `skipsByFile` (declared line 132) only ever gains entries — `this.skipsByFile.set(file.path, result.skips)` is never paired with a delete when a run is cleared, a file is deleted/renamed, or the plugin runs for hours across many notes. Beyond the slow leak, `getPanelBinding` (line 319) serves these stale skips for any later run on the same path even if that run's skip set should be empty-but-fresh (it is overwritten on start, but survives run cancellation/completion cleanup indefinitely). Fix: key the skip report on the run instead of the file (store it on/next to the `RunHandle` in `RunController`, cleared when the run is discarded), or delete the map entry in `refreshGlue`/`updatePanels` whenever `runController.getRun(path)` returns null. Also register a `vault.on('rename')` handler or re-key on rename if file-keyed storage stays.
13. `src/app/ui/review-controller.ts:692` (correctness) — the 1.5 s selection-collapse timer in `revealFinding` captures `editor` and calls `editor.getCursor()`/`setSelection()` when it fires, guarding only `this.disposed`. If the user closes the leaf (or the view switches file / enters reading mode) within that window, the callback operates on a detached editor whose CM instance has been destroyed, which can throw and also silently move the cursor if the same view now shows a different note. Fix: in the timer callback, re-validate the target before touching the editor: `const v = this.findMarkdownView(filePath); if (!v || v.editor !== editor || v.file?.path !== filePath) return;` (or look the glue up in `this.glues`) before the `posEquals` check.
14. `src/app/ui/editor/finding-card.ts:310` (ui-polish) — `openCard` closes the card on `scrollDOM` scroll, document mousedown, Escape, and doc changes, but never listens for window resize (or outer-container scroll). The card is `position: fixed` with coordinates computed once; resizing the window or a popout leaves it floating at stale coordinates, potentially fully outside the visible viewport with no way to see it (click-away still closes it, so it's recoverable, but it looks broken). Fix: in `openCard`, also register `win = doc.defaultView; win?.addEventListener('resize', this.onScroll)` and remove it in `closeCard` (reuse the existing close-on-scroll handler; alternatively re-run `positionCard()` on resize since the anchor rect is cached).

## 1. Vision

Bring AI editing/reviewing/QA **into** the Obsidian editor itself — not a chat sidebar bolted onto a note, but AI personas that live in the margins of the text, highlight what they care about, argue with you, and propose surgical edits you accept or reject inline.

The user writes; a configurable crew of **Editors** (AI personas) and **Panels** (groups of editors) review, critique, rewrite, fact-check, and coach — all grounded in the user's own vault (voice profile, style rules, personas documented as notes).

### Design north stars

1. **The text is the interface.** No modal chat. Findings anchor to spans in the note; actions happen where the text is (Maggie Appleton: "bring the LLM to the editing and thinking process, rather than exiting into a chat interface").
2. **Suggestions, never silent mutations.** Every AI change is a visible diff with Accept/Reject. The user stays the author.
3. **Ignorable by design.** Findings fade away when dismissed; daemons never block writing (Appleton's daemons principle).
4. **Vault as configuration.** Every prompt (editor persona, panel charter, voice profile) can be typed directly in a textarea OR sourced from 0-n vault notes. Documenting your assistant in your vault IS configuring the plugin.
5. **Full parity in Live Preview AND Source mode.** Both are CM6, so highlights, rails, cards, diffs, and margin comments must work identically in both. Source mode is a first-class citizen, not a degraded fallback — test every surface in both modes.
6. **Desktop-first.** Mobile support is explicitly not a priority.

## 2. Inspiration analysis

### 2.1 Juri Strumpflohner's "Quill" (X posts, analyzed frame-by-frame from demo videos)

Sources:

- https://x.com/juristr/status/2074494746484236459 (8-min demo, July 7 2026)
- https://x.com/juristr/status/2077036970895872368 (28-s dark-mode demo, July 14 2026)
- https://x.com/juristr/status/2079297727364464700 (screenshot of async review, July 20 2026)

Observed UX (all confirmed via frame extraction):

| Element                        | Behavior                                                                                                                                                                                                                                                                     |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Persona rail** (left gutter) | Colored dots, one per persona. Hover reveals name (e.g. "DEVIL'S ADVOCATE") + edit/delete icons. A `+` dot creates a new persona.                                                                                                                                            |
| **Summon button**              | Above the rail. Click → all personas review the doc. Button becomes "Cancel"; dots become spinners; when done, each dot shows a **count badge** (findings count, e.g. 8/4/8).                                                                                                |
| **Span highlights**            | Each persona highlights the text spans it has findings for (subtle tinted background).                                                                                                                                                                                       |
| **Review card**                | Click a highlight → floating card near the span: persona name ("Concision Editor"), critique text, quoted passage, buttons **Suggest / Apply / Dismiss**, plus a freeform input "Push back, ask for evidence…" to argue with the persona.                                    |
| **Inline diff**                | "Suggest" → in-place diff: red strikethrough deletions, green insertions, one-line rationale ("Dropped 'just' and 'a bunch of', replaced the vague clause…"), **Accept / Reject**, plus "Refine this suggestion…" input.                                                     |
| **Selection context menu**     | Select text → menu: Rephrase, Say more, Critique, Find evidence, Get research, Identify assumptions, + "Ask the daemon…" freeform. Keyboard shortcuts shown.                                                                                                                 |
| **Generate more**              | A ghost "+ Generate more" affordance between blocks → inline "Generating…" placeholder → continuation drafted in place.                                                                                                                                                      |
| **Async review (v2)**          | Select text + type an instruction → margin comment card showing the quote, the instruction, status **SUBMITTED**, then a live **"Reviewing 0:27"** timer while a background agent works. Result lands back in the card. Google-Docs-comments where the reviewer is an agent. |

### 2.2 Maggie Appleton — Language Model Sketchbook (https://maggieappleton.com/lm-sketchbook)

- **Daemons**: background characters with distinct epistemic roles (devil's advocate, synthesizer, evidence-fetcher, elaborator, encourager). Non-intrusive; suggestions can be ignored and fade.
- **Epi**: context-menu integration — localized, task-specific help (rephrase, critique, evidence, assumption-checking) inside the writing tool.
- Core thesis: chatbots are the wrong interface for writing support; embed affordances in the editor.

### 2.3 The user's OSK AI ecosystem (feature quarry)

The vault already contains a mature agent architecture ([[AI Assistant Architecture]]) whose concepts map 1:1 onto plugin features:

| Vault concept                                                                                                                                                | Plugin analog                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| **Agent** = SOUL.md (identity, voice, DO/DON'T, expertise, decision framework, boundaries)                                                                   | **Editor** persona whose prompt can point at vault notes (a SOUL note works as-is)          |
| **Panel** = N agents → individual verdicts + aggregated recommendation + top-3 fixes + dissent (e.g. osk-panel-publish: Editor, Beginner, Power User, Hater) | **Panel** entity: 1-n editors + aggregation step → scorecard view                           |
| `user-voice-profile` context loader (My Voice Profile, Content Strategy, Content Types)                                                                      | **Voice/style configuration** (textarea + 0-n note refs) injected into every editor run     |
| `osk-writing-humanizer` (AI-pattern detection, banned vocabulary, P0/P1/P2 severity, structure-is-the-#1-tell)                                               | Starter editor: **Humanizer** — flags AI-sounding passages, metronomic rhythm, banned words |
| `osk-writing-rigor` (hedge-stacking, uncited claims, magnitude language, weasel openers)                                                                     | Starter editor: **Rigor Auditor**                                                           |
| `developassion-style-police` / `style-guard` (banned words, emdash abuse, signature elements, pass/fail gate)                                                | Starter editor: **Style Enforcer**; a "pre-publish gate" panel                              |
| Agents: Hater, Skeptic, Beginner, Power User, Storyteller, Provocateur                                                                                       | Starter personas for the editor gallery                                                     |
| Scorecards with verdicts (publish / needs work / kill it)                                                                                                    | Panel result UI                                                                             |

## 3. Decisions locked so far (from the grilling session)

| Decision             | Choice                                                                                                                                                                                                                                                                  |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Audience             | **Public community plugin**, fully configurable; the user's own style rules become his persona configs                                                                                                                                                                  |
| Backends             | **Both**: direct LLM APIs (BYOK) **and** agent CLIs (Claude Code, Codex). Desktop-only                                                                                                                                                                                  |
| v1 surfaces          | **All four**: persona rail + summon, selection context menu, async margin comments, generate/continue                                                                                                                                                                   |
| Persistence          | **Ephemeral findings** (in-memory CM6 decorations, remapped as you type, gone on close — re-summon is cheap) + **sidecar store** for async comments (survive editor sessions, background agents outlive the note being open)                                            |
| Persona model        | Per-editor **model/backend override** (fallback to global default) + starter pack + JSON import/export                                                                                                                                                                  |
| Anchoring            | **Exact-quote matching** (structured findings quote the text; plugin locates quote in raw markdown, exact → normalized → fuzzy; unmatched findings degrade to a note-level list)                                                                                        |
| Naming               | Personas are **"Editors"**; groups are **"Panels"**. Plugin name: **AI Editor**                                                                                                                                                                                         |
| Panels               | **In v1** ("I want it all"): user composes panels from 1-n editors; aggregated scorecard (verdicts, top fixes, dissent)                                                                                                                                                 |
| Action bindings      | Every UI action (rephrase, summarize, critique…) is **mapped in settings to an editor or panel**                                                                                                                                                                        |
| Prompt sourcing      | Every prompt field = **textarea AND/OR 0-n vault note refs** ("configure directly as a prompt, or indirectly by documenting things in my vault")                                                                                                                        |
| Finding arrival      | **Stream as they land** — highlights pop in progressively, badges count up live; first insight in seconds                                                                                                                                                               |
| Run scope            | **Whole note by default; selection overrides** when text is selected. Size warning + confirm above a configurable word count                                                                                                                                            |
| Panel aggregation    | **LLM aggregation call** after members finish: charter prompt + member outputs → recommendation, top-3 fixes, dissent. Panel has its own provider/model for this call                                                                                                   |
| Extra context        | **Opt-in linked notes per editor** (1 hop, capped) + `[[wikilink]]` references typed in ANY prompt/input field (persona prompts, push-back, refine, custom actions) resolve and attach those notes as context. Wikilink autocomplete in plugin inputs                   |
| Providers            | 1-n configurable provider instances: **Anthropic, OpenAI + any OpenAI-compatible (custom base URL), Azure AI Foundry (first-class: deployment name, api-version, auth header), Ollama**. Per-editor AND per-panel provider/model selection                              |
| Layout               | **Adaptive Juri layout**: pane ≥ ~900px → left rail + floating cards + right margin comment column; narrow panes collapse to highlight-click → card + side-panel list                                                                                                   |
| Keyboard             | **Full keyboard triage**: hotkey-assignable commands for next/prev finding, accept, reject, dismiss, suggest, focus push-back. Review = merge-conflict-style rapid triage                                                                                               |
| Onboarding           | **Guided setup wizard** on first use: pick/detect backend (Ollama running? `claude` on PATH?), test connection, seed starter editors, 30-second tour on sample text                                                                                                     |
| Guardrails           | Size warning + confirm; **exclusions** (folders/tags/`ai_editor: false` frontmatter flag block review; strip-frontmatter option); **nothing runs automatically on note open — every AI action is user-initiated (sole opt-in exception: daemon mode, BR #1 carve-out)** |
| Conversations        | **Per-finding threads**: push-back → editor reply → counter → revised suggestion, collapsed history in the card. Session-scoped                                                                                                                                         |
| Editor modes         | **Full parity in Live Preview AND Source mode** (both CM6); Reading view interaction out of scope                                                                                                                                                                       |
| Note-type rules      | **Binding rules** (folder/tag/frontmatter → default editors/panel/bindings) + optional **OSK auto-discovery** (feature-detected, never mandatory) + per-scope **kill switch** (disable the plugin's UI entirely for chosen note types/folders)                          |
| Bulk triage          | **Accept/dismiss all per editor** (rail dot menu), **accept all non-conflicting** (one undoable transaction, precondition-checked), **severity filter** (info/suggestion/warning)                                                                                       |
| Ambient UI           | **Status-bar item** for the active note: last run's finding count + gate verdict badge; click → side panel. No heat strip in v1                                                                                                                                         |
| Settings UX          | **Tabbed settings** (Backends / Editors / Panels / Actions / Voice & Style / Rules / Behavior), well organized                                                                                                                                                          |
| Overlapping findings | **Stacked card with tabs**: one blended highlight; the card shows every editor's take on that span (own suggestion + thread each); only one suggestion in diff-preview at a time                                                                                        |
| Language             | **Match the note's language automatically** (critiques, suggestions, threads in the text's language) + global override ("always respond in X")                                                                                                                          |
| Comment routing      | Async margin comments run on a **configurable default editor**, with a persona chip / @-mention in the input to reroute per comment                                                                                                                                     |
| Review surface       | **Body only in v1** — frontmatter/metadata review (description, title, aliases, full properties) is post-v1 (GitHub issue)                                                                                                                                              |

## 4. Domain model

```
Editor
  id, name, color, icon?
  promptSource:      { text?: string; noteRefs: NotePath[] }   // concatenated at runtime
  backend:           BackendInstanceRef + model | 'inherit'    // provider instance or CLI agent
  contextPolicy:     { includeLinkedNotes: bool; maxLinkedNotes: number }  // opt-in, 1 hop, capped
  capabilities:      { review: bool; rewrite: bool; research: bool }  // what it may do
  enabled:           bool

Panel
  id, name, color/badge style (visually distinct from Editors everywhere)
  members:           EditorId[]           // 1-n
  charterSource:     { text?: string; noteRefs: NotePath[] }   // aggregation instructions
  aggregationBackend: BackendInstanceRef + model | 'inherit'   // powers the scorecard call
  aggregation:       'scorecard' | 'merged-findings'
  enabled:           bool

BackendInstance (1-n configured in settings)
  ApiBackend:  provider (anthropic | openai-compatible | azure-ai-foundry | ollama),
               label, apiKey?, baseUrl?, deployment?/apiVersion? (azure), defaultModel
  CliBackend:  kind (claude-code | codex | custom), label, executablePath,
               args template, cwd (vault), timeout

VoiceProfile (global, singleton in settings)
  source:            { text?: string; noteRefs: NotePath[] }   // e.g. [[My Voice Profile]]
  injectInto:        'all' | per-editor opt-out

Action  (built-in verbs surfaced in context menu / command palette)
  id: rephrase | summarize | critique | say-more | find-evidence |
      identify-assumptions | simplify | continue | custom…
  binding:           EditorId | PanelId   // configurable in settings
  customActions:     user-defined {name, promptSource, binding}

Finding (ephemeral)
  editorId, quotedText, anchor {from,to} | null, critique,
  suggestedReplacement?, rationale?, status: open|applied|dismissed,
  thread: Message[]      // per-finding push-back conversation (session-scoped)

ReviewRun (ephemeral)
  target: note | selection, requestedBy: summon|action|panel,
  perEditor: pending|running|done|error, findings[], panelVerdict?

MarginComment (persisted, sidecar)
  id, filePath, anchor {quotedText, prefix, suffix},   // fuzzy re-anchoring
  instruction, status: submitted|running|done|dismissed,
  startedAt, elapsed, result?: Finding[]

Backend
  ApiBackend:  provider (anthropic|openai|openrouter|ollama|custom-openai-compatible),
               apiKey, baseUrl?, model
  CliBackend:  kind (claude-code|codex|custom), executablePath, args template,
               cwd (vault), timeout
```

Key invariants:

- An Editor's effective system prompt = voice profile (if injected) + persona prompt (textarea ⊕ resolved note contents, in declared order).
- Note refs are resolved at run time (fresh read), so editing [[My Voice Profile]] immediately affects every subsequent run — the vault is the config.
- Panels are visually distinguishable from Editors in every surface (rail, context menu, cards): distinct shape/badge (e.g. editors = solid dots, panels = ringed/stacked dots).

## 4b. Note-type awareness

- **Binding rules** (settings): ordered rules `[match: folder / tag / frontmatter property → default editors/panel + default action bindings]`. The Review button "does the right thing" per note type; rules only pick WHO reviews, never auto-run anything.
- **Optional OSK integration**: when the Obsidian Starter Kit plugin is installed, note types are auto-discovered via a feature-detected adapter (port of `starter-kit.service.ts` from obsidian-kanban-action-planner: `isStarterKitAvailable` / `listNoteTypes` / `recognizeNoteType`, defensive normalization, graceful `null`/`[]` degradation). Rules can then target a recognized note type directly instead of hand-configured folders/tags. **Never mandatory** — everything works without OSK.
- **Kill switch per scope**: rules can also map a note type / folder / tag to **"disabled"** — the plugin's UI (rail, context menu, actions) fully deactivates for those notes (e.g. daily notes, private folders). Complements the privacy exclusions in §5.7.

## 4c. Adversarial review outcomes (2026-07-29)

An adversarial review by codex (`gpt-5.6-sol`, xhigh) — full findings in `documentation/reviews/2026-07-29-plan-review-codex-gpt-5.6-sol.md` — surfaced 8 blockers / 24 majors. Adopted into this plan:

1. **Operation-shaped contract, not one `review()`**: versioned discriminated operations — `Review`, `TransformSelection`, `InsertAt` (continuation), `RefineProposal`, `ThreadTurn`, `AggregatePanel` — each with its own result schema, stable IDs, and a typed `PanelResult` (member verdicts, top fixes, dissent, provenance).
2. **Live Preview / Source mode = equivalent semantics, not identical rendering** (widgets hide syntax in LP). Mode detection via `editorLivePreviewField`; a markdown fixture corpus exercises marks across links/embeds/headings/callouts in both modes.
3. **Snapshot & concurrency model**: every run pins a content snapshot + hash; anchors map through every CM6 `ChangeDesc`; a proposal whose range was edited goes **stale** (never silently fuzzy-relocated); Accept verifies the precondition text still matches.
4. **Transport reality**: Obsidian's `requestUrl` doesn't stream; renderer `fetch` has CORS caveats per provider. M0 includes a transport spike per provider; **buffered structured output is the first-class baseline**, streaming is a per-provider progressive enhancement ("first insight in seconds" is an aspiration, not a requirement).
5. **CLI backends are a security boundary**: spawn without shell, content via stdin, isolated working directory (not the vault), allowlisted env, read-only sandbox flags, tools/session persistence off by default, process-tree kill on cancel/unload, versioned protocol conformance tests, separate consent flow for tool/research mode. CLI support is **opt-in and late** in the milestone order.
6. **`isDesktopOnly: true` from M0** (manifest currently says false — fix immediately).
7. **Provider adapters are explicit profiles** (native OpenAI, narrowly-defined OpenAI-compatible, Anthropic, Ollama, Azure OpenAI deployment-based), each with capability negotiation (streaming? JSON schema? usage?), normalized errors, `AbortSignal`, health check, and exactly-once terminal events.
8. **Context assembly is budgeted and previewable**: token/byte budget across the whole context graph (not note counts), dedup/cycle handling, and a per-run "what will be sent" preview; exclusions are enforced before context resolution (excluded notes can't ride in via wikilinks).
9. **Settings integrity**: stable UUIDs, schema versions + migrations, referential integrity on delete (impact dialogs), import validation/remapping, idempotent starter-pack seeding.
10. **Wikilink references via a dedicated note-ref control** (`AbstractInputSuggest`-based) or a purpose-built contenteditable prompt editor — not naive textarea autocomplete.
11. **Key storage disclosure**: keys live in `data.json` (may sync) — documented prominently; keys/prompts redacted from logs; warning on non-loopback HTTP endpoints.
12. **Canonical docs** (`Architecture.md`, `Domain Model.md`, `Configuration.md`, `Business Rules.md`) get the locked invariants at M0, before feature code.

## 5. Architecture

### 5.1 Backend abstraction

```
interface ReviewBackend {
  review(req: ReviewRequest): AsyncIterable<BackendEvent>
  // BackendEvent: finding | progress | token | done | error
}
```

- **ApiBackend** (v1 core): direct `fetch` to provider APIs, streaming. Structured output via JSON schema / tool-call where supported; strict Zod validation of findings. **Findings are parsed incrementally from the stream** and emitted as soon as each is complete (streaming-first UX). Provider adapters: Anthropic, OpenAI-compatible (custom base URL — covers OpenAI, OpenRouter, Groq, LM Studio…), Azure AI Foundry (deployment name + api-version + auth header), Ollama.
- **CliBackend** (v1, desktop-only): spawn `claude` / `codex` in headless mode (`claude -p --output-format stream-json`) with the note (or selection) as context, cwd = vault root. Parse streamed JSON into BackendEvents. This is what powers "Get research"-grade actions (web, tools) and long async reviews.
- Per-editor backend override; global default backend in settings.
- All requests carry: system prompt (assembled per §4), the note content (raw markdown), optional selection range, an extensible `context[]` (resolved `[[wikilink]]` refs from prompts/inputs, opt-in linked notes per the editor's contextPolicy), and the structured-findings output contract.
- **Nothing runs on note open.** Every backend call is triggered by an explicit user action (Review, an action verb, a push-back, a comment submission).

### 5.2 Finding contract (what every backend must return)

```json
{
  "findings": [{
    "quote": "exact text from the document",
    "critique": "what's wrong / observation",
    "suggestion": "replacement text (optional)",
    "rationale": "one-liner shown with the diff (optional)",
    "severity": "info | suggestion | warning",
    "confidence": 0-1
  }],
  "verdict": "publish | needs-work | kill",   // panels / gate editors only
  "summary": "note-level remarks (optional)"
}
```

### 5.3 Anchoring pipeline (highest technical risk — build first)

1. Exact string match of `quote` in raw markdown.
2. Fallback: whitespace/typography-normalized match (smart quotes, NBSP, newlines).
3. Fallback: fuzzy match (e.g. bitap/levenshtein window, threshold ~0.9) using `prefix`/`suffix` hints.
4. Unmatched → finding still shown in the run's list view (no highlight), never lost.
5. Anchors live as CM6 decorations → positions remap automatically on edits; a finding whose span is edited by the user is marked stale (dimmed).

### 5.4 Editor integration (CM6)

- `ViewPlugin` + `StateField` for highlight decorations (per-editor tint from persona color).
- Gutter/rail: custom DOM in the editor margin (left, like Juri) showing editor dots + panel badges, Summon/Cancel, spinners, count badges.
- Review cards: floating widget (CM6 tooltip API or absolutely-positioned overlay) anchored to spans.
- Inline diffs: widget decorations replacing the span while in preview state; Accept applies a transaction, Reject restores.
- Full parity in Live Preview and Source mode (north star #5): all decorations, cards, and diffs must render and behave identically in both — anchoring against raw markdown makes this natural since Source mode shows exactly what we anchor to. Reading view out of scope for interaction (maybe read-only highlights later).

### 5.5 Async margin comments

- Sidecar store: `.obsidian/plugins/ai-editor/comments.json` (or plugin data dir) keyed by file path; quote+prefix+suffix anchors, fuzzily re-matched on file open (Hypothesis-style).
- Background runs via CliBackend (long-lived) or ApiBackend; live elapsed timer in the card ("Reviewing 0:27"); results survive note switches and Obsidian restarts.
- Right-margin comment column when panel width allows; collapses to icons + side panel on narrow editors.

### 5.6 Side panel (workspace leaf)

One `ItemView` ("AI Editor: Review") showing, per active note:

- current run status per editor/panel, findings list (jump-to-span on click),
- panel scorecards: per-member verdict, aggregated recommendation, top-3 fixes, dissent,
- unanchored findings and note-level summaries,
- history of margin comments for the note.

### 5.7 Settings design (UX matters here too)

Tabs/sections:

1. **Backends** — **1-n provider instances** (add multiple, label them): Anthropic / OpenAI-compatible / Azure AI Foundry / Ollama, each with keys, base URL/deployment, default model, test button; CLI agents (path detection, health check). First-use **setup wizard** (detect Ollama + `claude`/`codex` on PATH, test connection, seed starter editors, sample-text tour).
2. **Editors** — gallery of persona cards (color, name, enabled); create/edit modal with: name, color, prompt textarea, **note-ref picker (0-n notes, ordered, with fuzzy note search)**, backend/model override, capability toggles. Import/export JSON.
3. **Panels** — compose from existing editors (multi-select, 1-n), charter (textarea + note refs), aggregation mode. Distinct visual identity.
4. **Actions** — table of built-in verbs (rephrase, summarize, critique, say more, find evidence, identify assumptions, simplify, continue) each with a binding dropdown (editor or panel); add custom actions (name + prompt source + binding); hotkey hints (actual hotkeys via Obsidian's hotkey system).
5. **Voice & Style** — global voice profile: textarea + note refs (e.g. [[My Voice Profile]]); per-editor injection opt-out.
6. **Behavior** — size-warning threshold, concurrency, max tokens per run, **exclusions** (folders, tags, `ai_editor: false` frontmatter flag; strip-frontmatter toggle), hotkey pointers for the keyboard triage commands. No telemetry.

All plugin text inputs (persona prompts, charters, push-back, refine, custom actions) support **`[[wikilink]]` autocomplete**; referenced notes are resolved and attached as context at run time.

### 5.8 Starter pack (shipped editors; all fully editable)

Drawn from Maggie's epistemic roles + the user's OSK skills/agents:

1. **Concision Editor** — cut flab, merge redundancy (Juri's demo).
2. **Devil's Advocate** — attack weak arguments, find holes (OSK Hater/Skeptic).
3. **Fact Checker** — flag uncited claims, dated stats, verify when backend has research capability (osk-knowledge-fact-check).
4. **Flow & Structure Editor** — transitions, ordering, scannability (OSK Editor).
5. **Humanizer** — AI-tell detection: metronomic rhythm, banned vocabulary, hedge-stacking (osk-writing-humanizer + osk-writing-rigor).
6. **Beginner Reader** — jargon, assumed context, accessibility (OSK Beginner).

Starter panel: **Pre-publish Review** = Devil's Advocate + Flow & Structure + Beginner Reader + Humanizer → scorecard with publish / needs-work / kill verdicts (mirrors osk-panel-publish).

## 6. Milestones (what, not when; each lands green: tsc + lint + test + build; reordered per the adversarial review)

- **M0 — Contracts & spikes**: locked invariants written into `documentation/` canonical docs; operation contract (discriminated ops + JSON Schemas + Zod, stable IDs, versioning); anchoring pipeline (snapshot hash, occurrence disambiguation, `ChangeDesc` mapping, stale detection) with property/fuzz tests; **three de-risking spikes**: (a) transport per provider (streaming vs buffered, CORS), (b) CM6 decorations + rail + tooltip in Live Preview AND Source mode against a markdown fixture corpus, (c) CLI headless protocol probe. `isDesktopOnly: true`.
- **M1 — Provider & settings vertical slice**: 1-n provider instances (Anthropic, OpenAI, OpenAI-compatible, Azure OpenAI, Ollama) with capability negotiation, health checks, normalized errors, `AbortSignal`; tabbed settings shell; minimal editor entity + starter pack seeding.
- **M2 — Buffered single-editor review**: Review/Cancel from the rail, span highlights, side panel findings list (also the narrow-layout fallback), whole-note + selection scope, size warning + confirm, exclusions + per-type kill switch.
- **M3 — Streaming & anchors under edit**: per-provider streaming decoders where verified, live badges, cancellation races, stale-marking while the user types, precondition-checked apply.
- **M4 — Cards, diffs & keyboard triage**: floating review cards (single active tooltip, collision-aware), non-destructive inline diff (source stays visible; CM6-undo-integrated Accept), per-finding threads, full keyboard triage state machine, bulk operations (per-editor accept/dismiss-all, accept-all-non-conflicting as one undoable transaction, severity filter), status-bar finding-count item, adaptive layout via `ResizeObserver`.
- **M5 — Editors/actions/context CRUD**: persona gallery (colors, prompt textarea + dedicated note-ref control), voice profile section, action→editor/panel bindings, custom actions, note-type binding rules + optional OSK auto-discovery adapter, `[[link]]`-in-inputs context with budget + "what will be sent" preview, import/export with validation, setup wizard.
- **M6 — Panels**: panel CRUD (1-n members, charter, per-panel aggregation backend), parallel member runs + typed `PanelResult` scorecard (verdicts, top fixes, dissent, partial-failure policy), editors-vs-panels visual distinction, "Generate more" / `InsertAt` continuation affordance.
- **M7 — CLI backends (opt-in)**: Claude Code + Codex adapters behind the security boundary (stdin content, isolated cwd, allowlisted env, read-only sandbox, process-tree kill, protocol conformance tests), separate consent for tool/research mode.
- **M8 — Durable margin comments**: sidecar repository (schema version, migrations, rename handling, corruption recovery), interrupted-job semantics on restart (Retry, never fake resumption), background runs with live timers, margin column UI.
- **M9 — Polish & release**: theming via Obsidian CSS vars, reduced-motion + ARIA + non-color persona indicators, performance passes (large notes, many findings), docs CONTENT (README, docs/ user guide — the docs site is already published and redeploys automatically on push), community-review checklist sweep. **Post-implementation adversarial codex review (same model/effort) before release.** **Sébastien handles personally (2026-07-29 — NOT agent scope, do not plan or attempt): pushing, marketplace submission, docs-site publishing, screenshots, video.**
    - **Support CTAs (fleet-wide convention, 2026-07-29)**: every surface a user can see must carry the same calls to action — newsletter, YouTube channel, Knowii community, GitHub Sponsors, Buy me a coffee. All 21 other plugin repos were migrated on 2026-07-29; **this repo was deliberately skipped to avoid interrupting in-flight work**, so it must catch up before release. `obsidian-plugin-template` is the canonical source for exact wording and code — copy from it rather than re-inventing (see its `TEMPLATE_USAGE.md` §4.2, "Support CTAs"). Seven surfaces:
        1. **`src/app/ui/support-links.ts`** — shared module holding `KNOWII_COMMUNITY_URL`, `GITHUB_SPONSORS_URL`, `BUY_ME_A_COFFEE_URL`, `YOUTUBE_CHANNEL_URL`, `NEWSLETTER_URL` plus `renderSupportSection(containerEl, renderBadge?)`. Single source of truth: everything else imports from it. Copy the file verbatim from the template.
        2. **`src/app/ui/whats-new-modal.ts`** — drop its local URL consts and import them from `./support-links` (the file is byte-identical across the fleet; keep it that way).
        3. **Settings tab** — the support section delegates to `renderSupportSection(containerEl, (el) => this.renderBuyMeACoffeeBadge(el))`. The badge asset stays plugin-local and is injected as a callback. Settings get opened repeatedly, unlike the one-shot what's-new dialog, so this is the highest-traffic in-app surface.
        4. **`manifest.json` → `fundingUrl`** — an **object** of labelled URLs (`"Join Knowii"`, `"GitHub Sponsors"`, `"Buy Me a Coffee"`), not a single string. Obsidian renders each entry in the community plugin browser and in the installed-plugin entry; it is the only CTA surface Obsidian itself provides.
        5. **`.github/FUNDING.yml`** — `github: [dsebastien]`, `buy_me_a_coffee: dsebastien`, and `custom:` with the Knowii and store URLs, driving GitHub's Sponsor button and dropdown.
        6. **`.github/release-footer.md` + `release.yml`** — the "Extract changelog for release notes" step appends `printf '\n---\n\n'` then `cat .github/release-footer.md` into `$GITHUB_OUTPUT`, so every GitHub release body carries the CTAs. Note the footer file must **not** start with `---` (Prettier mangles it as frontmatter); the separator is emitted by the workflow instead.
        7. **`README.md` + `docs/README.md` + `docs/_config.yml`** — both markdown files end with a `## News & support` section preceded by a `<!-- support-cta -->` marker (marker to next `##` = generated block, regenerable fleet-wide), and carry a `<!-- other-plugins:start -->` / `<!-- other-plugins:end -->` cross-promotion table generated from every plugin's `manifest.json` (name + description, current repo excluded). `docs/_config.yml` sets `footer_content` so just-the-docs renders the links at the bottom of **every** docs page. When this plugin ships, regenerate the other-plugins table in all repos so it appears in the fleet too.
    - **Value-moment support prompt (planned, tracked in the vault)**: a one-time, dismissible notice after N successful operations, leading with a concrete usage number before the ask. To be built in the template first and rolled out; if it lands before this plugin ships, adopt it here rather than writing a bespoke one.

## 7. Risks & mitigations

| Risk                                                                                       | Mitigation                                                                                                                                       |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Quote anchoring fails on markdown syntax mismatch (LLM quotes rendered text vs raw source) | Always send raw markdown to the model; instruct quoting verbatim from input; 3-stage matcher + graceful degradation; heavy test corpus           |
| CM6 overlay complexity (cards/diffs fighting Live Preview widgets)                         | Prototype the diff widget early (M1); prefer CM6-native decorations over DOM hacks                                                               |
| CLI backend fragility (paths, versions, auth)                                              | Health-check button, clear error surfaces, API backend always available as fallback                                                              |
| Cost runaway (N editors × long notes)                                                      | Scope control (selection/section), per-run token caps, count badge = findings only after completion, explicit Summon (never auto-run by default) |
| Findings spam / annoying UX                                                                | Severity filter, per-editor max findings, "fade on dismiss" everywhere, ignorable-by-design                                                      |
| Obsidian review guidelines (no `innerHTML`, sentence-case UI, no "Obsidian" in name/desc)  | Template conventions + review-lint before submission                                                                                             |

## 7b. Later: knowledge-integration features (post-v1, tracked as GitHub issues)

Beyond reviewing prose, editors should help integrate the note into the vault's knowledge graph:

1. **Tag review** — an editor reviews/improves the note's tags: suggests existing vault tags that fit (never inventing new ones without flagging), flags redundant/missing ones. Respects vault tag conventions.
2. **Related-notes discovery** — find vault notes relevant to the current note and propose them as wikilinks, targeted at a specific section (e.g. `## Related`); accept/reject per suggestion, like findings.
3. **References management** — add/update a full references list at the end of the note:
    - **Internal**: every vault note mentioned/linked in the body, consolidated.
    - **External**: URLs the user added manually + **every external source an LLM used as input/reference while editing/writing** (source-citation tracking: backends must report the sources behind their suggestions so citations are effortless and honest).
4. Prerequisite plumbing: the operation contract's evidence entries (review finding #27) double as the citation source for external references.

Additional post-v1 features (each a GitHub issue):

5. **Per-editor learning loop** — optional per editor: distill accept/reject patterns + push-back arguments into a memory store (user chooses per editor: plugin settings OR a dedicated vault note) that is injected into future runs. Transparent and editable — you can read exactly what an editor "learned".
6. **Interview-first drafting** — an action that runs a structured one-question-at-a-time interview (angle, audience, unique take) in the card/side panel before drafting; answers feed the generation context. Mirrors the Ghostwriter's "interview first, write later" rule; builds on the per-finding thread machinery.
7. **Community packs** — in-plugin pack browser with a rich explanatory UI (what each editor/pack is, what it's for). Packs are contributed via PRs to this repo under a `community/` parent folder (dedicated subfolders per editor and per pack); the plugin loads the registry from there. Contribution guide in the docs.
8. **Publish gates** — settings to define gates (designate a panel as gate, optionally per note-type rule); a public plugin API other plugins can call ("latest gate verdict for file X", "run gate"); status-bar verdict badge. First consumer: obsidian-ghost-publish ("Style Guard failed" warning before publishing).
9. **Metadata review** — editors propose frontmatter improvements: first description/title/aliases (prose-ish, publishing value), later any property as a structured, schema-aware diff (OSK type definitions when available). Companion of the tag-review issue.
10. **Session summary** — on-demand command: distill a review session (per-editor accepted/rejected counts, key changes) into a formatted digest, copied to clipboard or appended to a chosen note (e.g. the daily note).
11. **AI Providers plugin integration** — optional, feature-detected backend source: when https://github.com/pfrankov/obsidian-ai-providers is installed, its centrally-configured providers appear as selectable backends (configure keys once across plugins). Never required. GitHub issue #10.

## 8. Open questions (park for later; don't block M0-M1)

### Interaction surfaces design (M3 prerequisite — DONE 2026-07-29, see `interaction-surfaces-design.md`)

> Research + design completed and adversarially critiqued; the authoritative spec now lives in `documentation/plans/interaction-surfaces-design.md`. Headlines: no submenus (not public API), no default hotkeys (community guideline), stable command ids from entity UUIDs, CLI GO via public `registerCliHandler` (typings bump to 1.12.3 required), batch review deferred post-M4. The original scoping notes are kept below.

Design pass over the three ways users trigger actions, before wiring the M3 action surface:

1. **Context menus** — (a) editor context menu on a selection inside the note (the Juri/Epi surface: bound actions + "ask an editor" freeform); (b) file-explorer context menus on a note, folder, or multi-selection (a "collection"): run an editor/panel over a set of notes (batch review). Scope carefully: batch runs multiply cost — reuse the size-warning/confirm machinery per note.
2. **Command palette surface** — full command inventory: review note, review selection, each bound action as its own command (hotkey-assignable), cancel run, open panel, next/prev/accept/reject/dismiss finding triage commands, bulk operations.
3. **Obsidian CLI extensions** — register plugin-specific CLI handlers so reviews/actions can be driven from the terminal, following the pattern in obsidian-note-toolbar (https://github.com/chrisgurney/obsidian-note-toolbar/blob/master/src/Cli/CliHandlers.ts). NEEDS RESEARCH: how that plugin registers handlers with the Obsidian CLI, the handler contract, and what surface makes sense (e.g. `obsidian ai-editor:review file=... editors=...`, machine-readable findings output for scripting/agents). Not researched yet — do the research when this step starts.

### Action vocabulary expansion (planned step, post-M3)

The built-in verb list (rephrase, summarize, critique, say-more, find-evidence, identify-assumptions, simplify, humanize, continue) is a starting set, not a ceiling. A dedicated pass will expand it, candidates drawn from the OSK skill catalog and Juri/Maggie affordances: fact-check, add-examples, add-analogies, strengthen-hook, tighten-structure, extract-outline, translate, change-tone, make-scannable, title-suggestions, counter-argue, steelman, devils-advocate, explain-simpler (ELI5), add-transitions, remove-jargon. Selection criteria: each verb must be meaningfully distinct as a one-shot operation, benefit from a specific instruction prompt (vs just picking a different editor), and map cleanly onto the operation contract (transform-selection or review). Default bindings for new verbs ship unbound; the settings Actions tab lists them automatically from the schema.

- Diff granularity: word-level vs sentence-level rendering of red/green inline diff (leaning word-level, like Juri).
- Post-run usage display: show actual token usage per run (providers return it) — nice trust-builder, not required.
- Whether "Get research" (web) is API-backend-gated or CLI-only in practice.
- Publish gates: a "Style Guard" mode that blocks a publish pipeline (integration with obsidian-ghost-publish?) — later.
- Backlinks (not just outgoing links) as opt-in context.

## 9. References

- Juri Strumpflohner's Quill demos: x.com/juristr statuses 2074494746484236459, 2077036970895872368, 2079297727364464700
- Maggie Appleton, Language Model Sketchbook: https://maggieappleton.com/lm-sketchbook
- Vault: [[AI Assistant Architecture]], DeveloPassion Ghostwriter SOUL, osk-panel-publish, osk-writing-humanizer, osk-writing-rigor, developassion-style-police, user-voice-profile
- Obsidian plugin guidelines: https://docs.obsidian.md/Plugins/Releasing/Plugin+guidelines
